---
description: 分析栖最新对话与内部状态——用户说分析最新对话、评估内部状态、对话体检等时遵循（规范真源在 how-to 文档）
alwaysApply: false
---

# 栖 · 最新对话与内部状态分析（Cursor 捷径）

> 本规则**仅方便 Cursor**。规范真源是仓库文档（任意 Agent 都应读那份，不读本规则）：  
> `docs/how-to/ide-agent/IDE-Agent-分析栖最新对话与内部状态.md`

当用户要求分析/评估**最新对话**、逐句看栖的**内部状态**变化、或「对话体检」类表述时：

1. **先阅读并严格遵循**上述 how-to 文档（冲突以文档为准）
2. 只读 `data/qi.db`；默认不改代码、不改库、不写修复方案（除非用户另行要求）
3. 会话切分：消息间隔 >2h；取最后一场；每条栖回复对齐其后最近 `emotion_states`，并报 Δ
4. 输出：分析元信息（署名 Cursor）→ 概览 → 轨迹 → 分段逐轮表 → 关键发现 → 一句话总结；声称须有 message id / 时间戳 / 数值锚点
