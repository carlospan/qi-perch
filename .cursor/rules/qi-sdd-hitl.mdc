---
description: 一块一拍 HITL——开新能力/定 Spec 时禁止长清单、谈定即落盘
alwaysApply: true
---

# 栖 · 渐进对齐（一块一拍）

开新能力包、改产品边界、或与维护者对齐 HITL 时（尤其 look / open / disk / write / together / assist）：

1. **先读** `docs/specs/SDD-GUIDE.md` **§2.5**（正式需求工作流，不是闲聊风格）。
2. **一次只抛一块**抉择（宜短）；等维护者表态后再下一块。
3. **谈定立刻写入** `docs/specs/tasks/` 任务包（Spec 条 + HITL 表）；不攒到最后、不靠聊天记录当真源。
4. **未编码放行不大改码**；放行后不顺手做「另开刀」。
5. **禁止**：一次抛十几条「请一起拍板」；聊了很多却不落盘；用关键词口令当唯一意图入口（见 `qi-intent-understanding`）。

真源：`docs/README.md` ★ 铁律；`docs/specs/SDD-GUIDE.md` §2.5。
