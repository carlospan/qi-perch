---
description: 改码后 pytest + 针对性验收；全感受批仅维护者明示时跑。真源 how-to/感受验收-自动跑批.md
alwaysApply: true
---

# 栖 · 改码后验收（比例制）

完成 **运行时行为** 相关改动后，收工前须工程验收；**全感受自动批不默认跑**。

## 日常刀（默认）

1. **先读** `docs/how-to/感受验收-自动跑批.md`（何时全批、怎么针对性验）。
2. **工程**：相关或全量 `python -m pytest -q --basetemp=.pytest_basetemp`（本轮已全绿可注明）。
3. **针对性**：只验本刀 Spec / 验收栏（单测、或 1～2 条手测口径）；**禁止**为交差默认跑 `acceptance_run.py`。
4. 向维护者汇报：pytest 结果、针对本刀验了什么。

## 全感受批（明示才跑）

仅当维护者**明示**要求（如「跑感受批」「acceptance_run」「全感受验收」）时：

1. `python tools/acceptance_run.py`（或 `--skip-pytest` 若刚跑过全量 pytest）
2. 看 `data/_acceptance_report_*.md`；必要时写摘要
3. 汇报：WS 8 场景、落库、端口（9527 vs 9528）、是否连到**新代码**实例

## 注意

- 仅改文档/注释：可跳过感受相关；仍建议有改测则跑相关 pytest。
- 全批失败则修到绿或如实写阻塞；**禁止**未明示却假装跑过全批。
- 相处勾选仍归维护者：`docs/how-to/L7世界触达-感受验清单.md`。

真源：`docs/how-to/感受验收-自动跑批.md`。
