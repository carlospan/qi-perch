# 体系重构 · 步骤 A 成果 · Cursor 评审意见

> **用途**：转交 CodeBuddy / 维护者决定是否放行步骤 B；**仅评审，未改其它文件**。  
> **依据**：`步骤A-Cursor评审请求.md`、`体系重构方案.md`（v3）、commit `3261417`、实际 `docs/` 树。  
> **撰写**：Cursor（2026-08-02）

---

## 总评

**方向正确，步骤 A 可算达标，但建议「有条件放行步骤 B」——先修 3 个开工前必改项，再全量迁移。**

宪法 v3 分场景裁决、Diátaxis 映射、探针归类、acceptance / `_invariants` 空骨架纪律，均符合定稿 v3。主要风险不是分类错，而是：**旧路径副本仍在且无 stub → 已构成事实双源**；`stage-0` 文首「本文件为准」与架构 §五 仍全文并存措辞过满；宪法已链 `open-questions.md` 但文件尚未创建。

---

## 1. 方向校验

| 项 | 结论 |
|----|------|
| 目录骨架 | ✅ 符合 v3（含 `specs/archive`、`how-to/ui/assets`） |
| 宪法 v3 分场景裁决 | ✅ 已修正扁平总序；场景覆盖见 §4 |
| 架构方案 → `explanation/` | ✅ |
| contract → `reference/` | ✅ |
| stage-0 / 任务包 → `specs/` | ✅ |
| acceptance 命名（非 `tests/`） | ✅ |
| 空骨架不重写 C / 不复制 R | ✅ |

**无分类放错。** 探针选择（架构 + contract + stage-0 + 阶段二退出）合理。

---

## 2. 断链检查

| 引用 | 状态 |
|------|------|
| 新文件头「现行路径」注释（架构 / contract / 阶段二退出） | ✅ 有 |
| `specs/tasks/2026-08-02-阶段二退出.md` 依据仍写 `docs/design/…` | ⚠️ 旧路径；步骤 B 前应改为 `explanation/…`（或双写过渡） |
| 宪法指向 `explanation/栖·灵魂书.md` 等 | ⏳ 步骤 B 才迁入，**预期未到**；B 完成前 Agent 若 strictly 跟宪法会 404 |
| 宪法 / stage-0 指向 `specs/open-questions.md` | ❌ **文件不存在**（死链） |
| 根 `docs/contract.md`、旧 `design/架构方案` | ⚠️ 仍在，且 **旧副本无「已迁至」stub** |

新建文件内部：SDD-GUIDE / acceptance / `_invariants` 链到 `explanation/架构方案` — ✅ 可解析。  
**死链级别：`open-questions.md` 必须补骨架或从宪法/stage-0 暂时去掉硬链。**

---

## 3. 双源风险（步骤 A 阶段）

### 3.1 已存在的事实双源（严重）

下列成对文件正文并存，旧侧**没有**迁出 stub：

- `docs/design/栖·数字生命架构方案.md` ↔ `docs/explanation/栖·数字生命架构方案.md`
- `docs/contract.md` ↔ `docs/reference/contract.md`

习惯路径（design/、根 contract）仍会被打开 → **步骤 A 已制造双源**，不是「等步骤 B 才有」。

**开工前必改**：在旧路径文件顶部加 stub（`→ 已迁至 …；本文档步骤 B 删除`），或立刻改成仅重定向的短文件。新副本保持 canon。

### 3.2 stage-0 vs 架构 §五（中）

- 方案约定：步骤 B 才把架构 §五 退摘要+链接 —— ✅ 步骤 A 未提前删 §五，正文策略对。
- 但 `stage-0.md` 文首写：「架构方案 §五 阶段零退为摘要+链接，**本文件为准**」。

在 §五 仍全文时，这句**超前**，Agent 可能只信 stage-0、或两边都改。  

**建议改成**（步骤 B 前修正）：

> 目标态：本文件为阶段零施工权威；步骤 B 前架构方案 §五 仍保留全文并存。若冲突，以本文件与 progress 收官记录为准；步骤 B 将把架构 §五 退为摘要+链接。

### 3.3 阶段二退出包元数据过期（中）

`2026-08-02-阶段二退出.md` 文首仍写「闭环后按文档宪法**删除**」——与宪法 v3「归档 `specs/archive/` 不删」**冲突**。正文「依据」仍指 `design/`。  

搬家探针应至少改元数据三行（生命周期 + 依据路径），否则 B 全量时会复制错误习惯。

---

## 4. 宪法 v3 正确性

分场景表对照请求清单：

| 场景 | 宪法是否覆盖 | 指得对吗 |
|------|--------------|----------|
| 行为越界 → contract | ✅ | `reference/contract.md` |
| 工程纪律 → R 索引 | ✅ | `_invariants` → 架构 §七 |
| 语域 → 双轨 | ✅ | 第三节 |
| 价值 → 灵魂书 | ✅ | explanation/（B 后存在） |
| 生命定义 → 架构 | ✅ | |
| 阶段判据 → stages | ✅ | |
| C 定义 → 架构 §一 | ✅ | acceptance 只操作化 |
| 开放问题 → open-questions | ✅ 行在 | ❌ 文件未建 |

**小遗漏（非阻塞）**：未单列「config 默认值以 yaml 为准」裁决行（纪律在第四节有）。可在 B 补一行「配置默认值争议 → `settings.example.yaml`」。

---

## 5. 空骨架占位

| 文件 | 评价 |
|------|------|
| `acceptance.md` | ✅ 声明不重写 C；待填列表 + 链 §一/§六；适合 B 填满 |
| `_invariants.md` | ✅ 不变量表述不写死 190；工程纪律四条在；R 仅索引标题 |
| `open-questions.md` | ❌ **步骤 A 漏建骨架**（定稿 v3 与宪法已假定存在） |

---

## 6. 步骤 B 隐患预判（额外提醒）

1. **先 stub 旧路径，再搬其余文件**——否则迁移窗口期双源放大。  
2. **全库 `rg`**：`docs/design/`、`docs/contract.md`、`docs/dev/`、`docs/layers/`、`docs/learn/`，以及 `.cursor/`、领包模板、progress 史实（史实可保留旧路径 + 一句现行指向）。  
3. **`.html` → `how-to/ui/assets/`**（定稿偏好）；勿只丢进 `how-to/ui/` 根。  
4. **thoughts/README、learn/README** 并入后必须改卷内相对链接。  
5. **架构 §五 退摘要**：与 stage-0~4 **同一次提交**完成，避免半退状态；§二.5 病征标注已收官。  
6. **阶段二退出包**：B 前或 B 中同步改「删除→归档」与 design→explanation；并核对包 6/7/8 完工态与 progress 已拍板（#2/#4 观察项、日限 20）是否需在未来 `stage-2.md` 承接，避免任务包正文与退出规格再漂。  
7. **空目录**：确认 `specs/archive/`、`how-to/ui/assets/` 有 `.gitkeep` 或首个实文件，以免克隆后丢失。  
8. **体系重构任务包**状态仍写「A 进行中 / 宪法进行」——评审通过后应改为「A ✅ 待 B」。

---

## 7. 步骤 B 开工前须修正项清单

### 必改（建议挡住 B，直到勾完）

1. [ ] 旧 `docs/design/栖·数字生命架构方案.md`、旧 `docs/contract.md` 顶部加 **迁出 stub**（指向 explanation / reference）。  
2. [ ] 新建 `specs/open-questions.md` 空骨架（状态列表头 + Q0–Q6 占位），消掉宪法/stage-0 死链。  
3. [ ] 修正 `stage-0.md` 文首「已退摘要」超前表述（改为目标态 + B 前并存说明）。  
4. [ ] 修正 `2026-08-02-阶段二退出.md`：生命周期改为归档不删；依据路径改为 `explanation/…`。

### 建议改（可不挡 B，但成本低）

5. [ ] 宪法补一行：配置默认值争议 → `settings.example.yaml`。  
6. [ ] `specs/tasks/2026-08-02-体系重构v3.md` 状态改为「步骤 A ✅，待放行 B」。  
7. [ ] 空目录加 `.gitkeep`（若尚未入库）。

### 放行态度

- **修完必改 1–4 → 放行步骤 B。**  
- 方向无需返工；不必回滚 `3261417`。

---

*Cursor 评审意见 · 步骤 A · 2026-08-02*
