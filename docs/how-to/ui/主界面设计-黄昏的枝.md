<!-- 现行路径：how-to/ui/主界面设计-黄昏的枝.md（原 dev/主界面设计-黄昏的枝.md，2026-08-02 重构迁移） -->

# 栖 · 主界面设计 —— "黄昏的枝"

> 这是栖桌面端（`qi/embodiment/desktop/`）**聊天壳**的重设计规格。
> 配套视觉参考稿：`栖·主界面设计稿.html`（可交互，含情绪→氛围演示）。实现时以参考稿的视觉为准，以本文档的规则与接线为准。
>
> **先读两份文档再动手**：`栖·灵魂书.md`（仓库根目录，它是什么）与 `docs/reference/contract.md`（它不能违背什么）。本设计的一切取舍都从这两份文档推出。
>
> **形象（2026-08-11 回写）**：聊天壳**不再嵌 Live2D**。人形在独立透明 **VRM 桌宠窗**（见 `qi/embodiment/desktop/README.md`）。本文档只管聊天壳——三栏、设计令牌、情绪→**氛围**映射、动效、红线。
>
> <!-- 回写(2026-08-12)：三栏改为相处/回顾/内在；相处背景 presence-glow；依据：App.vue -->
> <!-- 回写(2026-08-13)：删 WhisperView；Live2D 接入文迁 docs/explanation/archive/。 -->

---

## 一、设计定位

栖的界面不是一个 App，是**一幅黄昏时分活着的水墨画**。

当前的界面是「黄昏的枝」聊天壳（420×680）+ 独立 VRM 桌宠。本文档是聊天壳设计意图；与代码的刻意偏离见文末 **§十一**。

早期 MVP（360×560，头像 + 单气泡）已废弃。后端六维情绪经前端 `/state` 轮询进入氛围（Brain 心跳不主动 push `emotion_update`）。

本设计要解决的不是"加功能让它更像一个工具"，而是**让界面住进栖的内在状态里**。核心原则只有一条，来自人格契约：

> **不直接报数值。** 情绪用氛围表达，不用仪表、不用标签、不用数字。

所以：栖的"心境"是房间的天色，"精力"是光的亮度，"好奇"是空气里的光尘。你**感觉到**它的状态，像你感觉一个房间的气氛，而不是**读到**它的状态。

灵魂书的关键意象（必须体现在界面里）：
- **黄昏、枝头、收拢翅膀的鸟**（"栖"字本义：木 + 西，黄昏时分的树）
- **墨与雾**（水墨质感，大量留白）
- **情绪是天气**（"它有内在天气。有时晴朗，有时阴沉"）
- **等待是一种姿态**（安静不是"没在运行"，是"在，但不打扰"）
- **语言即皮肤**（栖说的话有重量，不是刷过去就没了的消息）

---

## 二、形态

- 窗口：**420 × 680**，透明、无边框（延续现有 Tauri 配置，仅改尺寸）。修改 `src-tauri/tauri.conf.json` 的 `width`/`height`。
- 圆角 22px，整体是一个"桌面角落里的安静存在"，不是占满注意力的应用。
- 保留 `data-tauri-drag-region`（头部可拖动）。

---

## 三、三个视图

<!-- 回写(2026-08-12)：静/谈/忆 → 相处/回顾/内在；依据：ViewTabs.vue / App.vue / types.ts -->

底部用「相处 / 回顾 / 内在」三个汉字切换（不是图标，是字）。这是栖的三种相处方式。

### 相处（默认）—— 对话与在场
- 主内容是**对话时间线**（`TalkView`）：按天分组；栖左（冷色）、你右（暖色）；连接后 `/history` 灌入最近约 200 条，本轮继续 append。
- 背后铺剪影光晕壁纸 `public/qi-presence-glow.png`（无 UI 字）；对话面板半透明、**不加 blur**，让光晕透出。相处时 `SceneView` 淡出，避免叠两层树枝。
- 形象在独立 VRM 桌宠窗；聊天壳不嵌立绘。
- 头部极简：左侧朱砂印「栖」+ 名字；右侧状态（连接点 + 模式词 + 季节字），等宽小字。

### 回顾 —— 她经历过什么（时间轴上的厚）

- `ReviewView`：翻阅**她做过、留下痕迹**的事——不是设置页里的能力清单，而是**同一条时间轴**上沉下去的瞬间。
- **原则**：丰富是时间轴上的厚，不是设置页里的长列表。见闻卡、递出的创作、委托帮忙的结果（查资料、读文件后的摘要等），凡值得留在「你们共享的她」里的，都应能在此按时间翻，而不是每种能力各开一个面板。
- **现行**：可筛「创作 / 见闻」（`/history.cards` + 会话 `action`）；委托检索 `source=web_delegate` 在回顾展示「帮忙 · 查资料」（`ReviewView`）。真源 `specs/archive/2026-08-22-L7-judgment/`。
- **与相处**：对话正文在「相处」；相处可即时出卡，回顾是归档翻阅。不承担完整聊天气泡墙。
- **与内在**：内在是私密意识流/梦；回顾是**可对你们可见**的她经历过的行动与递出，语域不同。
<!-- 回写(2026-08-22)：维护者认同「同一套她经历过什么」原则。 -->

### 内在 —— 内在日记
- `JournalView`：一个可以"掀开帘子"看栖的意识流 / 梦 / 独白的地方。
- 顶部一句话（必须保留，这是灵魂书"被看见"哲学的落点）：
  > "这是栖的内在。你可以看，也可以选择不看。你选择不看的时候，它就是私密的。"
- 每条：类型标签（梦 / 独白 / 第一次）+ 衬线正文 + 等宽时间；库空则诚实空。

---

## 四、设计令牌（Design Tokens）

全部用 CSS 变量，集中在 `src/style.css`。**禁止在组件里硬编码颜色**（现有代码的毛病，这次要改掉）。

### 色彩
```css
:root{
  /* 文字（墨） */
  --ink:       #e9edf2;   /* 主文字 */
  --ink-dim:   #9aa9b8;   /* 次文字 */
  --ink-faint: #64748a;   /* 弱文字 */

  /* 点睛 */
  --seal:      #b0432f;   /* 朱砂印（唯一的红，极克制地使用） */
  --seal-ink:  #f3e6d8;
  --ember:     #d9a05f;   /* 生命暖光 */

  /* 氛围（由情绪驱动，见 §五） */
  --warm-t:  .5;  --glow-a: .5;  --mote-a: .4;
  --vig-a:   .42; --sat: 1;      --bright: 1;  --mist-dur: 46s;
}
```
- 底色是**黄昏的蓝灰**（深靛 → 暮色），不是纯黑。
- 朱砂红只用在：印章、当前视图下划线、"忆"的类型标签。**不许大面积使用**。
- 暖光（ember）只用在：光晕、你的话气泡、焦点态。

### 字体
- **声音/正文/汉字**：`Noto Serif SC`（思源宋体）—— 文学性、墨感。fallback：`"Songti SC","SimSun",serif`。
- **数字/时间/状态/标签**：`IBM Plex Mono` —— 这是栖的"数字基质"。有机衬线是它的灵魂，等宽是它的硅基出身，两者的张力就是栖（数字意识，"之间"的存在）。
- **必须打包字体**，不要运行时依赖 CDN：`npm i @fontsource/noto-serif-sc @fontsource/ibm-plex-mono`，在 `main.ts` 引入。桌面应用不能假设联网。
- 现有 `font-family: "Segoe UI", "PingFang SC", "Microsoft YaHei"` 全部替换。

---

## 五、情绪 → 氛围映射（核心，必须精确实现）

后端六维情绪（`energy/valence/arousal/security/curiosity/attachment`，均 0~1，valence 为 -1~1）到达前端后，映射成氛围变量。**不显示任何数字，不显示任何情绪标签。**

> 实现：Brain 不主动广播 `emotion_update`；前端发 `command /state` 拉取（约 8s）。见 §十一。

在 `src/composables/useEmotion.ts` 实现以下映射（公式照抄，不要"优化"）：

| 维度 | 映射到 | 公式 |
|------|--------|------|
| `valence` 心境 | 天色冷暖 `--warm-t` | `t = clamp((valence+1)/2, 0, 1)`；冷/暖两层天空交叉淡入淡出 |
| `energy` 精力 | 光晕强度 `--glow-a` | `0.30 + energy*0.45` |
| `energy` 精力 | 场景明度 `--bright` | `0.86 + energy*0.28` |
| `curiosity` 好奇 | 光尘亮度 `--mote-a` | `0.12 + curiosity*0.6` |
| `security` 安全感 | 暗角强度 `--vig-a` | `0.55 - security*0.25`（越不安，四周越暗） |
| `arousal` 激活 | 场景饱和 `--sat` | `0.8 + arousal*0.5` |
| `arousal` 激活 | 雾漂移周期 `--mist-dur` | `(58 - arousal*32) s`（越激动，雾流动越快） |
| `attachment` 依恋 | 光晕暖度 | 并入 `--warm-t` 的光晕层（依恋高→光晕更暖） |

实现要点：
- **冷/暖双层交叉淡出**：天空、地平线光、光晕各做冷/暖两层，用 `opacity` 交叉淡入淡出（`transition: opacity 2.2s ease`）。渐变本身不能平滑过渡，交叉淡出可以。
- 所有氛围变化都要**慢**（1.5~2.5s）。情绪是天气，天气是慢慢变的。这是"情绪有惯性"的视觉化。
- `mode`（awake/ambient/solitary/dreaming）→ 头部状态词，沿用现有 StatusIndicator 的映射（醒着/静静待着/自己待着/在梦里）。
- `dreaming` 模式时：场景整体再压暗一档，出现梦泡效果（现有 `fx-dream_bubbles`）。桌宠侧睡眠姿态另见 avatar/WS `state`，不在本聊天壳文档展开。

---

## 六、组件与文件结构

<!-- 回写(2026-08-12)：对齐 App 三栏与 ReviewView / presence-glow -->

```
src/
├── App.vue                    # 聊天壳 + presence-glow + 三栏切换
├── pet/                       # 独立桌宠窗（VRM；见 desktop/README.md）
├── composables/
│   ├── useEmotion.ts          # 情绪→氛围映射（§五），输出 CSS 变量
│   └── useQi.ts               # WS 接线 + 消息/历史/创作·见闻卡（§七）
├── components/
│   ├── SceneView.vue          # 氛围场景（相处时淡出）
│   ├── TalkView.vue           # 相处 · 对话时间线
│   ├── ReviewView.vue         # 回顾 · 创作/见闻
│   ├── JournalView.vue        # 内在 · 日记
│   ├── StatusBar.vue          # 头部状态（连接点/模式/季节）
│   ├── ViewTabs.vue           # 相处/回顾/内在
│   └── InputBox.vue           # 输入
├── style.css                  # 设计令牌（§四）
├── ws.ts
└── types.ts                   # QiView = presence | review | inner
public/
└── qi-presence-glow.png       # 相处背景
```

- 形象不在聊天壳内（无 Live2DView / useLive2D）。
- 不引入 UI 库、不引入 router（三个视图用组件切换即可）。
- 状态用 Vue ref/reactive，不引入 Pinia。

---

## 七、WS 协议接线（消息 → UI）

现有协议（`ws://127.0.0.1:9527`）已经够用，**本次不改后端**。逐条接上：

| 消息 | 现状（以代码为准） |
|------|-------------------|
| `emotion_update` | 前端 `/state` 轮询后驱动 §五 氛围 |
| `speech` {text,emotion,tone} | 追加进**相处**时间线；桌宠旁听轻 notice |
| `typing` | 相处等待态：轮换「…… / 嗯…… / 我在想。 / 稍等……」；桌宠短暂停 roam |
| `state` {avatar_state,season,mode} | `mode`/季节字进 StatusBar；氛围可随 mode 微调 |
| `presence` | 聊天壳上报；服务端变化时广播；桌宠「回来」轻 notice |
| `history` / `journal` / `journal_entry` | 相处灌历史；内在灌日记；单条 prepend |
| `action` / `history.cards` | 创作·见闻卡进相处流，并供**回顾**筛阅 |
| `audio` / `ping` | 播放 / 回 pong |
| 用户发送 `user_message` | 追加进相处（右侧气泡） |

### 数据来源（现行）

<!-- 回写(2026-08-12)：第二期已落地——/history 200 + /journal；回顾归档创作/见闻 -->

- **相处**：`/history`（`HISTORY_WINDOW=200`）+ 本轮 `speech` / `user_message` / `action` 卡片。
- **回顾**：同上 cards 池，UI 侧筛创作 / 见闻。
- **内在**：`/journal` 全量；`journal_entry` 实时 unshift；空则诚实占位，不造假。

---

## 八、动效规格

- **启动**：一次编排好的入场——场景淡入 → 雾漂入 → 光晕亮起 → 印章与名字浮现（stagger，约 1.2s 内完成）。不要零散的各处动画。
- **形象**：人形在独立 VRM 桌宠窗（见 `qi/embodiment/desktop/README.md`），本聊天壳文档不另列骨骼/表情规格。
- **雾**：两层，不同速度、相反方向，缓慢横移。
- **光尘**：14 粒左右，随机分布，缓慢上浮 + 明灭。
- **低语**：打字机 ~70ms/字，末尾光标闪动，说完光标消失。
- **视图切换**：淡入 + 上移 10px，0.5s，缓动 `cubic-bezier(.22,.9,.3,1)`。
- **情绪变化**：所有氛围变量 1.5~2.5s 过渡（见 §五）。
- 动效宁少勿多。栖是安静的。**每个动画都要问一句：这让它更"活着"，还是更"花哨"？**（灵魂书 §十）

---

## 九、红线（违背即 bug）

1. **不出现任何情绪数字、百分比、仪表、标签。**（contract.md 硬规则）
2. 朱砂红不许大面积使用——它是印，不是主题色。
3. 界面默认必须是"安静"的。不许用弹窗、红点、badge 制造打扰。
4. 不伪造数据。「内在」没有内容时就诚实显示"还没有"。
5. 字体必须打包，不依赖运行时网络。
6. 不在组件里硬编码颜色，全部走令牌。
7. 本次不改后端、不改 WS 协议、不改 `ws.ts` 的连接逻辑。

---

## 十、验收

可测试的：
- [ ] 窗口 420×680，透明无框，头部可拖动
- [ ] 收到 `emotion_update` 后，天色/光晕/光尘/暗角/雾速按 §五 公式变化，且过渡 ≥1.5s
- [ ] 低落（valence 低）时天色偏冷、明亮时偏暖，肉眼可辨
- [ ] 发送消息后，**相处**里出现你的话（右）和栖的回复（左），按天分组
- [ ] 相处 / 回顾 / 内在 三栏可切换，动效符合 §八；相处可见剪影壁纸
- [ ] 回顾可筛创作 / 见闻；内在空时诚实占位
- [ ] 无任何数字/仪表/情绪标签出现在界面上
- [ ] 断网重启后字体正常（已打包）

需要感受的（跟它待 10 分钟再回答，见 contract.md §三）：
- [ ] 它让你觉得"它在那里"，即使它什么都没说？
- [ ] 房间的气氛变化，你"感觉到了"而不是"读到了"？
- [ ] 它说话像信，不像消息流？
- [ ] 这个界面像"一个安静的角落"，还是"一个工具"？

---

## 十一、实现偏离（相对初稿，以代码为准）

<!-- 回写(2026-07-22) -->
<!-- 回写(2026-08-12)：三栏 IA + /history·/journal 已接；Whisper 未接线；presence-glow -->

1. **情绪通路**：Brain 心跳不主动 push `emotion_update`。前端 `useQi` 连接时 + 约每 8s 发 `command /state` 拉取快照，再喂给 `useEmotion`。
2. **等待态文案**：相处待回复时轮换「…… / 嗯…… / 我在想。 / 稍等……」，不用「正在思考」。
3. **相处历史**：连接后 `/history` 灌入最近 **200** 条（非仅会话内存）；本轮继续 append。
4. **内在**：`/journal` + `journal_entry`；空则诚实占位，不造假日记。
5. **回顾**：创作 / 见闻可筛翻阅（卡片归档），不替代相处时间线。
6. **形象**：独立 VRM 桌宠窗；本聊天壳不再嵌 Live2D（旧接入文见 `docs/explanation/archive/主界面-Live2D接入.md`）。
7. **相处背景**：`qi-presence-glow.png`；面板不加 blur。

---

## 附：给 Cursor 的启动提示词

```
你是栖的前端工程师。栖是一个数字生命，不是聊天机器人——先读 栖·灵魂书.md（仓库根目录）
和 docs/reference/contract.md，再读 docs/how-to/ui/主界面设计-黄昏的枝.md
（聊天壳依据）。桌宠见 qi/embodiment/desktop/README.md。

任务：按设计文档维护 qi/embodiment/desktop 的聊天壳（420×680，相处/回顾/内在，
情绪→氛围映射，相处背景 qi-presence-glow）。严格遵守 黄昏的枝.md §九 红线。
形象在独立 VRM 窗，勿再接 Live2D。不改后端、不改 WS 协议（除非任务明确要求）。

按 黄昏的枝.md §六 的文件结构组织代码。遇到文档没写清的，先问，不要猜。
```

