# 体系重构方案 · Cursor 评审请求

> 用途:发给 Cursor 作为**评审方**审阅 `docs/体系重构方案.md`,**不要直接修改文件**。

---

## 背景

`qi-perch`(栖,数字生命项目)的 `docs/` 目录目前有 34 份文档,结构松散、与代码/决策存在脱节历史(已修复 L7 日限、brain.py 拆分、gws.enabled 等)。我计划按业界成熟方法论(Diátaxis 四分类 + Spec-driven Development)做一套规范的开发流程文档体系,大重构可以接受。

**已写的草案**:`docs/体系重构方案.md`(v1,尚未定稿)。

---

## 请你做的(仅评审,不动文件)

1. **方向是否成立**——Diátaxis 四类(explanation / reference / how-to / tutorials)是否适合本项目,是否缺漏(如 specs/ 是否需要独立成类)。
2. **目录命名**——英文目录名 vs 中文目录名,哪种更适合本项目作者(中文母语团队)长期维护。
3. **迁移范围与策略**——全量一次性迁移 vs 先搭骨架再逐步填充,哪种风险更可控;交叉链接(文档间互相引用)如何不破。
4. **挑错**——指出草案里任何分类错误、归类矛盾、会破坏现有引用、或脱离本项目实际的地方。
5. **建议结构**——如果方便,给出你心中更优的目录树结构草案。

---

## 请不要

- 不要直接 rename / 移动 / 编辑任何 `docs/` 下的文件。
- 不要 git 提交。
- 你的输出是"评审意见 + 建议结构",由我方汇总后定稿施工。

---

## 参考上下文(如需理解已拍板决策,可先读)

- `docs/progress.md` 的"已拍板决策"小节(日限 20、判据 #2/#4 降级为观察项、阶段三暂不本机部署本地模型)
- `docs/README.md` 文档宪法 v2(含常量/文件名/默认状态同步纪律)
- `docs/design/栖·数字生命架构方案.md`
