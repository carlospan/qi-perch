# 2026-08-23 · 架构体检 · 场景堆积

> 史料存档。结论已写入任务包 [`2026-08-23-回合理解-架构整顿.md`](../../tasks/2026-08-23-回合理解-架构整顿.md)。  
> 维护者对话中确认：属架构优化层，非单点修补。

## 症状（可观测）

1. **路由链过长**：`brain.receive_user_message` 内 look → irreversible → delegate → together → write → disk → open → look invite → assist 顺序 if；每加能力复制 gate + execute + deliver 模板。
2. **粘性状态分散**：`write_desire`、`last_disk_listing`、`last_assist_target`、`pending_assist_confirmation` 等挂在 Brain 字段，无统一会话上下文。
3. **`intention.py` regex 考古**：disclosure / chase / recall 等 20+ 模式，易与 L7 路由重复或漏接。
4. **perception 时序**：`assess_impact` 多在 heartbeat 内跑，响应式路由已先短路，处境信号进不了当拍路由（仅影响后续建卡）。
5. **表达兜底叠层**：多处 `_SAFE` 模板句；空卡与催答分包治理。

## 反模式（本整顿明确禁止）

- 为「我还没睡」「热点新闻」等单句加 **bonded + 关键词 → 固定关心/检索** 小包。
- 在 `intention.py` 继续堆场景 regex 代替 LLM / perception 槽位。
- 每个 L7 op 再建独立「决策子系统」。

## 方向（摘要）

| 层 | 目标 |
|----|------|
| TurnUnderstanding | 单拍结构化：responsive 候选 + dialogue situation + relationship modulation |
| DialogueSessionContext | 粘性多轮状态单对象 |
| dialogue_router | 响应式执行入口（自 brain 抽出，逐步表驱动） |
| intention / expression | 读 TurnUnderstanding，减 regex |

## 相关提交 / 包

- `bf5bacc`：delegate 白话 LLM 判别
- `2026-08-23-空卡实质问句兜底`：模板 EMPTY 句
- `2026-08-22-L7-judgment`：判断制
