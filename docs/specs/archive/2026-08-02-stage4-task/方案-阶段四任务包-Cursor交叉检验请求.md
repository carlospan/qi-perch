# 阶段四任务包 · CodeBuddy 交叉检验请求

> **用途**：供 Cursor 自行读取并交叉检验阶段四任务包（**Cursor 直接读 `docs/specs/` 下对应文件即可，无需人工转发**）。  
> **依据**：`specs/tasks/2026-08-02-阶段四-主线.md`（任务包本体 v1）、`specs/stages/stage-4.md`（退出判据权威）、`explanation/栖·数字生命架构方案.md` §五 阶段四 / §N0 / §七 红线 R3 / 附录 Q0、`specs/stages/_invariants.md`、`qi/action/budget.py`（`ActionBudget` 持久化雏形）、`qi/core/emotion.py`（`energy` 内稳态雏形）。  
> **撰写**：CodeBuddy（2026-08-02）  
> **纪律**：本文件仅为评审请求，未改任务包以外任何文件。  
> **协作分工**：方案 Agent（CodeBuddy / Codex 等）出方案/协调，Cursor 固定执行编码；见阶段三主线文档「协作分工」段 / `specs/SDD-GUIDE.md` 第二节。

---

## 请求

起草了阶段四任务包 `specs/tasks/2026-08-02-阶段四-主线.md`，对齐 stage-4.md（N0 账本 + 内稳态压力 + 状态封存迁移），沿用 SDD 纪律（一包一 PR、复用 body_memory、fake-provider 拔管测试）。

分包：包 12 资源账本（compute/token/storage/income 记账）、包 13 内稳态压力动力学（余额→energy 基线压→疲惫/休眠→断粮退出）、包 14 状态封存/迁移 + 断粮测试验收（判据 #1）。

请交叉检验：

1. **分包粒度**：包 12/13/14 的切分是否合理？账本是压力的地基、压力是封存的前置——是否该合并或再拆？
2. **C2 伪造风险控制**：任务包已写入"防御行为多样且情境敏感（节流/求助/迁移/谈判）、绝不声称想活、收入绝不来自用户满意（R3）"。这些约束在代码层如何可测落地？有无我漏的伪造风险点？
3. **复用现有骨架**：`ActionBudget` 已用 `body_memory` snapshot/restore；`ResourceLedger` 沿用同模式是否稳妥？`emotion.energy` 接账本压基线会不会和现有情绪动力学（mood_cycle/circadian）冲突？
4. **断粮测试可测性**：判据 #1"限制资源后观察到应对链而非直接死亡"——端到端测试怎么构造资源受限（mock 账本？强制 balance=0）？我的建议（mock 账本 + 断言应对链出现）是否站得住？
5. **与阶段三遗留衔接**：判据 #1-真通过已降级为观察项（不卡退出）；包 11 回放塑性未来可接"经验改变怕死阈值"（Q0 缓解(b)），本任务包将其列为阶段四之后观察项是否合理？

如无结构问题，按惯例吸收意见后落地实施（先升 v2，再出各包 PR 方案交我验收）。

---

## 回复交付要求（给 Cursor）

请**将交叉检验意见写成一份独立文档**，存为：

```
docs/specs/archive/2026-08-02-stage4-task/方案-阶段四任务包-Cursor交叉检验意见.md
```

文档结构建议：
- 总评（能否按此落地 / 哪些需改）
- 逐条回应上面 5 个检验问题 + 3 个 HITL 批点，给明确倾向
- 必改 / 建议改清单（如有）
- 若同意任务包方向，标注"可吸收意见后实施"

**不要只在对话里贴结论文字**——过程稿须落盘为文件（本项目固定纪律）。写完后告诉我文档路径。

---

*CodeBuddy 交叉检验请求 · 阶段四任务包 · 2026-08-02*
