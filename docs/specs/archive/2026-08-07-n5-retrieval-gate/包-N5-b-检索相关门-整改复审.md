# N5-b 检索相关门——整改复审（Cursor）

> **角色**：Cursor（对照交叉审查 B 项 + 编码请求）  
> **对照**：PR 方案 v2、`包-N5-b-检索相关门-编码请求.md`  
> **时刻**：2026-08-07  

---

## 总判

**B1 / B2 已关闭，可编码；落地须按编码请求，并修正「保留」单测的假重叠假设。**

---

## B 项核对

| 项 | 状态 | 说明 |
|----|------|------|
| **B1** 全滤空不塞回 | ✅ | `return kept`；验收与 `test_return_empty_when_all_irrelevant` 对齐 |
| **B2** 全体低置信 | ✅ | §3.2 删除；编码请求写明不碰 `intention.py` |
| **B3** template 放大器 | ℹ️ 不阻塞 | 错记忆进不了卡即可；另开小包若仍见倾倒 |
| **B4** tasks 索引 | ✅ | 已列 N5-b |

编码请求中的 `_STOP_WORDS` 已写出（含「不要 / 其实 / 只是 / 一个」等），优于方案骨架。方案骨架仍残留无用 `MemoryManager` import——**以编码请求为准，勿照抄方案伪码里那行**。

---

## 编码时必须处理（非架构改向）

### T1. 「关键词」抽取：连续 CJK 段 ≠ 词

编码请求用的 `re.findall(r"[\u4e00-\u9fff]{2,}", …)` 取出的是**整段汉字串**（遇标点/空格才断），不是双字词。

后果：

1. `_STOP_WORDS` 几乎不起作用（整句「你不要放在心上」≠ 停用词「不要」）。  
2. 编码请求里「睡不着 / 躺着 / 强迫 有多词重叠」**不成立**——两侧连续段/双字交均为空，按现写法单测会红。

**落地约定（执行判断，不改任务方向）**：

- 用 **双字滑动窗口**（只取汉字）生成集合，再减 `_STOP_WORDS`；或等价的轻量切词，仍无新依赖。  
- 「保留」用例改为**确有字面重叠**，例如 query 含「助眠」、记忆含「助眠/躺着」。  
- 「过滤」用例（菜名 vs 重置/珍惜）在双字下仍应无交 → 返回 `[]`。

### T2. 任务包 Spec 措辞

`tasks/…N5-b….md` 仍写「过滤或降级」——与 B2 后「只过滤」略旧。编码不阻塞；方案 Agent 可顺手改一字。

---

## 进度建议

| 步骤 | 状态 |
|------|------|
| 整改复审 | ✅ 本文件 → **可编码** |
| Cursor 编码 | 按编码请求 + **T1** |
| 04 验收 | 待做 |

---

## 一句话

B1/B2 过了；动手时请用双字交（或保证单测真有字面重叠），不要照抄「睡不着∩躺着」那条假重叠。
