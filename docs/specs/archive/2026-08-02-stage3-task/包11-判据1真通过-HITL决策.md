# 包 11 · 判据 #1-真通过 —— 维护者 HITL 决策

> **用途**：阶段三唯一剩余硬决策点。包 11 已交付 **#1-地基**（语料版本化 + 异时测试骨架 + `run_training` 默认隔离），但 **#1-真通过**（异时响应可归因漂移）需维护者显式拍板。本文件供维护者二选一，拍板后由方案 Agent 回写权威文档。  
> **依据**：`specs/tasks/2026-08-02-阶段三-主线.md`（判据 #1 复核表 + 降级路径段）、`specs/stages/stage-3.md`（判据权威）、`specs/progress.md`（拍板记录）、阶段二路 B（降级先例）。  
> **撰写**：CodeBuddy（2026-08-02）  
> **纪律**：本决策须 HITL 显式拍板，**不得静默**；禁止仅靠"语料版本 diff"在任务包里宣称判据 #1 已过。

---

## 背景（已就绪的地基）

包 11 已落地并验收通过，提供了：
- `data/corpus/` 语料版本化（`corpus_{tag}_{ts}.jsonl`，可 diff）
- 异时测试骨架 `qi/learning/drift_check.py`：对比两版语料的 winner_kind / curiosity / world_surprise 分布与探针对差异（**不训也能跑，仅对比语料版本字段**）
- `ReplayBuffer.run_training(dry_run=True)`：默认只打印资源预估，**不触发训练**；真训分支仅占位 `not_implemented`，不 import 重依赖

**地基已满足判据 #1 的"可版本化 / 可 diff / 可显式调用且默认不进心跳"。** 本决策只解决"真通过"——即是否真的产生异时响应漂移。

---

## 两条路径（二选一）

### 路径 A：显式触发至少一次训练（判据 #1 真通过）

**前提**：硬件允许——显存 ≥8GB 或 3B 级模型流畅（已拍板重启条件）；维护者同意占用资源。

**动作**：
1. 接训练后端（peft/transformers 等）为**可选依赖**，单列 `requirements` 注释，不进主 `pyproject`。
2. 基于 `data/corpus/` 语料跑一次 LoRA / 等价微调 step（或参数固化）。
3. 用 `drift_check.py` 的扩展（喂真实模型输出）做**异时响应漂移对比**：同类探针输入，训前/训后响应出现可归因差异。
4. 漂移证据落盘（如 `data/corpus/drift_report_{ts}.json`）。

**回写**：`stage-3.md` 判据 #1 标"真通过（YYYY-MM-DD 显式训练）"；`progress.md` 记拍板句。

### 路径 B：降级为观察项（不真训）

**前提**：硬件长期无法真训（RTX 4060 4GB 显存 + 16GB 内存，常驻训练与日常多任务打架，且短期内无升级）；或维护者判断经验回放微调非阶段三必要退出条件。

**动作（仿阶段二路 B）**：
1. **不**靠"语料版本 diff"宣称判据 #1 已过。
2. 将 **#1-真通过降级为观察项**：阶段三退出不以真训为硬条件。
3. 回写 `stage-3.md` + `progress.md` 拍板句，明确"#1-真通过降级为观察项，原因：硬件/资源约束"。
4. 经验回放管线保留为"离线就绪"资产，未来资源允许时可再触发路径 A。

**回写**：`stage-3.md` 判据 #1 加注"真通过降级为观察项（YYYY-MM-DD 维护者拍板，硬件约束）"；`progress.md` 记拍板句。

---

## 决策影响

| 决策 | 阶段三退出 | 后续 |
|------|-----------|------|
| 路径 A | 判据 #1 真通过，阶段三可正式退出 | 进入阶段四；经验回放可常态化 |
| 路径 B | 判据 #1 真通过降级为观察项，阶段三**仍可按其余判据退出** | 包 11 管线留作离线资产；不阻塞后续阶段 |

> 无论 A/B，包 9/10/11(地基)/9b 均已验收通过；阶段三其余判据（#2 好奇驱动可见、#4 等）不受影响。

---

## 请维护者拍板

- [ ] **路径 A**：显式训练一次（资源允许时）
- [ ] **路径 B**：降级为观察项（硬件/资源约束）

拍板后，方案 Agent 执行对应回写（`stage-3.md` + `progress.md`），并把结论登记进本目录 INDEX 与阶段三主线文档。

---

*HITL 决策 · 判据 #1-真通过 · 2026-08-02*
