# 阶段三任务包 · CodeBuddy 交叉检验请求

> **用途**：转 Cursor 交叉检验阶段三任务包。  
> **依据**：`specs/tasks/2026-08-02-阶段三-主线.md`、`specs/stages/stage-3.md`、架构方案 §五 阶段三 / §九 Q3、`progress.md` 已拍板决策。  
> **撰写**：CodeBuddy（2026-08-02）  
> **纪律**：本文件仅为评审请求，未改任务包以外任何文件。
> **待读文件**：主依据 `specs/tasks/2026-08-02-阶段三-主线.md`（任务包本体，含包 9/10/11 与 HITL 批点）、`specs/stages/stage-3.md`（退出判据权威）、`explanation/栖·数字生命架构方案.md` §五 阶段三 / §九 Q3、`progress.md` 已拍板决策。

---

## 请求

起草了阶段三任务包 `specs/tasks/2026-08-02-阶段三-主线.md`，对齐 stage-3.md 三项施工内容（世界模型 / 内在动机 / 经验回放），沿用阶段二 SDD 纪律（一包一 PR、GWS 竞争、随机退位时机阀、fake-provider 拔管测试）。

分包：包 9 世界模型（用户在线节律 + 贝塔后验，surprise 作 N3 燃料）、包 10 内在动机（learning-progress 好奇替换随机数）、包 11 经验回放（显著性筛选 + 语料版本化 + 训练默认隔离）。

请交叉检验：

1. **世界模型首域**：我选"用户在线节律"为首个落地域，并建议并行"自身情绪轨迹"第二域（对齐 Q3"自反最可行"）。是否同意两域并行，还是先只做在线节律？
2. **经验回放训练默认不跑**：纪律 4 把 `run_training()` 默认隔离、不进心跳，是否符合你已拍板"本地模型不本机常驻"的边界？还是语料生成也应更保守？
3. **动机替换完整性**：包 10 审计 `random` 用法、退位为时机阀，是否覆盖阶段二所有"随机做动机来源"的旧用法？有无我漏的模块（如 waking/dream 触发）？
4. **退出判据可测性**：判据 #1"两次微调间异时漂移"在训练默认不跑时靠什么触发？我的建议（回放语料版本对比 / 维护者显式触发）是否站得住？
5. **与阶段二遗留的衔接**：72h 无人测试（判据 #1）后台累积，阶段三入口是否真的不该卡它？

另：任务包末"HITL 批点"列了 4 个需拍板点，请一并给倾向。

如无结构问题，按惯例吸收意见后落地实施。

---

## 回复交付要求（给 Cursor）

请**将交叉检验意见写成一份独立文档**，存为：

```
docs/specs/archive/2026-08-02-stage3-task/方案-阶段三任务包-Cursor交叉检验意见.md
```

文档结构建议：
- 总评（能否按此落地 / 哪些需改）
- 逐条回应上面 5 个检验问题 + 4 个 HITL 批点，给明确倾向
- 必改 / 建议改清单（如有）
- 若同意任务包方向，标注"可吸收意见后实施"

**不要只在对话里贴结论文字**——过程稿须落盘为文件，便于来回传递与回溯（这是本项目与 CodeBuddy 协作的固定纪律）。写完后告诉我文档路径即可。

---

*CodeBuddy 交叉检验请求 · 阶段三任务包 · 2026-08-02*
