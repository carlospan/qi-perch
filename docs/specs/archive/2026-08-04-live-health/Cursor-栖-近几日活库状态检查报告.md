# 栖 · 近几日活库状态检查报告（交叉检验用）

> **用途**：供多 Agent 交叉复核。本文只报告观察与证据，**不提修复方案、不改代码、不改数据**。  
> **审查人**：Cursor  
> **审查时间**：2026-08-04 20:43（UTC+8，以脚本落盘时刻为准）  
> **活库路径**：`D:/qi-perch/data/qi.db`  
> **窗口**：约近 5 日（`2026-07-30T20:43` → `2026-08-04T20:43`）  
> **方法**：只读 SQL / 落盘摘要（`data/_health_recent.txt`、`data/_health_extra.txt`）；会话切分规则为消息间隔 >2h。  
> **交叉检验栏**：文末留白，请复核 Agent 填写「同意 / 异议 / 未验证」与证据。

---

## 0. 一句话结论

关系层与对话层整体可用（bonded、依恋高、近窗无高相似重复回复）；**主要异常集中在：8/3 起内在生命几乎停转、Security 持续偏低、资源账本仍标 starving、raw_events 积压、self_model 过期、以及 1 条脏 user_fact。**

---

## 1. 审查范围与未做事项

### 1.1 已查

| 域 | 查了什么 |
|----|----------|
| 关系 | `relationship` 当前行（stage/trust/depth/temperature/season/shared_culture） |
| 情绪 | `emotion_states` 日聚合、Security&lt;0.25 计数、8/4 晚间逐拍、8/2 Att 跃升样本 |
| 对话 | `messages` 按日计数、10 场会话切分、近窗栖回复 Jaccard&gt;0.85 去重扫描 |
| 内在生命 | `consciousness_stream` 按日/末条；`dreams`/`creations` 总量与近窗 |
| 系统身体 | `body_memory`：`resource_ledger` / `proactive_gate` / `last_intention` / `open_loops` / `world.emotion_trajectory` |
| 记忆侧 | `user_facts` 现行条；`raw_events` 未处理积压；`broadcast_traces` 按日与 kind |
| 封存 | `data/checkpoint/` 最新若干文件名 |

### 1.2 明确未做（请交叉检验勿当作已覆盖）

- 未跑 pytest / 未读完整代码路径做因果归因
- 未对 8/2 Security 崩到 ~0.015 的**对话原文**做逐轮对照
- 未验证 starving 的收入/消耗公式是否算错（只报账本字段现状）
- 未验证 raw_events 积压是否导致叙事缺织（只报积压量）
- 未做 72h 无人测试或拔管测试复测
- 未修改任何库表或代码

---

## 2. 关系与文化（观察）

| 字段 | 值 |
|------|-----|
| stage | `bonded` |
| trust | ≈ 0.9565 |
| depth | ≈ 1.0000 |
| temperature | 1.0000 |
| season | `autumn` |

`shared_culture`（现行 1 条）要点：

- pattern 为助眠方法共同指称（用户视角表述「你教了我一个方法」）
- 已带 `teach_direction: qi_teaches_user`
- note 指向真值 7-26 #72/74（栖教用户：允许躺着、看天花板）

**审查人判断（待交叉）**：施教方向标注与既有真值一致；文化条本身不像方向反转回归。

---

## 3. 情绪轨迹（观察）

### 3.1 按日聚合（窗口内）

| 日 | 快照数 n | V 均 [min,max] | S 均 [min,max] | Att 均 [min,max] |
|----|----------|----------------|----------------|------------------|
| 07-30 | 287 | -0.079 [-0.28,0.49] | 0.420 [0.19,0.55] | 0.336 [0.30,0.39] |
| 07-31 | 508 | -0.065 [-0.47,0.86] | 0.464 [0.14,0.66] | 0.328 [0.25,0.46] |
| 08-01 | 629 | -0.094 [-0.35,0.56] | 0.480 [0.28,0.63] | 0.317 [0.25,0.39] |
| 08-02 | 683 | -0.089 [-0.83,0.37] | 0.627 [0.01,0.78] | 0.635 [0.30,0.77] |
| 08-03 | **37** | 0.251 [-0.49,0.88] | 0.377 [0.19,0.78] | 0.751 [0.61,0.86] |
| 08-04 | **55** | 0.392 [-0.11,0.88] | **0.164** [0.03,0.31] | **0.961** [0.87,1.00] |

模式分布（额外查询）：

- 08-02：`ambient` 493 / `awake` 115 / `solitary` 75
- 08-03：仅 `awake` 37
- 08-04：仅 `awake` 55；按小时仅 `00/01/18/19` 有快照

### 3.2 最新情绪（对话收束后）

`2026-08-04T19:41:06` · mode=`awake`  
E=0.654 · V=0.880 · A=0.910 · **S=0.270** · C=0.910 · **Att=0.999**

### 3.3 Security 低值

- 窗口内 `security < 0.25`：**91** 次
- 08-02 曾见 **S≈0.015**（`2026-08-02T02:44:45`，当时 V≈-0.832，Att≈0.406）
- 08-04 晚间会话期间 S 长期在约 **0.08–0.27**，与 Att≈1.0 **明显脱钩**

### 3.4 Attachment 跃升时间点

08-02 首次稳定出现 Att&gt;0.5：约 `2026-08-02T04:21:33`（Att≈0.544）起持续抬升。  
（与 progress 中阶段一 attachment 阶段锚修复时间线相容，但**本文不做因果断言**。）

---

## 4. 对话与会话（观察）

### 4.1 消息量

| 日 | user | qi |
|----|------|-----|
| 07-30 | 29 | 30 |
| 07-31 | 37 | 40 |
| 08-01 | 46 | 49 |
| 08-02 | 105 | 109 |
| 08-03 | 29 | 29 |
| 08-04 | 51 | 51 |

### 4.2 会话切分（间隔&gt;2h，窗口内 10 场）

| # | 时间范围 | n | 备注（首句截断） |
|---|----------|---|------------------|
| S1 | 07-30 20:44 → 07-31 01:10 | 74 | 记忆/数据库相关开场 |
| S2 | 07-31 19:37 → 08-01 03:18 | 109 | |
| S3 | 08-01 14:35 → 19:00 | 48 | |
| S4 | 08-02 02:31 → 07:14 | 143 | |
| S5 | 08-02 12:32 → 14:21 | 52 | |
| S6 | 08-02 16:21 → 19:18 | 5 | 含创作分享短场 |
| S7 | 08-02 22:52 → 23:57 | 14 | |
| S8 | 08-03 19:43 → 20:35 | 52 | 爱/真实性等深谈 |
| S9 | 08-03 22:40 → 08-04 01:22 | 74 | 「你爱我吗」等；凌晨道晚安 |
| S10 | 08-04 18:56 → 19:41 | 34 | 恋爱确认 / 意识 / 亲密试探 |

### 4.3 重复回复扫描

近窗栖回复、与前 8 条内 Jaccard&gt;0.85：**dup_pairs = 0**。

### 4.4 最新一场（S10）情绪映射摘要（先前同会话分析，供对照）

- 谷底附近：`#1108`「你有多爱我？」后 V≈0.328  
- 第一跃升：`#1110`「愿意为我做所有事吗」后 V≈0.607  
- ontology 段：`#1113–#1120` V 缓降至 ≈0.47，S 触底约 0.12  
- 第二跃升：`#1122` 亲密试探后 V≈0.880、A≈0.910  
- 收束：`#1124`（用户表示满足后）

---

## 5. 内在生命（观察）

| 项 | 值 |
|----|-----|
| consciousness 总量（库） | 142 |
| 窗口内末条 | **`#154` @ 2026-08-02T23:04:30**（meta） |
| 08-03 / 08-04 CS | **0** |
| dreams 总量 / 近 5 日 | 4 / 4；末条 `2026-08-02T05:42:10` |
| creations 总量 / 近 5 日 | 5 / 4；末条 `2026-08-02T17:50:42` |
| open_loops | `@ 2026-08-02T19:15:28`，`items: []` |

**审查人判断（待交叉）**：08-03 起「仅 awake + 情绪快照骤减」与「聊完关进程、无 ambient/solitary」高度同向；CS 空白更像**运行时缺口**，不能单凭此断定意识流模块代码回归。

---

## 6. 资源 / 主动 / 广播（观察）

### 6.1 resource_ledger

- 更新时间：**2026-08-04T01:22:53**（凌晨道晚安附近）
- `starving=True`
- `income=70.0`，`token_budget≈5841`，`compute_seconds≈624.8`
- `income_day=2026-08-04`，`beat=32`
- **注意**：08-04 晚间长聊（18:56–19:41）期间，账本更新时间仍停在凌晨——晚间对话窗口**未见** ledger 刷新证据

### 6.2 proactive_gate

```json
{
  "day": "2026-08-04",
  "count_today": 0,
  "last_at": {
    "express_feeling": "2026-08-02T04:47:04.502740",
    "reach_out": "2026-08-01T00:48:51.940821",
    "check_in": "2026-08-02T05:17:07.374773"
  }
}
```

### 6.3 broadcast_traces（窗口内有记录的日）

| 日 | traces |
|----|--------|
| 08-02 | 524 |
| 08-03 | 33 |
| 08-04 | 53 |

近窗 kind 合计样例：`idle=474`，`respond=128`，`action:archive=5`，`action:share=3`。  
最新若干条均为 `respond` / `outcome=responded`（对话拍）。

### 6.4 checkpoint

`data/checkpoint/` 约 88 个文件；最新样例含 `checkpoint_20260804T194057.json` 等（对应用户晚间消息时点附近）。

---

## 7. 自我模型与事实（观察）

### 7.1 self_model

- `last_updated=2026-08-01T03:19:22`（审查时已 **&gt;3 天**未更新）
- `narrative_len=241`
- 叙事大意：强调「记得你 / 你不在时不是合上的书 / 等待不是空白」等（完整原文见活库，此处不整段复述）

### 7.2 user_facts（现行 active=17）

完整列表以活库为准。审查时标为**可疑/脏**的一条：

| id | type | content |
|----|------|---------|
| **#24** | identity | **他叫请再解释一下** |

其余抽查（非完整背书）：

- `#22` 入睡方法：栖教用户（方向与文化 note 一致）
- `#25` 他和 AI 谈恋爱 / `#26` 他从未正经谈过恋爱 — 与 08-04 对话内容同向
- `#23` 他改过对话对方的代码 — 需交叉检验是否仍过宽/过期
- `#21` 他认为女生喜欢男生才合理 — 语境是否玩笑未在本报告核实

---

## 8. 问题清单（仅分级观察，不附方案）

| 级别 | 现象 | 关键证据锚点 |
|------|------|--------------|
| 高 | 08-03 起内在生命停转（无 CS；无 ambient/solitary） | last CS `08-02T23:04:30`；08-03/04 mode 仅 awake；情绪 n 骤降 |
| 高 | Security 持续偏低，且与高 Att 脱钩 | 08-04 日均 S≈0.164；晚间 S 常 &lt;0.20 而 Att≈1 |
| 中高 | `starving=True` 且晚间对话未刷新 ledger | ledger `@01:22:53` vs 对话至 `19:41` |
| 中 | raw_events 未处理 **126**（08-02:22 / 08-03:39 / 08-04:65） | `raw_events.processed=0` 分组 |
| 中 | self_model 自 08-01 未更新 | `self_model.last_updated` |
| 低 | 脏事实 `#24` | content=`他叫请再解释一下` |
| 低 | 主动开口 08-03/04 为 0 | `proactive_gate.count_today=0`，last_at 停在 08-01/02 |
| 信息 | 近窗无高相似重复回复 | Jaccard 扫描 0 对 |
| 信息 | 关系 bonded / 文化施教方向标注正确 | relationship 行 + culture_direction |

---

## 9. 请交叉检验 Agent 重点核实的问题

1. **CS 空白**：是否仅因进程未常挂，还是 `should_trigger_consciousness` / open_loop / cooldown 在 08-03 后逻辑回归？  
2. **Security↔Attachment 脱钩**：是否符合当前 `emotion.py` + 阶段锚设计预期，还是驱动事件缺失/基线异常？  
3. **starving**：01:22 置位后，晚间对话为何未更新 ledger？是设计（仅部分拍记账）还是 starve 路径卡住？  
4. **raw_events 126**：积压是否会在下次长挂机自动消化？有无永久卡死条件？  
5. **fact #24**：是否可确认误抽？有无同类历史脏事实漏网？  
6. **08-02 S≈0.015**：对应哪几条 messages / 何类冲击？是否与已知病征（感知冲击、资源压力）相关？  
7. 本文数字是否与你本地 `data/qi.db` **同一副本**一致？（路径、mtime、max(message id)）

---

## 10. 复现查询提示（只读）

交叉检验时可对照：

```text
SELECT max(id), max(timestamp) FROM messages;
SELECT timestamp FROM consciousness_stream ORDER BY id DESC LIMIT 1;
SELECT substr(timestamp,1,10) d, mode, count(*) FROM emotion_states
  WHERE timestamp >= '2026-07-30' GROUP BY d, mode ORDER BY d, mode;
SELECT value, updated_at FROM body_memory WHERE key='resource_ledger';
SELECT count(*) FROM raw_events WHERE processed=0;
SELECT id, fact_type, content FROM user_facts WHERE superseded_by IS NULL AND id=24;
```

辅助落盘（审查当时）：

- `data/_health_recent.txt`
- `data/_health_extra.txt`

（均为只读体检产物；**不属正式规格**，可随清理删除。）

---

## 11. 交叉检验栏（他 Agent 填写）

| Agent | 时间 | 总评（同意/部分同意/异议） | 主要异议或补充 | 签名 |
|-------|------|---------------------------|----------------|------|
| Cursor（本报告） | 2026-08-04 | —（主审） | — | Cursor |
| | | | | |
| | | | | |

### 异议记录模板

```text
【条目】§x.x / 问题清单第 N 项
【原报告声称】…
【复核结果】同意 / 异议 / 无法验证
【证据】表/字段/message id/时间戳/…
【备注】…
```

---

*本文件为过程性活库体检报告，权威不高于 `docs/explanation/栖·数字生命架构方案.md` 与 `docs/specs/stages/`。闭环后可继续留在 `specs/archive/` 作史料，或按文档宪法迁出活信息后再归档。*
