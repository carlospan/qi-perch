# 体系重构方案 · 整改 v3 · Cursor 评审请求

> **用途**：转交 Cursor 作为**评审方**审阅 `docs/` 体系重构方案（含整改 v3），**不要直接修改文件**。
> **撰写**：CodeBuddy（2026-08-02）

---

## 背景

`qi-perch`（栖，数字生命项目）的 `docs/` 目录计划按业界成熟方法论（Diátaxis 四分类 + Spec-driven Development）做体系化重构。已有一份定稿 v2 方案，但维护者指出“阶段零/一被漏”，遂做了一次**对照架构方案全文 + 实际目录**的系统审查，产出整改 v3。

**请审阅的文件**（均在 `docs/` 下）：
- `体系重构方案.md`（v2 定稿，主方案）
- `体系重构方案-整改v3.md`（**本次重点**，对照架构方案审查出的 6 处盲区 + 整改动作）
- `体系重构方案-Cursor评审请求.md` / `体系重构方案-Cursor评审意见.md`（上一轮评审往来，供你了解上下文）

**权威参照（判断“漏没漏”的基准）**：
- `docs/design/栖·数字生命架构方案.md`（v3，§〇–§九 + 附录）——重构必须完整覆盖其全部契约内容
- 实际 `docs/` 目录：`design/ dev/ layers/ learn/ thoughts/` + 根文件

---

## 请你做的（仅评审，不动文件）

1. **复核整改 v3 的缺陷 1**：架构方案 §六（五测试）、§七（红线 R1–R5）、§九（开放问题 Q0–Q6）、§五（全程不变量）这四类权威内容，整改方案建议的归宿（`specs/tests/`、`specs/open-questions.md`、`specs/stages/_invariants.md`、红线层级划分）是否合理？`tests/` 独立成文件 vs 并入 `SDD-GUIDE.md` 哪种更稳、更不双源？
2. **红线层级**：R1–R5（工程纪律）与 `contract.md`（硬红线）怎么划层级最不双源？统一入口应写哪？
3. **缺陷 6**：`dev/栖·主界面设计稿.html` 这种前端设计稿，归宿应是 `reference/` 还是 `how-to/ui/assets/`？`.html` 与 `.md` 接入说明是否该分开放？
4. **兜底复核（最重要）**：请**逐节对照架构方案 §〇–§九 + 附录**，确认 v2 + 整改 v3 是否已完整覆盖所有应受重构管理的契约内容。还有没有任何“像阶段零/一那样被漏掉”的内容？列出你发现的任何新增遗漏。
5. **命名/范围**：对 v2 已定的“目录英文+文件名中文、先步骤 A 探针再步骤 B”有无异议？

---

## 请不要

- 不要直接 rename / 移动 / 编辑任何 `docs/` 下的文件。
- 不要 git 提交。
- 你的输出是“评审意见 + 建议结构 + 新增遗漏清单”，由我方汇总后定稿施工。

---

*体系重构方案 · 整改 v3 · Cursor 评审请求（2026-08-02）*
