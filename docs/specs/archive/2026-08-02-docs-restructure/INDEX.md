# archive/2026-08-02-docs-restructure · 体系重构史料

> 本目录归档 `docs/` 体系重构（Diátaxis 四分类 + SDD）的过程终态。
> 归档不删（对齐 OpenSpec / GitHub Spec Kit）；中间往来稿已精选剔除，仅留定稿 + 终验闭环。
> 文档链接 CI 化、阶段三任务包已各自独立分桶（见 `../` 总索引）。

## 包含

- `体系重构方案-整改v3.md` —— 整改 v3 定稿（对照架构方案全文审查出的盲区与整改，已吸收进 `docs/体系重构方案.md` 定稿 v3）
- `步骤B-最终验收-Cursor确认闭环.md` —— Cursor 确认体系重构正式闭环

## 脉络

1. 体系重构方案 v1/v2 → 整改 v3 → 定稿 v3（`docs/体系重构方案.md`，`838e140`）
2. 步骤 A 搭骨架（`3261417`）→ 步骤 B 全量迁移 + 收口（`ce2b139`）
3. 最终验收收口（`6a8a933`，7 必改 + 3 建议清零）→ Cursor 确认闭环

> 体系重构全过程共 5 次提交，于 `6a8a933` 闭环。现行有效文档：`docs/体系重构方案.md`（定稿 v3）、`docs/README.md`（宪法 v3）、`docs/specs/` 下各规格。
