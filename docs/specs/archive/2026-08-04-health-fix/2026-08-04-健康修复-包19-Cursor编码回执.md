# 包 19 实施 PR · Cursor 编码回执

> **用途**：开工前理解确认 + 完工结果；交方案 Agent（Qoder）实施验收。  
> **依据**：同桶 `2026-08-04-健康修复-包19-PR方案.md`、`2026-08-04-健康修复-包19-主线.md`（2026-08-07 自 tasks 归档）。  
> **撰写**：Cursor（2026-08-04）

---

## 【开工前理解确认段】

已读取包 19。理解四块落地（顺序 B→A→C→D）：

1. **19-B 人名门控**：`looks_like_person_name` 主防线改为纯汉字 2–4 字 / 含「·」3–12 字；黑名单补「请/再/解释/一下/存在/本地」。不放宽回 `{1,6}`。
2. **19-A 清存量**：`tools/repair_dirty_facts.py` 预演/`--apply`，用新门控动态判定 active identity，`retire`（superseded_by=自身）。
3. **19-C 后台节奏**：`memory_decay` 接 `_resume_interval_wait`；`narrative_weaving` 启动积压 `first_pass` 立即织。
4. **19-D 反思标志**：`_pending_major` → body_memory `self_model_pending_major`；`mark_major_event` / `note_emotion_surge` 改 async；brain / inner_life 调用点 await。

红线：不改 `DECAY_RATES` / `COUPLING` / `STAGE_BASELINES`；不改 weave/decay 公式本体；不自动跑修复脚本；不碰 starving（H1 观察）。

本段写完即继续写码。

---

## 【完工结果段】

### 变更文件列表

| 文件 | 说明 |
|------|------|
| `qi/memory/facts.py` | 19-B 人名门控收紧 |
| `tools/repair_dirty_facts.py` | 19-A 新建清存量脚本 |
| `qi/core/brain_background.py` | 19-C memory_decay resume + weave first_pass |
| `qi/inner_life/self_model.py` | 19-D pending_major 持久化 |
| `qi/core/brain.py` | `await mark_major_event()` |
| `qi/inner_life/__init__.py` | `await note_emotion_surge()` |
| `tests/test_user_facts.py` | 门控用例扩展 |
| `tests/test_weave_schedule.py` | first_pass + resume 断言 |
| `tests/test_self_model_fields.py` | 持久化全链路 |
| 本回执 | |

### 关键实现决策

- 清存量**不写死 id**；活库实测仅剩 **id=24**「他叫请再解释一下」需 retire（id 2/3/7/18 此前已被包 16 等清理）。
- 活库 active identity 中**未见「阿振」**（仅余 id=20「他是男生」）；脚本打警告但不硬失败——与方案「预期命中 5 条 + 保留 id1」有偏差，属**数据现状偏离**，门控逻辑与脚本判定仍按方案。
- `get_body_memory("1")` 经 `json.loads` 可能得 `int 1`；加载/清除均按 falsy/`"1"|1` 兼容。
- `test_weave_backlog_uses_short_cycle` 按方案改为首轮 `sleeps==[]`；另增第二轮才睡 backlog_interval 的用例。

### 19-A 活库操作

```text
预演：命中 id=24（frag=请再解释一下）
--apply：retire 1 条；复查剩余 active identity：id=20 他是男生
```

### 测试命令与结果

```text
python -m pytest tests/test_user_facts.py::test_looks_like_person_name_gate tests/test_weave_schedule.py tests/test_self_model_fields.py -q
→ 9 passed

python -m pytest -q
→ 430 passed in 49.23s   （基线 426，不回退）

python -m ruff check <改动文件>
→ All checks passed!

python tools/verify_package.py --full
→ 全部通过（430 passed）
```

### 红线自检

- `git diff`：`qi/core/emotion.py` / `STAGE_BASELINES` / `DECAY_RATES` / `COUPLING` **零改动**
- 未改 `weave_narrative` / `decay_narrative_strengths` 公式
- 修复脚本未接入 CI / 心跳

### 验收勾选（Cursor）

- [x] 19-B：拒收/放行用例通过；未放宽回 `{1,6}`
- [x] 19-A：预演+`--apply` 完成（活库命中 1 条 id=24；方案写的 5 条为历史预期，见上偏离说明）
- [x] 19-C：resume + first_pass 测试证明
- [x] 19-D：置位→新实例→reflect→清标志；两处 await
- [x] 情绪动力学零改动
- [x] pytest / ruff / verify_package --full 通过
- [x] 本回执落盘

### 未做 / 交维护者

- H1 security 低位：未实施（按方案）
- H2 整夜挂机复测：交维护者

---

## 方案 Agent 验收栏（Cursor 勿填）

| 项 | 结果 |
|----|------|
| 对照方案四子项 | ✅ 全部符合（独立实测：git diff 逐行核对 + 活库查询 + 重跑 pytest 430 passed / ruff 绿 / verify_package --full 通过） |
| 活库偏离是否可接受 | 偏离 1（命中 1 条非 5 条）✅ 可接受：id 2/3/7/18 已被包 16 自退，动态判定无错。偏离 2（真名不在 active）❗升级为缺陷：实测 id=1「他叫阿振」被脏事实 id=18 superseded 顶掉（历史遗留），当前 active identity 无任何姓名事实——栖已丢失用户真名，需补修恢复 |
| 总评 | **验收通过**（代码侧零偏离零越界）；真名恢复作为验收发现的数据缺陷另开修复脚本闭环，详见 `2026-08-04-健康修复-包19-验收记录.md` |

---

*包 19 · Cursor 编码回执 · 2026-08-04*
