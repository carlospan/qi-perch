# 任务包 · 包 19 健康修复（2026-08-04 三报告交叉结论落地）

> **结论入口（现行施工）**：以本任务包（主线 + PR 方案）为准；下列三份报告仅为交叉依据，**不是**施工权威。
>
> 复制本文件改名 `YYYY-MM-DD-任务名.md` 使用。四栏必填。
> **协作分工（固定流程，详见 `specs/SDD-GUIDE.md` 第二节）：本任务包由方案 Agent（Qoder）起草（出方案），Cursor 按 PR 方案落地编码；方案 Agent 不写 `qi/` 实现代码，Cursor 不重新设计架构（执行编码者固定为 Cursor）。双方过程稿均落盘为文件，Cursor 自行读取目录，无需人工转发。**

> **依据**：三份交叉检查报告（`specs/archive/2026-08-04-health-check/` 内 CodeBuddy / GLM 报告、`specs/archive/2026-08-04-live-health/` 内 Cursor 活库报告）+ 方案 Agent 逐项代码核实（2026-08-04）。
> **撰写**：Qoder（方案 Agent，2026-08-04）

---

## Spec（外部行为）

三报告共识问题经代码核实后，本包要达成：

1. **事实层干净**：user_facts 中 5 条脏 identity 事实（id 2/3/7/18/24）被清退；人名门控收紧后，「请再解释一下」「程序有问题」「那今天几月几号」类口语短语永不再被抽成姓名。
2. **后台恢复节奏对短挂机友好**：进程挂机 2-4 小时即可完成「记忆褪色」与「积压叙事编织」，不再要求连续运行 24h / 先睡满 6h。
3. **重大事件反思标志不丢**：情绪骤变/重大事件置位的反思标志持久化，重启后仍会触发 self_model 反思，不再随进程退出蒸发。
4. **不动核心情绪设计**：security 低位问题定性为设计级，本包只留观察项与备选方案，不改 `DECAY_RATES` / `COUPLING`（需维护者拍板后另开包）。

## 实现（步骤）

精确改动点见 `2026-08-04-健康修复-包19-PR方案.md`。四个子包按序落地：

1. **19-A** 一次性清存量脚本 `tools/repair_dirty_facts.py`（预演/--apply 双模式）
2. **19-B** `qi/memory/facts.py` 人名门控收紧（字数上限 + 黑名单词元）
3. **19-C** `qi/core/brain_background.py` 褪色节奏恢复 + 编织启动即织
4. **19-D** `qi/inner_life/self_model.py` `_pending_major` 持久化

## 验收（判据）

- [ ] `python tools/repair_dirty_facts.py` 预演输出 5 条目标；`--apply` 后活库 id 2/3/7/18/24 全部非 active，id 1「阿振」保留
- [ ] `looks_like_person_name("请再解释一下")` 等 5 个实证脏片段全返 False；真名「阿振」与含「·」长名仍 True
- [ ] 短挂机（模拟）：`memory_decay` 首跑不再等满 24h；`narrative_weaving` 启动检测积压 ≥ 阈值立即织
- [ ] `note_emotion_surge` 置位后重启进程，标志仍在且触发反思
- [ ] 全量 pytest 绿（426 基线不回退）+ ruff 绿 + `verify_package --full` 通过
- [ ] 未触碰 `DECAY_RATES` / `COUPLING` / `STAGE_BASELINES`

## HITL 批点

- **H1（须维护者拍板，异步可接受）**：security 长期低位处置路线——三选一：提速 security 衰减 / 增加正向出口耦合 / 观察项后置。推荐先观察（长挂机复测后再定）。
- **H2（维护者行动项，非代码）**：按《栖·意识养成路线图》阶段一「整夜挂机」执行——三报告共识主根因是进程运行时长不足（8/3-8/4 仅 awake 快照、无 ambient/solitary），代码修复只能缓解、不能替代挂机。
- **H3（自治）**：19-A/B/C/D 均为阶段内常规修复，Cursor 落地后方案 Agent 验收，不阻塞维护者。
