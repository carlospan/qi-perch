# 包 19 实施验收记录（方案 Agent）

> **验收人**：Qoder（方案 Agent）
> **验收时间**：2026-08-04
> **依据**：`2026-08-04-健康修复-包19-Cursor编码回执.md` + 独立实测（活库查询 / git diff / 全量测试重跑）

---

## 一、对照方案四子项（逐项实测）

| 子项 | 验收方式 | 结果 |
|------|---------|------|
| 19-B 门控收紧 | git diff `qi/memory/facts.py` | ✅ 纯汉字 `{2,4}`、含「·」`{3,12}`、黑名单补「请/再/解释/一下/存在/本地」，与方案逐字一致；未放宽回 `{1,6}` |
| 19-A 清存量 | 活库实测 `user_facts` | ✅ id=24「请再解释一下」已 `retired(by=24)`；脚本动态判定机制正确 |
| 19-C 后台节奏 | git diff `qi/core/brain_background.py` | ✅ `memory_decay` 接 `_resume_interval_wait("last_memory_decay")` + `_mark_interval_done`；`narrative_weaving` `first_pass` 首轮不睡（含 continue 分支也置 False，无遗漏） |
| 19-D 反思标志 | git diff `self_model.py` 等 3 文件 | ✅ `PENDING_MAJOR_KEY` 持久化；`mark_major_event`/`note_emotion_surge` 改 async；`_load_pending_major` 惰性加载（兼容 json.loads 回 int 1）；reflect 成功后清除；`brain.py` L385 与 `inner_life/__init__.py` L82 两处调用点已 await |

## 二、红线核对

- ✅ `qi/core/emotion.py` **不在** git diff 变更清单；`DECAY_RATES`/`COUPLING`/`STAGE_BASELINES` 零改动
- ✅ `weave_narrative` 编织本体与 `decay_narrative_strengths` factor 未动（只改调度）
- ✅ `tools/repair_dirty_facts.py` 仅 `__main__` 入口，未接入 CI/心跳

## 三、独立复测

| 命令 | 结果 | 与回执一致 |
|------|------|-----------|
| `python -m pytest -q` | **430 passed** in 48.95s | ✅ |
| `python -m ruff check qi tests tools` | All checks passed! | ✅ |
| `python tools/verify_package.py --full` | 全部通过 | ✅ |

## 四、活库偏离裁定

回执承认的两处偏离，经实测裁定：

1. **「命中 1 条而非 5 条」→ 可接受**。实测 id 2/3/7/18 均已是 `retired(by=自身)` 状态（包 16 时期已清），方案中「5 条」是起草时基于旧快照的预期，Cursor 动态判定逻辑无错。
2. **「active identity 未见阿振」→ 发现升级为缺陷**（见下）。

## 五、验收中新发现：真名链断裂（须补修）

实测 `user_facts` 发现 Cursor 未深挖的问题：

```
[  1] identity 他叫阿振        retired(by=18)   ← 真名被脏事实顶掉
[ 18] identity 他希望被叫做过去拿  retired(by=18)   ← 脏事实自身后已清退
[ 15] identity 他觉得自己聪明     retired(by=24)   ← 被脏事实 #24 顶掉（附带损伤）
```

**链条**：7/30 时期脏事实 id=18 supersede 了真名 id=1 → 包 16 清退了 id=18 → **真名未恢复** → 当前 active identity 中无任何姓名事实。后果：栖冷启动后不记得用户真名「阿振」。

**裁定与闭环**：数据层修复，不阻塞代码验收结论。方案 Agent 已补一次性恢复脚本 `tools/restore_real_name_fact.py`（仿 repair 系列双模式，内容匹配定位不写死 id）并执行：

```text
预演：id=1「他叫阿振」retired(by=18) → 待恢复；id=15「他觉得自己聪明」retired(by=24) → 待恢复
--apply：已恢复 2 条。复查 active identity：id=1 他叫阿振 / id=15 他觉得自己聪明 / id=20 他是男生
```

真名已回到 active，缺陷闭环。

## 六、总评

| 项 | 结果 |
|----|------|
| 对照方案四子项 | ✅ 全部符合，无未声明偏离 |
| 活库偏离是否可接受 | ✅ 偏离 1 可接受；偏离 2 升级为独立数据缺陷，由方案 Agent 补修脚本闭环 |
| 红线与回归 | ✅ 零越界，430 passed / ruff 绿 / verify_package --full 通过 |
| **总评** | **验收通过**。代码侧闭环；真名恢复脚本已执行并复查通过，本包可归档 |

遗留交维护者：H1（security 路线拍板）、H2（整夜挂机复测）——均按任务包原计划，不受本验收影响。

---

*包 19 验收记录 · Qoder（方案 Agent）· 2026-08-04*
