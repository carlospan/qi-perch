# L7 · assist-5 对话集成修复——整改复审

> **角色**：Cursor（执行侧整改复审，本轮**仍禁码**）  
> **依据**：修订后 [PR 方案](./2026-08-09-L7-assist5-对话集成修复-PR方案.md)（文首 `审查后修订` + `复审-1 修订`）+ [方案审查回复](./2026-08-09-L7-assist5-对话集成修复-方案审查回复.md)（含复审-1 修订段）+ [交叉审查](./2026-08-09-L7-assist5-对话集成修复-Cursor交叉审查.md)  
> **时刻**：2026-08-09  

---

## 复审-2 总判（当前）

**B1 / B2 / B3 均已关闭。整改复审通过。**

复审-1 修订后的 3c 控制流正确：`last_assist_target is not None and _is_confirm_reexec_cue(text)` 才进分支；分支内新鲜 → 补执行；不新鲜/落空 → `_deliver_qi_message` 引导；**非 reexec 绝不短路**。测补 `test_non_reexec_with_target_not_shortcircuited`。仍禁码，待 `-编码请求.md` + 明示可编码。

---

## 复审-2：阻塞项逐条核对

| 项 | 交叉审查要求 | 复审-1 后 PR 落点 | 状态 |
|----|--------------|------------------|------|
| **B1** | 粘性路径；#1520 夹杂后 #1522 仍真读 | 3a 粘性 target；确认成功后保留；3c 用粘性补执行；夹杂追问测 | **关闭** |
| **B2** | 勿用全量 `_is_confirm_cue`；勿劫持闲聊 | 3b 窄词 + 绑 target；3c 非 reexec 落入正常对话；`test_bare_confirm_cue_no_hijack` / `test_non_reexec_with_target_not_shortcircuited` | **关闭** |
| **B3** | 引导句须 deliver | 引导仅在 reexec 分支内、补执行未成功时 `_deliver_qi_message` 再 return | **关闭** |

### 3c 复审-1 骨架（核对通过）

```python
if self.last_assist_target is not None and self._is_confirm_reexec_cue(text):
    now = datetime.now()
    if self._assist_target_fresh(now):
        ...
        if result is not None:
            await self._deliver_action_result(...)
            return ...
    await self._deliver_qi_message("嗯？你想让我看什么？", ..., proactive=False)
    return "嗯？你想让我看什么？"
# 非 reexec：绝不短路 → assist-4 / 正常对话
```

| 用户句 | target | 预期 |
|--------|--------|------|
| 「好你读吧」且新鲜 | 有 | 补执行 ✓ |
| 「啊？你看到了什么」 | 有 | 不进 3c → 正常对话 ✓ |
| 「嗯嗯」 | 有 | 不进 3c → 正常对话 ✓ |
| 「好你读吧」且过期 | 有但不新鲜 | 引导句 + deliver ✓ |
| 「你好」 | 无 | 不进 3c → 正常对话 ✓ |

---

## 知悉（编码请求 / 编码时消化，不阻塞复审）

| # | 备注 |
|---|------|
| N1 `_fail` async | 已写入 PR 改动 1 |
| N2 Spec §1 | 任务包已改 |
| **「可以」仍在 REEXEC** | 子串下「可以啊」在有 target 时会进 3c；与审查回复「可以啊不进兜底」不完全一致。建议编码请求去掉裸「可以」，或整句匹配——**不升 B** |
| **测名过时** | `test_confirm_after_pending_consumed_no_target_guides`：`target is None` 时新 3c **不会**引导。应改为 **stale target**（有 path、`_assist_target_fresh` False）或 result None 时期望引导——编码请求改测名/断言 |
| R6 / 超时 | `_assist_target_fresh` 现仅 5 分钟；心跳清 pending 时一并清 target——编码请求写死 |
| N3/N4 | digest 跑偏观察；重复真读可接受 |

---

## HITL

扩权采纳 **a/a/a**——复审认可；维护者可事后追认。

---

## 编码前理解确认（仍禁码）

获编码请求 + 明示可编码后，将按修订后 PR：

1. `assist.py`：成功/`_fail(failed_capability)` `insert_action`；`_confirm_gate` 不写；`_fail`→async + 5 处 await  
2. `conversation.txt`：加 assist 语义硬规则一行  
3. `brain.py`：`last_assist_target`/`_at`；3c 按复审-1 控制流；`confirmed_override`；拒绝/超时清 target  
4. 测试：insert 三测 + dialog 夹杂补执行 / bare 不劫持 / non-reexec 不短路 / **stale 引导** / expire；prompt 硬规则断言  

---

## 下一拍

1. **Trae**：落盘 `-编码请求.md`（建议吸收：去掉裸「可以」或注明；改 `no_target_guides`→stale；超时只钉 5 分钟 + 心跳同清）  
2. 明示可编码 → Cursor 编码 → `-Cursor编码回执.md` 完工段  
3. Trae 实施验收  

---

## 附录：复审-1（历史，2026-08-09）

**当时总判**：B1 关；B2/B3 未关（引导句在 `if` 外无条件 return）。已由复审-1 PR 修订关闭，见上文复审-2。

**Cursor 结论（复审-2）：B1/B2/B3 关闭，复审通过；等待编码请求与明示可编码。仍禁码。**
