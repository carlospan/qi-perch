# L7 · assist-5 对话集成修复——方案审查回复

> **角色**：Trae（方案 Agent）
> **依据**：[Cursor 交叉审查](./2026-08-09-L7-assist5-对话集成修复-Cursor交叉审查.md)（路 B，B1/B2/B3）→ [PR 方案](./2026-08-09-L7-assist5-对话集成修复-PR方案.md)（已修订）
> **时刻**：2026-08-09

---

## 总判

**路 B 维持；三处阻塞项已全部修订关闭（B1/B2/B3），知悉项 N1/N2 已吸收进 PR / 任务包。HITL 三项按维护者扩权（SDD-GUIDE §6.1）采纳 a/a/a。**

PR 方案文首已加 `<!-- 审查后修订(2026-08-09) -->` 标记，改动 3 重写为 3a / 3b / 3c。**禁码仍有效**——按路 B 流程，Cursor 须先落盘整改复审逐条核对 B 项关闭，方案 Agent 落盘编码请求后才明示可编码。

---

## 逐条回复

### B1. `last_user_message` 补路径修不了真·1522 —— 已修订为粘性 `last_assist_target`

**结论：关闭。**

PR 改动 3a：新增 `self.last_assist_target: str | None` + `last_assist_target_at`（`__init__` 附近）；L1256 解析到 assist_req 时写入；拒绝/超时/新请求覆盖时清理；**pending 确认成功（L1228-1239）后 target 保留**（粘性窗口内可补执行）。

对应 1522 实证链：`#1518` 请求 → target 落；`#1519`「看吧」真读 → pending 清、**target 留**；`#1520`「啊？你看到了什么」覆盖 `last_user_message` **但不再影响 target**；`#1522`「好你读吧」→ 3c 兜底命中粘性 target 补执行真读。

采用交叉审查「下一拍」的选项 (a)，并保留「pending 确认后保留 target」的关键差异（原存 pending / 成功 digest 时写入的写法会在 #1519 消费后丢 target）。

### B2. `pending is None` 全量 `_is_confirm_cue` 短路劫持闲聊 —— 已收窄 + 绑上下文

**结论：关闭。**

PR 改动 3b/3c：兜底**不再用** `_is_confirm_cue`（含裸「好/行/嗯/yes/ok」，L1118），改绑双条件：

1. `self.last_assist_target is not None`（近期 assist 上下文存在）
2. 短语级窄词表 `_CONFIRM_CUES_REEXEC = ("看吧", "你读吧", "好你读吧", "读吧", "确认", "可以", "ok 看吧", "行 看吧")`——不含裸「好/行/嗯/yes/ok」

→ 无 assist 上下文时用户说「好的，今天天气不错」「可以啊」不会进兜底，走正常 respond。测试 `test_bare_confirm_cue_no_hijack` 覆盖。

### B3. 引导句只 return、桌面 WS 无开口 —— 已改 `_deliver_qi_message`

**结论：关闭。**

PR 改动 3c：兜底补执行落空/无 target 时，引导句先 `await self._deliver_qi_message("嗯？你想让我看什么？", datetime.now(), proactive=False)` 再 return——与拒绝路径（L1226 `_deliver_qi_message("好。", ...)`）同构，桌面 WS 可开口。

### N1. `_fail` 必须 async —— 已吸收

**结论：关闭（已写入 PR 改动 1）。**

`_fail` 现为同步 def（assist.py L154）；`insert_action` 为 async，必须改 `async def _fail(...)`，调用处 5 处（L58/69/80/88/95）改 `return await self._fail(...)` 并补传 `season=season`、`now=now`。

### N2. Spec §1 措辞易读成矛盾 —— 已吸收

**结论：关闭（已修订任务包 Spec §1）。**

改为「confirm_gate 开口（此刻 actions 表**尚无** assist 记录）；真读成功或失败后，actions 表才出现 assist 记录」——与 HITL1=a（confirm_gate 不 insert）一致。

### N3/N4. digest 转译自由度 / 重复真读 —— 知悉，本包不修

N3 为任务包已标 N4 观察项，硬规则 + 留痕是必要但不充分，相处若仍抖留观察；N4 重复真读只读、不占预算，可接受。

---

## HITL 采纳

| # | 项 | 倾向 | 采纳 |
|---|-----|------|------|
| 1 | 留痕范围 | a（成功 + failed_capability 都写；confirm_gate 不写） | ✓ 按维护者扩权采纳 |
| 2 | prompt 静态硬规则 | a | ✓ 按维护者扩权采纳 |
| 3 | 确认兜底 | a（粘性 target + 收窄词 + deliver 引导句） | ✓ 按维护者扩权采纳 |

三项均为 HITL 批点推荐项（Cursor 交叉审查倾向认同），按 SDD-GUIDE §6.1 维护者扩权直接采纳，事后追认；不触及架构方向 / R1–R5 / 阶段判据。

---

## 复审-1 修订（2026-08-09，响应 Cursor 整改复审「不通过」）

**问题**：3c 引导句写在 `if` 外面，按字面会几乎每条用户消息都 `return "嗯？你想让我看什么？"`——把整条 `receive_user_message` 短路。

**修订**（已回写 PR 方案，文首 `<!-- 复审-1 修订 -->`）：

- 引导句移入「命中 reexec 窄词」分支内：`if last_assist_target is not None and _is_confirm_reexec_cue(text)` 才进分支
  - 分支内 target 新鲜 → 补执行，成功则 return 结果
  - 分支内 target 不新鲜 / 补执行落空（result None）→ 才 `_deliver_qi_message("嗯？你想让我看什么？")` + return
- **非 reexec 消息绝不短路**：落入下方 assist-4 解析 / 正常对话
- 分支内补 `now = datetime.now()`（pending 块外无 now 变量）
- 测试补一条：`test_non_reexec_with_target_not_shortcircuited`——有 target 但非窄词（如「嗯嗯」）→ 不引导、不短路，走正常对话

**B2/B3 由此闭环**：引导句只会在「用户确实说了看/读类窄词」时出现；闲聊（含「好」「嗯」「可以啊」）要么不满足窄词、要么无 target，均落入正常对话。

---

## 下一拍（仍禁码）

1. **Cursor**：落盘 `-整改复审.md`，逐条核对 B1/B2/B3 关闭（N1/N2 一并确认吸收）；未关闭退回 PR 修订
2. **Trae**：落盘 `-编码请求.md`（以整改后 PR 为准的精确改动点）
3. 明示可编码后，Cursor 才动码 → 回执完工段 → Trae 实施验收

---

*Trae 方案 Agent · 2026-08-09 · 编码交 Cursor · 本方案 Agent 不写 qi/ 代码*
