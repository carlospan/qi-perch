# L7 · assist-5 对话集成修复——编码请求

> **角色**：Trae（方案 Agent）→ 编码交 Cursor
> **依据**：[PR 方案](./2026-08-09-L7-assist5-对话集成修复-PR方案.md)（含审查后修订 / 复审-1 / 复审-2 标记）、[方案审查回复](./2026-08-09-L7-assist5-对话集成修复-方案审查回复.md)（含复审-1 修订说明）
> **前置**：Cursor 交叉审查（B1/B2/B3）→ Trae 修订 → Cursor 复审-1（不通过）→ Trae 修订 → **Cursor 复审-2（通过：B1/B2/B3 均关闭）**
> **时刻**：2026-08-09
> **状态**：**已明示可编码**——按本文件落地后写完工回执，交 Trae 实施验收

---

## 精确改动点（以整改后 PR 为准；与 PR 冲突处以本文件为准）

### 改动 1：`qi/action/assist.py` —— insert_action 留痕（HITL1=a）

1. `_fail` 改为 **`async def _fail(self, qi_line, summary, outcome, *, season="spring", now=None)`**（N1：`insert_action` 是 async）
2. `_fail` 内（return 前），**仅当 `outcome == OUTCOME_FAILED_CAPABILITY`** 时 `await self.db.insert_action("assist", summary, target="user", outcome=OUTCOME_FAILED_CAPABILITY, season=season, now=now)`
3. 调用处 5 处（L58 / L69 / L80 / L88 / L95）改为 `return await self._fail(...)`，**逐一补传 `season=season`、`now=now`**
4. 成功路径（L100-110 return 前）：`await self.db.insert_action("assist", summary, target="user", outcome=OUTCOME_SUCCESS, emotion_context=None, season=season, detail_json=json.dumps({"op": op, "target_path": target_path, "digest": summary}, ensure_ascii=False), now=now)`，返回值赋给 result 的 `action_id`
5. `_confirm_gate` **不** insert_action
6. `execute` 内 `_ = now or datetime.now()` 改 `now = now or datetime.now()`（留痕需真实 now）

### 改动 2：`qi/prompts/conversation.txt` —— assist 语义硬规则（HITL2=a）

- L62-71 硬规则段，「- 不主动提供帮助建议，除非对方问了」之后加一行（一字不改）：
```
- 对方请求看/读文件时你有读文件的协助能力：真读过就按记忆说；没读过或找不到就老实说「没找到」「我打不开」——不要编造「读不到你的文件系统」「隔着玻璃」
```

### 改动 3：`qi/core/brain.py` —— 粘性 target + 收窄确认词 + deliver 兜底（HITL3=a）

**3a. 粘性 `last_assist_target`（修 B1）**：
1. `__init__` 附近加：
```python
self.last_assist_target: str | None = None   # 粘性：最近一次 assist 请求的目标路径（assist-5）
self.last_assist_target_at: datetime | None = None
```
2. L1256 解析到 assist_req 时（`self.last_assist_request = assist_req` 处）一并设：
```python
if assist_req is not None:
    self.last_assist_target = getattr(assist_req, "target_path", None)
    self.last_assist_target_at = datetime.now()
```
3. 拒绝分支（L1223-1227）与 pending 超时清理处：一并清 `last_assist_target` / `last_assist_target_at`
4. **pending 确认成功（L1228-1239）后 target 保留**（粘性窗口内可补执行）——不要清
5. 超时清理复用 pending 的 5 分钟 / 3 轮心跳「先到先清」逻辑；**确认心跳分支（`pending_assist_heartbeats`）清 pending 时一并清 target**

**3b. 收窄确认词 + 绑上下文（修 B2）**：
- L1118 附近新增常量（**不含裸「可以」**，复审-2）：
```python
# assist-5：pending 已消费后补执行的确认词（短语级，不含裸「好/行/嗯/可以/yes/ok」）
_CONFIRM_CUES_REEXEC = ("看吧", "你读吧", "好你读吧", "读吧", "确认", "ok 看吧", "行 看吧")
```

**3c. 兜底分支（修 B3）**——放在 L1244 `_clear_pending_assist` 之后、assist-4 解析分支之前，**控制流严格如下**（复审-1/复审-2 确认）：

```python
# assist-5：pending 已消费但用户仍短语确认（1522 场景）——粘性目标补执行
if self.last_assist_target is not None and self._is_confirm_reexec_cue(text):
    now = datetime.now()
    if self._assist_target_fresh(now):
        target = self.last_assist_target
        self.last_assist_target = None  # 补执行后清（防重复）
        result = await self._execute_assist_on_request(
            AssistRequest(op="read_file", target_path=target),
            confirmed_override=True,
        )
        if result is not None:
            await self._deliver_action_result(result, datetime.now())
            return (result.get("qi_line") or "").strip() or None
    # 命中 reexec 窄词但 target 不新鲜 / 补执行落空 → 引导句 deliver（修 B3）
    await self._deliver_qi_message("嗯？你想让我看什么？", datetime.now(), proactive=False)
    return "嗯？你想让我看什么？"
# 非 reexec：绝不短路，落入下方 assist-4 解析 / 正常对话
```

- 辅助方法（加在 `_is_confirm_cue` 附近）：
```python
def _is_confirm_reexec_cue(self, text: str) -> bool:
    t = text.strip().lower()
    return any(cue in t for cue in self._CONFIRM_CUES_REEXEC)

def _assist_target_fresh(self, now: datetime) -> bool:
    if self.last_assist_target_at is None:
        return False
    return now - self.last_assist_target_at <= timedelta(minutes=5)
```
- `_execute_assist_on_request`（L1141）签名加 `confirmed_override: bool = False`，内部 `confirmed=False`（L1174）改为 `confirmed=confirmed_override`；**assist-4 既有调用（L1261）不传，默认 False，行为不变**
- 3c 用到的 `AssistRequest`：`brain.py` 未 import 则补（从 `qi.action.volition` 导入，与 L1250 一致）

---

## 测试计划（含复审-2 调整）

**新文件 `tests/test_assist_action_rewrite.py`**（复用 assist-1 测试模式）：
1. `test_assist_success_inserts_action`：execute 成功 → actions 表 kind='assist' + outcome=SUCCESS + detail_json 含 target_path
2. `test_assist_fail_inserts_action`：文件不存在 → _fail → actions 表 outcome=failed_capability
3. `test_assist_confirm_gate_no_action`：confirm_required 不 insert_action

**`tests/test_assist_dialog_trigger.py` 补**：
4. `test_confirm_after_pending_consumed_re_executes`（B1）：pending 已清 + 中间插一句普通消息覆盖 last_user_message + 用户「好你读吧」→ 粘性 target 补执行（confirmed=True）+ qi_line 开口
5. `test_confirm_after_pending_consumed_stale_target_guides`（B3）：last_assist_target 存在但超 5 分钟（stale）→ 进 3c 但 `_assist_target_fresh` False → 回引导句 + `_deliver_qi_message` 被调；并断言 target=None 时**不进** 3c、不引导（落入正常对话）
6. `test_bare_confirm_cue_no_hijack`（B2）：无 assist 上下文时用户说「好」「嗯」→ 不触发补执行，走正常 respond
7. `test_confirm_reexec_cue_phrase_only`（B2）：pending 已消费后裸「好」不补执行；「好你读吧」补执行
8. `test_non_reexec_with_target_not_shortcircuited`（B2/B3）：last_assist_target 存在但用户说「嗯嗯」等非窄词 → 不进 3c、`_deliver_qi_message("嗯？…")` 不被调，落入正常对话
9. `test_assist_target_expires`：last_assist_target 超 5 分钟 → `_assist_target_fresh` False，不补执行

**conversation.txt 断言**：含「读文件」硬规则（在既有 prompt 测试中补一条，或随 2 新建轻量断言）。

---

## 落地约定（编码时注意）

- 3c 分支位置：**L1244 `_clear_pending_assist()` 之后、L1247 `self.last_user_message = text` 之前**（在 pending 处理块外、assist-4 解析前）
- `_fail` 5 处调用逐一核对：L58（op 不支持）/ L69（关系不够）/ L80（非可读文件）/ L88（文件不存在）/ L95（读取失败）——均补 `season=season, now=now`
- digest 落空降级（`_digest_file` 异常时 return 默认句）路径不受影响；insert_action 用 `summary`（digest 结果），失败也留痕
- **不改** assist-1/2/3/4 既有结构、**不改**前端（AssistConfirmCard / ExploreCard 不动）
- R3：digest 走 `llm.call(purpose="consciousness", ...)`，不走别路

## 自测与回执要求

- `pytest` 全量 **≥567 passed**（assist-5 新增约 9-10 条）
- 回执 `-Cursor编码回执.md` 完工段：列出实际改动文件 + 新增测试 + 全量测试数 + 相对本文件的执行判断（若有偏离，说明原因）
- 收口后由 Trae 做实施验收（纪律红线 R1/R3/Step 5/contract 25 + 验收清单）

## 纪律红线对照（编码时自检）

- R1（不编造）：conversation.txt 硬规则 + 留痕注入 ✓
- R3（LLM 走 gateway）：digest 走 consciousness ✓
- Step 5（行动必留痕）：assist 成功/失败 insert_action；confirm_gate 不写 ✓
- contract 第 25 条（不主动提供帮助建议）：assist 仅用户请求触发；新增硬规则紧贴「对方问了才伸手」 ✓

---

*Trae 方案 Agent · 2026-08-09 · 明示可编码 · 本方案 Agent 不写 qi/ 代码*
