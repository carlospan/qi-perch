# L7 assist-4（对话拍触发）——PR 方案

> **角色**：Trae（方案 Agent）；编码交 Cursor
> **依据**：[任务包](./2026-08-09-L7-assist4-对话拍触发-任务包.md)
> **对照代码**：`qi/core/brain.py`（`receive_user_message` L1174-1229 / `_execute_confirmed_assist` L1141-1172）、`qi/action/layer.py`（`execute_kind` L301-434）、`qi/action/assist.py`（`_confirm_gate`）
> **时刻**：2026-08-09

---

## 外部可观测行为

1. 用户「帮我看一下 D:/x.txt」→ 栖**不走对话 LLM**，直接 `execute_kind("assist", confirmed=False)` → confirm_gate 开口「要我看的话，说一声我就看」（friend+）；不进 pending_queue
2. stranger → `_confirm_gate` 拦 → failed_capability 开口「熟一点」（assist.py 现有逻辑，本包不改）
3. 普通消息（无 assist 请求）→ 走原 respond 路径（pending_queue append + _heartbeat + respond LLM）

## 精确改动点

### 1. `qi/core/brain.py` `receive_user_message`（L1174-1225）

**现状**（L1210-1225）：
```python
if len(self._pending_queue) >= PENDING_QUEUE_MAX:
    dropped = self._pending_queue.popleft()
    logger.warning(...)
self._pending_queue.append(text)
self.last_user_message = text
try:
    from qi.action.volition import parse_assist_request
    self.last_assist_request = parse_assist_request(text)
except Exception:
    logger.debug("assist_request 解析失败", exc_info=True)
    self.last_assist_request = None
await self._heartbeat()
speech = self._take_pending_speech()
```

**改为**：解析提前 + assist_req 非 None 时直接 execute_kind（同构 `_execute_confirmed_assisted` L1141-1172，confirmed=False）：

```python
# assist-4：对话拍有 assist 请求时，assist 开口 = respond（不走 conversation LLM）
self.last_user_message = text
assist_req = None
try:
    from qi.action.volition import parse_assist_request
    assist_req = parse_assist_request(text)
except Exception:
    logger.debug("assist_request 解析失败", exc_info=True)
    assist_req = None
self.last_assist_request = assist_req

if assist_req is not None:
    # assist 开口 = respond：直接 execute_kind(assist, confirmed=False)
    # 不进 pending_queue、不跑 respond LLM、不跑 _heartbeat
    try:
        result = await self._execute_assist_on_request(assist_req)
        if result is not None:
            # B1：与 GWS 路径 L997-1011 同构——confirm_gate 返回后存 pending，
            # 让 assist-3「看吧」/「不用」能接上（用局部 assist_req，不依赖已消费的 last_assist_request）
            if result.get("needs_confirmation") or (
                result.get("outcome") == "confirm_required"
            ):
                self.pending_assist_confirmation = assist_req
                self.pending_assist_confirmation_at = datetime.now()
                self.pending_assist_heartbeats = 0
            self.last_assist_request = None
            await self._deliver_action_result(result, datetime.now())
            return (result.get("qi_line") or "").strip() or None
    except Exception:
        logger.exception("assist 对话拍 execute 失败")
    return None

# 正常对话路径（无 assist 请求）
if len(self._pending_queue) >= PENDING_QUEUE_MAX:
    dropped = self._pending_queue.popleft()
    logger.warning(...)
self._pending_queue.append(text)
await self._heartbeat()
speech = self._take_pending_speech()
```

### 2. `qi/core/brain.py` 新增 `_execute_assist_on_request`（同构 `_execute_confirmed_assist` L1141-1172，confirmed=False）

```python
async def _execute_assist_on_request(self, assist_req) -> dict | None:
    """assist-4：对话拍直接 execute_kind(assist, confirmed=False)。
    与 _execute_confirmed_assist 同构，差别在 confirmed=False。"""
    if self.action is None:
        return None
    now = datetime.now()
    scars = None
    if self._db is not None:
        try:
            scars = await self._db.list_scars()
        except Exception:
            scars = None
    trust = 0.5
    if self.relationship is not None:
        trust = float(
            getattr(self.relationship.state, "trust", 0.5) or 0.5
        )
    return await self.action.execute_kind(
        "assist",
        self.emotion,
        self.relationship_stage,
        self._current_season(),
        now,
        mode=self.emotion.mode.value,
        user_online=self.user_online,
        scars=scars,
        sensing=self.last_sensing,
        pressure=self.last_pressure_response,
        trust=trust,
        op=getattr(assist_req, "op", None),
        target_path=getattr(assist_req, "target_path", None),
        confirmed=False,
    )
```

## 纪律红线对照

- R1（不编造）：assist.py `_confirm_gate` 不执行时 found=None，开口诚实 ✓
- R3（LLM 走 gateway）：assist digest 用 consciousness（assist-1 已接）✓
- 不引入 Agent framework：execute_kind 直接调，不走 LLM tool calling ✓
- contract 第 25 条（不主动提供帮助建议）：assist 只在用户开口请求时触发 ✓

## 测试计划与验收清单

### 新增测试 `tests/test_assist_dialog_trigger.py`

1. `test_receive_user_message_with_assist_request_triggers_execute_kind`
   - 用户「帮我看 D:/x.txt」→ execute_kind("assist", confirmed=False) 被调 + qi_line 开口 + 不进 pending_queue
2. `test_receive_user_message_normal_message_unchanged`
   - 普通消息「你好」→ 走原 respond 路径（pending_queue append + _heartbeat）
3. `test_assist_request_stranger_fails`
   - stranger + assist 请求 → `assist.py` L67-72 `_fail`（**非 `_confirm_gate`**）→ outcome=failed_capability + 开口「这个我得先跟你熟一点再说。」
4. `test_assist_request_friend_confirm_gate_stores_pending`
   - friend+ + assist 请求 → execute_kind 返回 needs_confirmation → **brain.pending_assist_confirmation 存（B1，局部 assist_req）** + qi_line 开口 + 不进 pending_queue

### 验收清单

- [ ] execute_kind("assist", confirmed=False) 在对话拍被调
- [ ] assist 请求时不进 pending_queue、不跑 respond LLM
- [ ] **confirm_gate 返回后 pending_assist_confirmation 存（B1）——assist-3「看吧」链通**
- [ ] 普通消息不受影响
- [ ] stranger 时 `_fail`（「熟一点」，非 confirm_gate）
- [ ] 全量 ≥556 passed

## 风险 / 不确定点

- **R1**：不跑 `_heartbeat` → 情绪/内在生命这一拍不更新（assist 响应式可接受，下一拍心跳补）
- **R2**：对话拍短路不进 pending_queue → **用户句「帮我看 X」不落对话记忆**（用户句正常路径经 pending_queue → _heartbeat 落库）。权衡：避免 respond 双重开口；assist 请求句不进记忆的长期影响待相处观察，必要时另开小包补落库
- **R3**：对话拍不跑 GWS → 其他主动候选（share/tend/explore）这一拍也不生成（可接受，对话拍本就以 respond 为主）
- **R4**：`execute_kind` 签名需 Cursor 核对（参考 `_execute_confirmed_assist` L1141-1172 同构调用）

## 需维护者拍板项

- 无（HITL a/a/a 已追认）

---

*Trae 方案 Agent · 2026-08-09 · 编码交 Cursor · 本方案 Agent 不写 qi/ 代码*
