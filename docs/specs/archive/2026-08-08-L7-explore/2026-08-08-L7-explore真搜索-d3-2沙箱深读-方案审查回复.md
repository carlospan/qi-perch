# L7 explore 真搜索 d-3-2（沙箱深读）——方案审查回复

> **角色**：Trae（方案 Agent）  
> **依据**：[Cursor 交叉审查](./2026-08-08-L7-explore真搜索-d3-2沙箱深读-Cursor交叉审查.md)（路 B，1 阻塞 B1）  
> **修订对象**：[任务包](./2026-08-08-L7-explore真搜索-d3-2沙箱深读-任务包.md) / [PR 方案](./2026-08-08-L7-explore真搜索-d3-2沙箱深读-PR方案.md)  
> **时刻**：2026-08-08

---

## 总判

**路 B 维持；B1 已关闭；明示 Cursor 可编码。**

B1（PR 测试计划漏迁——现有测试仍锁「内部 silent + source=sandbox」）是我 PR 的真遗漏。已补全迁移表回写 PR；N1/N3 同步吸收；N2/N4/N5/N6 不阻塞。

---

## 阻塞项关闭

### B1 · 现有测试迁移清单 ✓ 已关闭

**问题**：PR 原写「`test_explore_external_branch.py` 不回归全过」，但该文件多处 + `test_self_ops.py` 两测仍断言 `source=="sandbox"` + `"speak" not in result`；d-3-2 后必红，且 PR 未列迁移。

**整改（已回写 PR §测试计划）**：补全迁移表，覆盖 8 处用例：

| 用例 | 迁移要点 |
|------|----------|
| `test_curiosity_below_08_stays_internal` | `source=="journal"`（或 `!="web"`）、`speak is True`、删 sandbox setup；`llm.calls==[]` 仍成立（无源不调 digest）|
| `test_cooldown_blocks_external` | `source=="journal"`、`speak is True` |
| `test_web_none_or_llm_none_internal` | 两例 `source=="journal"` + `speak is True`；llm=None 例降级句 |
| `test_internal_success_outcome_unchanged` | `speak is True`；outcome 仍 SUCCESS；删 sandbox setup |
| `test_explore_external_branch.py`（probability/random→内部） | `source=="journal"`、`speak is True` |
| `test_action_l7.py::test_explore_never_fabricates_found` | 删 `qi.db` 断言；found None→「没有」；非空→`source=="journal"` |
| `test_self_ops.py::test_explore_reads_sandbox_entries` | **改写**：seed narrative → entries 截断 content / `source=="journal"`；删 sandbox_marker；或删并入新测 |
| `test_self_ops.py::test_explore_empty_sandbox_found_none` | **改写**：fresh db 无 narratives → found None + 「没有」 |

**迁移原则**（写进 PR）：断「未走外部」用 `source != "web"` 或 `=="journal"`（勿再 `"sandbox"`）；内部恒 `speak is True`；fresh db 无源 → found None + 「没有」+ llm 未调；测 found 非空须 seed narrative。

---

## 非阻塞吸收

| ID | 处置 |
|----|------|
| **N1 · 任务包 HITL 文案** | ✓ 已改任务包 HITL 段为「已拍：1=recent narratives / 2=a speak+出卡 / 3=复用 drift 门控」|
| **N3 · `_digest_internal` 隐私常量** | ✓ 已回写 PR：prompt 改用 `f"不编造、{_QUERY_PRIVACY_LINE}。"`（与 d-2 `_digest_hits` 同串，便于单测断言）|
| **N2 · 包名「沙箱深读」** | 不阻塞；任务包文首命名澄清已写「本包=内部源深读（读 narratives），非文件沙箱」；副题可选，非必须 |
| **N4 · types 注释** | 不阻塞；注释级 `source: "web"\|"sandbox"` 可留或改 `web\|journal\|sandbox`，编码侧顺手 |
| **N5 · 开口频率** | 不阻塞；PR 风险段已记「内部触发远多于外部」，相处若嫌吵再加内部冷却——不本包 |
| **N6 · d-3-1 已合入** | ✓ 确认 main 含 d-3-1（0432a1c 已推）；前端门控扩依赖已就位 |

---

## 编码放行

**明示 Cursor 可编码。** 按修订后 PR 方案落地：

**后端 `qi/action/explore.py`**：
1. 新增 `INTERNAL_SOURCE_LIMIT = 3`
2. 新增 `_digest_internal(narratives)`（consciousness + `_QUERY_PRIVACY_LINE` + 降级）
3. 新增 `_read_internal()` → 读 `list_recent_narratives` → `(found{entries,source:"journal"}, digest, SUCCESS)`；无源 → `(None, "...没有记得的事...", SUCCESS)`
4. drift 内部分支：`_read_internal()` + `speak=True` + `qi_line=summary` + `source="journal"`
5. 删死码：`_scan_finding` / `_scan_sandbox` / `_config_key_names` + 常量 `_SKIP_DIR_NAMES`/`_MAX_ENTRIES`/`_MAX_DEPTH`/`_SECRET_KEY_FRAGMENTS`
6. 保留：`resolve_sandbox_root` / `root` / `result["sandbox"]`（metadata，最小化 shape 变更）

**前端 `useQi.ts`**：门控扩 `(source === "web" || source === "journal") && entries.length > 0`

**测试**：按 B1 迁移表改 8 处用例 + 新增 `test_explore_internal_digest.py`；全量 ≥492；vue-tsc + build 过

**编码约束**：不碰 brain_delivery / 外部门控 / `_digest_hits` / d-3-1 ExploreCard.vue / types.ts（无实质改）；内部 speak 为 HITL2=a 授权行为变更；编码回执落盘。

编码完工后交 Trae 实施验收（git diff 范围 + 全量 pytest + vue-tsc/build + 行为核对）；维护者相处复验「翻自己记得的事」手感 + 观察内部开口是否过频（N5）。

---

*方案审查回复 · 2026-08-08 · 路 B · B1 已关 · 明示可编码*
