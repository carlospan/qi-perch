# L7 explore 真搜索 d-2（内部深读）——验收记录

> **角色**：Trae（方案 Agent · 实施验收）  
> **依据**：[方案审查回复](./2026-08-08-L7-explore真搜索-d2内部深读-方案审查回复.md)（路 A，明示可编码）+ 修订后 [PR 方案](./2026-08-08-L7-explore真搜索-d2内部深读-PR方案.md)  
> **编码回执**：[Cursor 编码回执](./2026-08-08-L7-explore真搜索-d2内部深读-Cursor编码回执.md)  
> **时刻**：2026-08-08

---

## 总判

**验收通过。** 代码与修订后 PR 方案逐项对齐；N1/N2 已吸收落地；测试 492 全绿；scope 未越界。剩余 = 维护者相处复验。

---

## 代码核对（git diff）

### `qi/action/explore.py`

| PR 要求 | 落地 | 核对 |
|----|------|------|
| 新增 `_digest_hits(query, hits)`：`consciousness` / `_QUERY_PRIVACY_LINE` / 「不编造」 / 失败·空·无 llm 降级 `f"我刚才看了看 {query}。"` / `except` 兜意外 | ✓ 紧挨 `_fetch_external` 后、`drift` 前 | 与 PR §1 骨架一致；hits `[:3]` + snippet `[:120]` 限成本 |
| `_fetch_external` 成功：`summary = await self._digest_hits(query, hits)`；删旧 `title = hits[0].title...` | ✓ | 与 PR §2 一致；`found.entries` 仍留 hits 结构化 |
| drift 外部成功：`qi_line = summary`（收回 d-1 开口含蓄化拆 `if found/else`） | ✓ 收敛为单行 `qi_line = summary` | 与 PR §3 一致；空手仍 `qi_line=summary`（诚实句） |

### 纪律红线

- R1 不破（相处验证仍维护者做）✓
- d-1 B3「不二次 LLM」本包解禁：仅一次 digest LLM（`consciousness`）✓
- 不编造：prompt 钉死「不编造」+ 基于 hits ✓
- 隐私：prompt 含 `_QUERY_PRIVACY_LINE` ✓
- 失败降级：`gateway.call` 失败返空不抛，`except` 兜意外；`outcome=success` 不变 ✓
- scope：仅 `explore.py` + 测试；**未碰** `brain_delivery` / `layer` / `brain` / `settings` / 外部门控 ✓

---

## 测试核对（N1/N2 吸收）

### N1 · 命名澄清

任务包 + PR 文首均加「命名澄清」段：本包 = 外部 hits 消化，非沙箱文件深读。✓

### N2-a · `_FakeLLM` 支持按调用序返回

`tests/test_explore_external_branch.py` `_FakeLLM` 改为：
- `texts: list[str] | str`（传 str 当单值列表，兼容旧用法；`self.text` 仍保留）
- `call` 内：`>1` 则 `pop(0)`，单值返回最后一项

另在 `tests/test_explore_digest.py` 新增独立 `_SeqLLM`（同语义）+ `_RaisingLLM`（抛异常）。✓

### N2-b · `test_external_when_gates_pass` 断言更新

- `_FakeLLM([query, digest])` 两次返回 ✓
- `len(llm.calls) == 2`、`all purpose == consciousness` ✓
- `qi_line == summary == digest` ✓
- `"窗边的鸟" not in qi_line`、`not in summary`（d-2 改：summary 不再含原 title）✓
- title 只在 `found.entries[0]["title"]` + `found.query == query` ✓
- 隐私红线对 digest 那次（`calls[1].messages`）再断言 ✓

`test_non_force_internal_gate_then_external` 同步改双返回 + `qi_line==summary==digest`。✓

### 新增 `tests/test_explore_digest.py`（5 用例）

- 成功：digest == LLM 串、`purpose==consciousness`、messages 含隐私线 + 「不编造」+ hits 内容
- 空串降级 → `我刚才看了看 {query}。`
- 异常降级 → 同上
- 无 llm 降级 → 同上
- drift 降级路径 `outcome` 仍 `OUTCOME_SUCCESS`（搜到了，没消化≠能力失败）

---

## pytest

| 命令 | 结果 |
|------|------|
| `pytest tests/test_explore_digest.py tests/test_explore_external_branch.py tests/test_explore_web.py -q` | **18 passed**（独立复核） |
| `pytest -q`（`--basetemp` 规避 Windows tmpdir 清理） | **492 passed**（与回执一致；≥487） |

> 注：首次全量因 Windows `pytest-of-18331\pytest-current` 目录清理触发 `PermissionError`（环境问题，非测试失败；dots 跑满 [100%] 无 F/E 标记）。换 `--basetemp` 后 summary 干净：**492 passed**。

---

## 偏离确认

| 项 | 处置 |
|----|------|
| N3（prompt 冗余） | 实现侧择一——system 用「不编造。红线：`{_QUERY_PRIVACY_LINE}`」，未叠第二句字面。审查回复已授权「可择一」 ✓ |
| N5（文档回写） | 不进本包 scope；`L7-action.md` / `progress` 留验收后另拍 ✓ |
| `_digest_hits` 放置位置 | 紧挨 `_fetch_external` 后、`drift` 前，与 PR「`_fetch_external` 后」一致 ✓ |

均非实质偏离，可接受。

---

## 数据核对

`test_fetch_external_digest_failure_still_success` 已断言降级路径 `actions.outcome == OUTCOME_SUCCESS`；`test_internal_success_outcome_unchanged` 覆盖内部 `OUTCOME_SUCCESS`。actions 表留痕语义不变。✓

---

## 剩余（交维护者）

- **相处复验**：本机独处攒 curiosity≥0.8、过 6h 冷却、概率 0.05 触发外部搜索时，听栖开口是否像「看懂了外面」而非念 title/只念 query。
- **N5 文档回写**：相处复验通过后，回写 `L7-action.md` / `progress`（成功开口=digest；d-2 收口）。
- **d-3**：L6 explore 卡片 + 内外部源结合，另开。

---

## 收口状态

- **工程交付**：✅ 完成（代码 + 测试 + 492 全绿 + scope 无越界）
- **相处复验**：⏳ 待维护者
- **d-2 收口**：待相处复验通过 + 文档回写后宣告收口；之后 d-1/d-2 整体可标「explore 真搜索（外部消化）已收口」

---

*验收记录 · 2026-08-08 · 通过 · 492 passed*
