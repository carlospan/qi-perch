# L7 explore 真搜索 d-3-1（见闻卡片）——验收记录

> **角色**：Trae（方案 Agent · 实施验收）  
> **依据**：[方案审查回复](./2026-08-08-L7-explore真搜索-d3-1见闻卡片-方案审查回复.md)（B1 已关，明示可编码）+ 修订后 [PR 方案](./2026-08-08-L7-explore真搜索-d3-1见闻卡片-PR方案.md)  
> **编码回执**：[Cursor 编码回执](./2026-08-08-L7-explore真搜索-d3-1见闻卡片-Cursor编码回执.md)  
> **时刻**：2026-08-08

---

## 总判

**验收通过。** 纯前端、后端零改；代码与修订后 PR 逐项对齐；B1/N1/N4 已吸收；vue-tsc + build 通过；scope 未越界。剩余 = 维护者相处复验。

---

## scope 核对（git diff）

| 文件 | 状态 | 核对 |
|------|------|------|
| `desktop/src/types.ts` | M | ✓ ExploreHit/ExploreFound/ExploreCard + 收窄 explore_drift + TalkCardItem 并集 |
| `desktop/src/components/ExploreCard.vue` | 新建 | ✓ 角标「看」+ hits≤3 faint + 纸感实拷 + 侧条调淡 |
| `desktop/src/composables/useQi.ts` | M | ✓ cardKey 分支去重（N1）+ B1 门控 |
| `desktop/src/components/TalkView.vue` | M | ✓ 按 card.type 分支渲染 |

**diff stat**：3 改 + 1 新，共 +61/-10，全部在 `desktop/src/`。**`qi/` 后端零改**（drift/brain_delivery/explore.py/layer/brain/settings 未碰）✓

---

## 代码核对（逐项对齐 PR）

### 1. `types.ts`
- `ExploreHit`/`ExploreFound`（entries/source/query）✓
- `ExploreCard`（type/found/summary/qi_line/action_id/season/curiosity/source/sandbox）对齐后端 drift 返回 ✓
- `ActionPayload.explore_drift` 收窄：`found: ExploreFound | null` + 补 `qi_line?`/`speak?`/`source` ✓
- `TalkCardItem.card: CreationCard | ExploreCard` 并集 ✓

### 2. `ExploreCard.vue`
- 角标「看」✓ HITL3
- hits `slice(0, 3)` ≤3、`--ink-faint` 小字 faint ✓ HITL1a
- query 作 faint hint（`--mono` 9.5px）✓
- `.paper` 实拷 ActionCard 纸感（padding/bg/border/radius/color + `::before` 侧条）✓ N4
- `.paper.explore::before` 侧条 `--ember` 55%→35% 调淡，区分创作卡 ✓
- season footer ✓
- `rel="noopener noreferrer"`（略强于 PR 的 `noopener`，更稳，非偏离）✓

### 3. `useQi.ts`
- `cardKey(card)`：`creation_card→c${creation_id}` / `explore_drift→e${action_id}` 按 type 分支 ✓ N1
- `appendCard(card: CreationCard | ExploreCard)` 泛化 + cardKey 去重 ✓
- `qiWs.on("action")`：creation_card→append+return；explore_drift→`source==="web" && Array.isArray(entries) && entries.length>0`→append ✓ B1（空手/内部不入卡）

### 4. `TalkView.vue`
- import ExploreCard ✓
- `m.kind==="card"` 分支：`creation_card`→ActionCard，`explore_drift`→ExploreCard ✓（v-if 收窄，vue-tsc 通过）

---

## B1 专项核对

| 场景 | 后端 broadcast | 前端门控 | 结果 |
|------|------|------|------|
| 外部有 hits | source=web, found.entries 非空 | `entries.length>0` ✓ | **先开口后见闻卡** |
| 外部空手 | source=web, found=null/entries 空 | `entries.length>0` 不满足 | **只开口、无卡** ✓ |
| 内部沙箱 | source=sandbox | `source==="web"` 不满足 | **无卡、不 speak** ✓（d-1 不回归）|

B1 关闭确认 ✓

---

## 构建

| 命令 | 结果 |
|------|------|
| `npx vue-tsc --noEmit`（`desktop`） | **通过**（exit 0，独立复核） |
| `npm run build` | **通过**（✓ built in 7.72s） |

后端未动，492 测试基线不受影响（d-2 commit 838b047 已含）。

---

## 纪律红线

- R1 不破（相处验证仍维护者做）✓
- **纯前端**：不碰 `qi/` 后端 ✓
- 不改 d-2 `qi_line=summary`（HITL1a，digest 由开口承载，卡片不重复）✓
- 内部沙箱扫不渲染（HITL2）✓
- 外部空手不出卡（B1）✓
- 卡片是「瞥见来源」非搜索结果列表（hits faint ≤3）✓

---

## 偏离确认

| 项 | 处置 |
|----|------|
| `rel="noopener noreferrer"` vs PR `noopener` | 更稳，行为同，非实质偏离 ✓ |

无其他偏离。

---

## 剩余（交维护者）

- **相处复验**：本机独处攒 curiosity≥0.8、过 6h 冷却、概率 0.05 触发外部搜索有 hits 时，看谈区「先 digest 开口 → 后见闻卡（角标看 + hits 小字）」；空手只开口无卡；内部无卡。
- **d-2 push**：网络恢复后 `git push origin main`（838b047 含 d-2）。
- **d-3-1 + d-2 一并提交推送**：本包验收通过后可 commit/push。
- **d-3-2**：沙箱深读（后端新内部源），另开。

---

## 收口状态

- **工程交付**：✅ 完成（前端 + vue-tsc + build + scope 无越界）
- **相处复验**：⏳ 待维护者
- **d-3-1 收口**：待相处复验 + 提交推送后宣告

---

*验收记录 · 2026-08-08 · 通过 · 纯前端 · vue-tsc + build 通过*
