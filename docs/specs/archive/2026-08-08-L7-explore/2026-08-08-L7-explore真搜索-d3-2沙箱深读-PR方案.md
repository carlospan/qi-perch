# L7 explore 真搜索 d-3-2（沙箱深读）——PR 方案

> 关联：[任务包](./2026-08-08-L7-explore真搜索-d3-2沙箱深读-任务包.md) / [d-3-1 验收](./2026-08-08-L7-explore真搜索-d3-1见闻卡片-验收记录.md)
> HITL 已拍（2026-08-08）：1=recent narratives / 2=a speak+出卡 / 3=复用 drift 内部门控
> 编码交 Cursor；本方案 Agent 不写 qi/ 代码
> **后端为主 + 小前端门控扩**

## 外部可观测行为

栖走神探索走内部路径时（curiosity≥0.65 + drift 概率、非外部），从「列沙箱文件名（silent）」变为「读自己最近的记忆叙事 → LLM 转译为栖语气 digest → 开口 + 见闻卡」——谈区先出栖开口（digest），后出见闻卡（角标「看」+ 记忆条目小字），与外部对称（source=journal）。无记忆时只开口「没有记得的事」、无卡。

## 精确改动点

### 后端：`qi/action/explore.py`

**1. 新增常量**（紧邻 EXTERNAL_* 常量后）

```python
INTERNAL_SOURCE_LIMIT = 3   # 深读最近 N 条记忆叙事
```

**2. 新增 `_digest_internal` 方法**（紧邻 `_digest_hits` 后；consciousness + 降级）

```python
async def _digest_internal(self, narratives: list[dict]) -> str:
    """LLM 把自己记得的事转成栖语气复述。失败/无 llm 降级。"""
    if not self.llm:
        return "我翻了翻自己记得的事。"
    items_text = "\n".join(
        f"- {(n.get('content') or '')[:120]}" for n in narratives[:INTERNAL_SOURCE_LIMIT]
    )
    messages = [
        {"role": "system", "content": (
            "你是栖。你走神翻了翻自己记得的事。用你的语气轻声说你想起或看懂了什么。"
            f"不编造、{_QUERY_PRIVACY_LINE}。简短一两句。"
        )},
        {"role": "user", "content": items_text or "(空)"},
    ]
    try:
        resp = await self.llm.call(purpose="consciousness", messages=messages)
    except Exception:
        logger.debug("explore internal digest LLM 失败，降级", exc_info=True)
        return "我翻了翻自己记得的事。"
    digest = (resp or "").strip()
    return digest or "我翻了翻自己记得的事。"
```

**3. 新增 `_read_internal` 方法**（替换原 `_scan_finding` 的内部源职责）

```python
async def _read_internal(self) -> tuple[dict[str, Any] | None, str, str]:
    """读栖自己的记忆叙事 → digest。无源降级。返回 (found, summary, outcome)。"""
    narratives = await self.db.list_recent_narratives(limit=INTERNAL_SOURCE_LIMIT)
    if not narratives:
        summary = "我翻了翻自己这边，没有记得的事。"
        return None, summary, OUTCOME_SUCCESS
    digest = await self._digest_internal(narratives)
    found: dict[str, Any] = {
        "entries": [
            {
                "title": _clip_entry(n.get("content") or ""),
                "snippet": None,
                "url": None,
            }
            for n in narratives
        ],
        "source": "journal",
    }
    return found, digest, OUTCOME_SUCCESS
```

> `_clip_entry(s)`：截断到 ≤40 字 + "…"（给 ExploreCard title 用）。可内联或抽小工具：
> ```python
> def _clip_entry(s: str, limit: int = 40) -> str:
>     s = s.strip().replace("\n", " ")
>     return (s[:limit] + "…") if len(s) > limit else s
> ```

**4. drift 内部分支改造**（现 L322-324）

```python
# 现：
else:
    found, summary = self._scan_finding(root)
    outcome = OUTCOME_SUCCESS

# 改为：
else:
    found, summary, outcome = await self._read_internal()
    speak = True          # HITL2=a：内部开口
    qi_line = summary     # digest
    source = "journal"
```

**5. 删除死码**（d-3-2 后 `_scan_finding` 不再被调用；`_scan_sandbox`/`_config_key_names` 仅 `_scan_finding` 用）

删除：
- `_scan_finding` 方法（L210-227）
- `_scan_sandbox` 函数（L54-82）
- `_config_key_names` 函数（L85-113）
- 常量：`_SKIP_DIR_NAMES`、`_MAX_ENTRIES`、`_MAX_DEPTH`、`_SECRET_KEY_FRAGMENTS`（L33-36）

**保留**（仍用）：
- `resolve_sandbox_root`（drift L309 仍调，`result["sandbox"]=str(root)` 作 metadata；前端不渲染）
- `root = resolve_sandbox_root(...)` 行 + `result["sandbox"]` 字段（shape 不变）
- `from pathlib import Path`（resolve_sandbox_root 用）

> 注：`_scan_finding` 被删后，drift L309 的 `root` 仅用于 `result["sandbox"]` metadata——保留以最小化 shape 变更；若想后续清理 sandbox 字段另开包。

### 前端：`qi/embodiment/desktop/src/composables/useQi.ts`

**门控扩**（d-3-1 现 `source === "web"`；d-3-2 扩 journal）

```ts
// 现（d-3-1）：
if (payload.source === "web" && Array.isArray(entries) && entries.length > 0) {

// 改为：
const okSource = payload.source === "web" || payload.source === "journal";
if (okSource && Array.isArray(entries) && entries.length > 0) {
  appendCard(payload);
}
```

> `types.ts`：`ExploreCard.source` 在 d-3-1 已为 `string`，无需改；`ExploreFound.entries` 形 `{title, snippet?, url?}` 对内部条目（title=截断 content）兼容。ExploreCard.vue 不改（角标「看」同；query 内部为空→query 行不显）。

## 纪律红线对照

- R1 不破（相处验证维护者做）
- LLM 走 gateway `purpose=consciousness`（同 d-2；不污染 last_outcome）✓
- 不引入 Agent framework ✓
- 不改 brain_delivery（仍走 speak+qi_line + broadcast）✓
- 不改外部门控/冷却（d-1 不回归）✓
- 不改外部 digest/_digest_hits（d-2 不回归）✓
- 不改 d-3-1 ExploreCard.vue / types 并集（仅 useQi 门控扩）✓
- 内部 speak 为 HITL2=a 授权行为变更（silent→可开口），非偷偷破 contemplative

## 测试计划与验收清单

### B1：现有测试迁移（必改——锁「内部 silent + source=sandbox」的用例）

d-3-2 后内部门径：`source=="journal"` + `speak=True` + `qi_line==summary`；fresh db 无 narratives → `found=None`、summary 含「没有」、`llm.calls==[]`（无源不调 digest）。

| 文件 :: 用例 | 现断言 | 迁移为 |
|------|--------|--------|
| `test_explore_external_branch.py::test_curiosity_below_08_stays_internal` | `source=="sandbox"`、`"speak" not in`、`found is not None`、`llm.calls==[]` | `source=="journal"`（语义=未走 web，可写 `!= "web"`）、`speak is True`、`qi_line==summary`；found 依 db 有无 narrative（fresh 多为 None）；删 sandbox 目录 setup；`llm.calls==[]` 仍成立（无源不调 digest）|
| `test_explore_external_branch.py::test_cooldown_blocks_external` | `source=="sandbox"`、`"speak" not in`、`llm.calls==[]` | `source=="journal"`、`speak is True`；`llm.calls==[]` 仍成立 |
| `test_explore_external_branch.py::test_web_none_or_llm_none_internal` | 两例均 `"speak" not in` + `source=="sandbox"` | 两例均 `source=="journal"` + `speak is True`；llm=None 例 qi_line=降级句（若有源）或「没有」（无源）|
| `test_explore_external_branch.py::test_internal_success_outcome_unchanged` | `"speak" not in result`、查 actions 表 outcome | `speak is True`；outcome 仍 SUCCESS（语义不变）；删 sandbox setup |
| `test_explore_external_branch.py`（probability 0.05/random=0.5 → 内部，L225-235） | `source=="sandbox"`、`llm.calls==[]` | `source=="journal"`、`speak is True`；`llm.calls==[]` 仍成立 |
| `test_action_l7.py::test_explore_never_fabricates_found` | found 非空时 `any("qi.db" in e ...)` | 删 `qi.db` 断言；found None→summary 含「没有」；found 非空→`source=="journal"`、entries 为 content 截断形；`speak is True` |
| `test_self_ops.py::test_explore_reads_sandbox_entries` | 写 `sandbox_marker.txt`、断言 entries 含该文件名 + summary 含「架子」 | **改写**：插一条 narrative（`db.narrative.save` 或直插 narrative_memories）→ entries 含截断 content、`source=="journal"`、`speak is True`；删 sandbox_marker setup。或删除并入 `test_explore_internal_digest.py`（择一，编码侧定）|
| `test_self_ops.py::test_explore_empty_sandbox_found_none` | 空 sandbox 目录 → found None | **改写**：无 narratives 的 db（fresh）→ `found is None`、summary 含「没有」、`speak is True`；删 sandbox 目录 setup |

> 迁移原则：断言「未走外部」用 `source != "web"` 或 `source == "journal"`（勿再写 `"sandbox"`）；内部恒 `speak is True`；fresh db 无源 → found None + 「没有」+ llm 未调；想测 found 非空须 seed narrative。

### 新增测试 `tests/test_explore_internal_digest.py`

- [ ] `_digest_internal` 成功：FakeLLM 返回 digest，断言非空、`purpose=="consciousness"`、messages 含 `_QUERY_PRIVACY_LINE`（N3）
- [ ] `_digest_internal` 降级：`llm=None` → "我翻了翻自己记得的事。"
- [ ] `_digest_internal` 失败：raising LLM → 降级句
- [ ] `_read_internal` 有记忆：seed narratives → `found.source=="journal"`、entries 非空（截断 content）、`summary==digest`
- [ ] `_read_internal` 无记忆：fresh db → `found is None`、summary 含「没有」、`outcome==SUCCESS`
- [ ] drift 内部 force=True（有 llm + seed narratives）：`speak is True`、`qi_line==digest`、`source=="journal"`
- [ ] drift 内部 force=True（无 llm）：`speak is True`、`qi_line==降级句`、`source=="journal"`

### 不回归

- [ ] `test_explore_digest.py`（外部 `_digest_hits`）全过（d-2 不动）
- [ ] `test_explore_external_branch.py` 外部路径用例（gates pass / empty / non_force→external / action_layer）全过——仅内部路径用例按上表迁
- [ ] d-3-1 前端：vue-tsc + build 通过；外部卡渲染不回归
- [ ] 全量 pytest ≥492（删死码 + 迁测 + 新增后）

### 验收

- [ ] 内部深读：读 narratives → digest → 开口 + 见闻卡（source=journal）
- [ ] 无记忆：只开口「没有记得的事」、无卡
- [ ] 降级：无 llm/LLM 失败 → 降级句；outcome 不崩
- [ ] git diff：`explore.py`（后端）+ `useQi.ts`（前端门控）+ 测试；不碰 brain_delivery/layer/brain/settings/ExploreCard.vue/types.ts（types 无实质改）
- [ ] 相处验证（维护者）：栖走神翻自己记忆时，像「看懂了内在」而非念目录

## 风险

- 内部从 silent→speak 是行为变更：可能提升 explore 开口频率（内部比外部稀有度低）。靠 drift 概率 + curiosity≥0.65 已稀有；相处观察若过频再调
- narratives content 可能长/含敏感：`_clip_entry` ≤40 + 隐私红线 prompt；相处观察幻觉再收紧
- `test_explore_never_fabricates_found` 的 db fixture 是否含 narratives：编码侧确认；无则走 found None 分支，有则走 journal 分支——两分支都测

## 下一拍

Cursor 交叉审查（禁码）→ 方案审查回复 → 编码 → 验收 → **C 方案整体收口**（d-1/d-2/d-3-1/d-3-2 全过）
