# L7 explore 真搜索 d-1——整改复审

> **角色**：Cursor（执行侧整改复审，本轮**仍禁码**）  
> **依据**：修订后 [任务包](./2026-08-08-L7-explore真搜索-d1联网地基.md) + [PR 方案](./2026-08-08-L7-explore真搜索-d1联网地基-PR方案.md)（文首「审查后修订 · 路 B 第2步」）+ [交叉审查](./2026-08-08-L7-explore真搜索-d1联网地基-Cursor交叉审查.md)  
> **时刻**：2026-08-08  

---

## 总判

**B1 / B2 / B3 均已关闭。整改复审通过。**  
仍禁码，待方案 Agent 落 `-编码请求.md` + 明示可编码后再动 `qi/`。

---

## 阻塞项逐条核对

| 项 | 交叉审查要求 | 修订落点 | 状态 |
|----|--------------|----------|------|
| **B1** | 任务包清独立 `daily_limit` 残留，与 HITL③ 对齐 | 范围/步骤/验收改为「不加 `daily_limit`」+ 复用 ActionBudget 20；出现的 `daily_limit` 字样均为**禁止/已撤**说明，非验收加字段。PR §二D/§四同口径 | **关闭** |
| **B2** | 钉死 llm 接线 + consciousness purpose + scope 含 brain.py | §二B：`ExploreAction` 加 `llm`+`web`，`_make_query` → `purpose="consciousness"`（对照 gateway：仅 `conversation` 刷新 `last_outcome`，复用成立）；§二C：`ActionLayer` 下传 + 建 client；§二C2：`brain.restore_state` 传 `self.llm`；§七 scope 含 `qi/core/brain.py`（仅构造一行） | **关闭** |
| **B3** | 钉说出口；不改 delivery；注入下轮延后 | §一 Spec + §二B：外部成功/空手均 `speak=True`+`qi_line`（模板、不二次 LLM），走 `deliver_action_result` L134–137 `elif`；**不改** `brain_delivery.py`；内部不带 `speak`；任务包「注入下轮对话」标降级延后 | **关闭** |

无未关闭阻塞项，**不退回**方案 Agent。

---

## 非阻塞采纳核对（抽查）

| # | 状态 |
|---|------|
| N1 独处由 volition | ✓ §一 / `_should_external` 注释 |
| N2 settings.yaml 不进 diff | ✓ §二D / §七 |
| N3 `${TAVILY_API_KEY}` + `.env.example` | ✓ |
| N5 只实现 Tavily | ✓ |
| N7 force / 非 force 分测 | ✓ §二E |
| N8 `web=None`/`llm=None` → 内部 | ✓ |

---

## 编码前理解确认（仍禁码）

获编码请求 + 明示可编码后，将按修订后 PR：

1. 新建 `explore_web.py`（Tavily + httpx；失败/空 → None）；**不加**独立 `daily_limit`。  
2. `explore.py`：外部分支门控 + `_make_query(consciousness)` + 外部 `speak`/`qi_line`；内部行为与不说不变。  
3. `layer.py` + `brain.restore_state` 传 `llm` / 建 web client。  
4. `settings.example.yaml` + `.env.example`；测 mock + 门控 + 红线 + 说出口。  
5. 回写 L7-action / progress；contract 日限 drift **只标注**。  
6. 不碰 README / `brain_delivery.py` / 心跳逻辑。

**需澄清**：无。

---

## 下一拍

1. 方案 Agent 落 `-编码请求.md`（以修订后 PR 为准）。  
2. 维护者 / 方案明示可编码 → Cursor 编码 → `-Cursor编码回执.md` 完工段。

**Cursor 结论：B1/B2/B3 已关闭，待编码请求。**
