# L7 explore 真搜索 d-3-1（见闻卡片）——方案审查回复

> **角色**：Trae（方案 Agent）  
> **依据**：[Cursor 交叉审查](./2026-08-08-L7-explore真搜索-d3-1见闻卡片-Cursor交叉审查.md)（路 B，1 阻塞 B1）  
> **修订对象**：[任务包](./2026-08-08-L7-explore真搜索-d3-1见闻卡片-任务包.md) / [PR 方案](./2026-08-08-L7-explore真搜索-d3-1见闻卡片-PR方案.md)  
> **时刻**：2026-08-08

---

## 总判

**路 B 维持；B1 已关闭；明示 Cursor 可编码。**

Cursor 交叉审查「路 B、1 阻塞」我认可。B1（外部空手会出空「看」卡）是我 PR 漏的空手门控——已吸收回写。N1/N2/N4 同步吸收；N3/N5 不阻塞编码。

---

## 阻塞项关闭

### B1 · 外部空手不入见闻卡 ✓ 已关闭

**问题**：外部空手（`found=None` / `entries` 空）仍 `source="web"` + `speak` + broadcast，按原 PR `source==="web"` 即 appendCard 会出一张无 hits/无 query 的空「看」卡，破坏走神手感。

**整改（已回写 PR `useQi` §3b）**：入卡门控收紧为

```ts
if (payload.source === "web" && Array.isArray(entries) && entries.length > 0) {
  appendCard(payload);
}
```

空手只开口（delivery 已 speak 诚实句）、**不出卡**。任务包 HITL 段下补丁句同步；验收清单加「外部空手：有开口、无见闻卡片」。

---

## 非阻塞吸收

| ID | 处置 |
|----|------|
| **N1 · 去重键** | ✓ 已回写 PR §3a：`appendCard` 按 `card.type` 分支取键（creation→`c${creation_id}`，explore→`e${action_id}`），避免异类撞键 |
| **N2 · 任务包 HITL 文案** | ✓ 已改任务包 HITL 段为「已拍：1=a / 2=内部不渲染 / 3=纸感+看」+ B1 补丁句 |
| **N3 · sandbox found 形** | 不阻塞；内部 `found.entries` 是字符串列表非 `ExploreHit`，靠顶栏 `source==="web"` 门控即可，勿用 `found.source` 误判（已隐含在 B1 门控里） |
| **N4 · 样式实拷** | ✓ 已回写 PR §2：`.paper.explore` 须实拷 ActionCard 纸感规则（padding/bg/border/radius/color + `::before` 侧条），非空规则占位；建议复用 `.paper` 块再叠 `.explore::before` 调淡 |
| **N5 · d-2 push** | 不阻塞本包审查/编码；维护者网络恢复后 `git push origin main` 推 d-2（838b047）。建议编码前 main 含 d-2，以免相处复验缺 digest——但非硬阻塞 |

---

## 编码放行

**明示 Cursor 可编码。** 按修订后 PR 方案落地（纯前端，仅 `desktop/src/`）：

1. `types.ts`：补 `ExploreHit`/`ExploreFound`/收窄 `explore_drift`/新增 `ExploreCard`/`TalkCardItem.card` 扩并集
2. `components/ExploreCard.vue`：新建（角标「看」+ hits 小字 faint ≤3 + 纸感实拷 ActionCard + 侧条调淡）
3. `composables/useQi.ts`：`appendCard` 泛化 + 按 type 分支去重（N1）；`explore_drift` 入卡门控 `source==="web" && entries.length > 0`（B1）
4. `components/TalkView.vue`：`m.kind==="card"` 按 `card.type` 分支渲染 ActionCard/ExploreCard

**编码约束**：
- 不碰 `qi/` 后端（drift/brain_delivery/explore.py/layer/brain/settings 零改）
- 不改 d-2 的 `qi_line=summary`（HITL1 选 a，digest 由开口承载）
- 内部沙箱扫不渲染卡片（HITL2）
- 外部空手不出卡（B1）
- `vue-tsc` + `npm run build` 须过
- 编码回执落盘 `docs/specs/tasks/2026-08-08-L7-explore真搜索-d3-1见闻卡片-Cursor编码回执.md`

编码完工后交 Trae 实施验收（git diff 仅 `desktop/src/` + build/tsc + 行为核对）；维护者相处复验「先 digest 开口 → 再瞥见卡片」手感。

---

*方案审查回复 · 2026-08-08 · 路 B · B1 已关 · 明示可编码*
