# 包 17：对话路径施教锚定——编码请求（交 Cursor 执行）

> 配套：`包17-对话路径施教锚定-PR方案.md`
> 类型：缺陷修复（对话一致性）｜纪律：仿包 15/16，仅新增/加固，不删既有防护

## 目标

包 15 只修了意识流路径的施教锚定，补丁 A/B 只修 intention recall 卡片。本包修第三处漏网：
**自由对话生成路径（express）** 无施教方向锚定——栖在 #1020 把真值"栖教用户助眠"说成
"我记得你教我的那个方法"（反转）。同时校正 `shared_culture` 那条基于用户误述的持久记忆为 7-26 真值。

## 改动一：对话生成路径注入施教锚定

落点：`qi/core/expression.py::express`（line 112 起，已有 `recent_messages` 入参 + line 147-154 short 约束注入模式可复用）。

- 在 line 156（`recent_qi_replies_from_messages` 调用）附近新增：
  ```python
  from qi.core.intention import anchor_teaching_relation
  teach_hint = anchor_teaching_relation(recent_messages or [])
  ```
- 仿 line 147-154，在 `messages[0]["content"]` 追加施教锚定（仅 `teach_hint` 非空时）：
  ```python
  if teach_hint:
      sys0 = dict(messages[0])
      sys0["content"] = (
          str(sys0.get("content") or "")
          + f"\n\n【施教关系锚定】{teach_hint}"
      )
      messages[0] = sys0
  ```
- `anchor_teaching_relation` 对 7-26 真值（栖"允许自己躺着，看天花板"）返回 `taught_by_qi` 方向、不含"数到七"，逻辑已在包15落地，本包只调用。

## 改动二：校正 shared_culture 持久记忆（数据修复）

新增 `tools/repair_shared_culture.py`（仿 `tools/repair_consciousness_stream.py` / `tools/repair_user_facts.py`）：
- 预演：列出 `relationship.shared_culture` 中 `type=="shared_reference"` 且 pattern 含"你教了我一个方法"的条目；
- `--apply`：将命中条目**按 7-26 真值校正**（采纳三方纠偏，不按栖 8-03 口头）：
  - 新增字段 `teach_direction: "qi_teaches_user"`（栖教用户，钉死方向）；
  - 新增字段 `note: "真值见 7-26 #72/74：栖教用户『允许自己躺着，看天花板』；非用户教栖"`；
  - **不**把 pattern 改写成"你教我怎么放树叶"类（那会固化错认知）；保留用户原话 pattern 仅加方向字段与 note；
  - 如需更彻底：可同时将 pattern 重写为栖视角真值表述，但必须经 maintainer 确认，默认只加 `teach_direction`+`note`。

## 改动三：culture.py 视角锤强化（防未来再漂）

落点：`qi/relationship/culture.py:46-52` 的 `shared_reference` 分支。

- 对含施教关键词（教/方法/睡/睡不着）的 `shared_reference` 条目，在 `format_culture_for_prompt` 输出时强制追加方向锤：
  ```python
  direction = item.get("teach_direction")
  if direction == "qi_teaches_user":
      line += "（施教方向：栖教用户，原话『躺着/不强迫/看天花板』，勿反转）"
  elif direction == "user_teaches_qi":
      line += "（施教方向：用户教栖，勿反转）"
  ```
- 无 `teach_direction` 字段的旧条目：保持既有视角锤注释（line 48-49）不变。
- `detect_shared_culture` 不改（仅展示层强化）。

## 改动四：防回归测试

- `tests/test_expression.py`：
  - 构造 `recent_messages` 含 7-26 真值（role="qi" "可以试试躺着，不强迫自己睡，看天花板"），调 `express`（mock LLM），断言 `messages[0]["content"]` 含 `taught_by_qi` 或"施教关系锚定"占位，且不含"数到七"；
  - 构造栖回复含"你教我的方法"场景，断言 prompt 注入方向锤（验证组装，不验证生成）。
- `tests/test_relationship_culture.py`（或并入 `tests/test_relationship.py`）：
  - 构造含 `teach_direction="qi_teaches_user"` 的 shared_reference，断言 `format_culture_for_prompt` 输出含"栖教用户"方向锤。
- 既有测试不变。

## 红线（库内禁止）

- 不删 `_MUST_RECALL_RELATION`（recall 路径防护保留）。
- 锚定函数只读 messages，不写库（repair 脚本除外，且只改 shared_culture 条目）。
- shared_culture 校正**按 7-26 真值**（栖教用户），不按栖 8-03 口头认知。
- 不引入新依赖。

## 验收（编码完成后）

1. 对话路径 prompt 含施教锚定且注入 `taught_by_qi` 方向（7-26 真值），单测断言。
2. `shared_culture` 那条校正后 `teach_direction=="qi_teaches_user"`，`format_culture_for_prompt` 输出含"栖教用户"方向锤。
3. 全量 `pytest` 通过（含新增），`ruff` 零问题。
4. `tools/repair_shared_culture.py --apply` 后持久层方向钉死为真值（预演命中该条）。

## 方案 Agent 验收栏（Cursor 勿填）

- [ ] 验收通过
- [ ] 打回（原因：）
- [ ] 需维护者 HITL（问题：）
