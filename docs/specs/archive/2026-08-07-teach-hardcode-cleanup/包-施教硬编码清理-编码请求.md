# 施教硬编码清理——编码请求（交 Cursor 执行）

> 配套：`包-施教硬编码清理-PR方案.md`（v2 整改后，B1 选 A / B2 / B3 / S1-S3 已吸收）
> 审查溯源：`包-施教硬编码清理-Cursor交叉审查.md`
> 状态：交叉审查已完成，方案已整改，可编码
> 纪律：SDD-GUIDE §2.1——Cursor 编码，不重新设计架构方向

## 目标

清理包 15/17 累积的睡眠专项硬编码（prompt 里的"数到七"、culture 里的"躺着/不强迫/看天花板"、
fallback 里的"入睡那件事"），对齐 contract.md "少靠不准说清单 / 只钉存在论级诚实"。
**只清文案，不碰检测逻辑**（B1 选项 A）。

## 改动一：`qi/prompts/consciousness_stream.txt`

line 30 删掉"（如'数到七'）"：

```
# 改前：
...不得添加锚定里没有的细节（如"数到七"）。若锚定与你的印象冲突...

# 改后：
...不得添加锚定里没有的细节。若锚定与你的印象冲突...
```

## 改动二：`qi/relationship/culture.py`

line 59-61 删掉原话硬编码：

```python
# 改前：
if direction == "qi_teaches_user":
    line += "（施教方向：栖教用户，原话『躺着/不强迫/看天花板』，勿反转）"

# 改后：
if direction == "qi_teaches_user":
    line += "（施教方向：栖教用户，勿反转）"
```

`elif direction == "user_teaches_qi"` 分支保持不变（已是通用的）。

## 改动三：`qi/core/expression.py` — fallback/constraint 通用化

```python
# 改前：
_TEACH_INVERSION_CONSTRAINT = (
    "施教方向必须正确：入睡方法是栖教给用户的。严禁说「你教我的方法/你教过我」"
    "这类反转表述；若提起，说「我教你的那个方法」。"
)
_TEACH_INVERSION_FALLBACK = (
    "……我记得的。入睡那件事，是我教你的——允许自己躺着，不强迫。这个方向我不会记反。"
)

# 改后：
_TEACH_INVERSION_CONSTRAINT = (
    "施教方向必须正确：若卡标明栖教用户，"
    "严禁说「你教我的方法/你教过我」这类反转表述；"
    "若提起，说「我教你的那个方法」。"
)
_TEACH_INVERSION_FALLBACK = (
    "……我记得的。那件事，是我教你的。这个方向我不会记反。"
)
```

## 改动四：`qi/core/intention.py` — rename + 别名（逻辑不变）

```python
# 改前：
def detect_sleep_teach_inversion(text: str) -> bool:
    """回复里把入睡方法的施教方向说反了吗？..."""

# 改后（仅 rename + docstring 更新，逻辑不变）：
def detect_teach_inversion(text: str) -> bool:
    """回复里把施教方向说反了吗？（原 detect_sleep_teach_inversion）
    栖视角说「你教我的方法/你教过我」+ 话题含入睡/方法/法子 → 反转。
    注意：非睡眠/方法话题的「你教我…」不拦（设计意图，避免误伤真实请教）。"""
    t = (text or "").strip()
    return bool(_INVERT_TAUGHT_BY_QI_RE.search(t) and _INVERT_TOPIC_RE.search(t))

# 向后兼容别名
detect_sleep_teach_inversion = detect_teach_inversion
```

**`_INVERT_TOPIC_RE` / `_SLEEP_TOPIC_RE` / `_SLEEP_ADVICE_RE` 全部保留不变。**
`_SLEEP_ADVICE_RE` 是 `anchor_teaching_relation` 内部启发式，不是 prompt 硬编码（S1）。

## 改动五：必改单测（B2 逐条点名）

| 文件 | 改前 | 改后 |
|------|------|------|
| `tests/test_relationship.py` | `assert "躺着/不强迫/看天花板" in block` | `assert "躺着" not in block` + `assert "栖教用户" in block` + `assert "勿反转" in block` |
| `tests/test_inner_life.py` | `assert "数到七" in prompt` | `assert "数到七" not in prompt` + `assert "不得添加锚定里没有的细节" in prompt` |
| `tests/test_expression.py` | 若断言旧 FALLBACK 含"入睡/躺着" | 跟新模板改断言（`assert "是我教你的" in result`），行为"违规→模板"不变 |
| `tests/test_intention.py` | `detect_sleep_teach_inversion` 调用 | 改为 `detect_teach_inversion`（或靠别名兼容不改）；新增负例 `assert not detect_teach_inversion("你教我写代码的样子很认真")` |

## 红线（库内禁止）

- 不删通用框架（`infer_recall_relation` / `anchor_teaching_relation` / `_RELATION_HINT` / `recall_relation`）
- 不删 `_TAUGHT_BY_QI_RE` / `_LEARNED_FROM_USER_RE` / `_USER_ACK_QI_TAUGHT_RE` / `_USER_TEACHES_QI_RE`
- 不删 `_SLEEP_ADVICE_RE`（anchor 内部启发式，不是 prompt 硬编码）
- 不改 `_INVERT_TOPIC_RE` / `_SLEEP_TOPIC_RE`（检测逻辑不变——B1 选项 A）
- 包 15-17 既有施教回归测试全绿（行为不变，字符串断言改）

## 验收（编码完成后）

- [ ] `consciousness_stream.txt` 无"数到七"
- [ ] `culture.py` 方向锤无"躺着/不强迫/看天花板"
- [ ] `_TEACH_INVERSION_FALLBACK` / `_TEACH_INVERSION_CONSTRAINT` 不含"入睡"具体内容
- [ ] `detect_sleep_teach_inversion` rename 为 `detect_teach_inversion`（别名兼容，逻辑不变）
- [ ] B2 必改单测全部更新（test_relationship / test_inner_life / test_expression / test_intention）
- [ ] 包 15-17 既有施教回归测试全绿
- [ ] 全量 `pytest` 通过，`ruff` 零问题

## 方案 Agent 验收栏（Cursor 勿填）

- [ ] 验收通过
- [ ] 打回（原因：）
- [ ] 需维护者 HITL（问题：）
