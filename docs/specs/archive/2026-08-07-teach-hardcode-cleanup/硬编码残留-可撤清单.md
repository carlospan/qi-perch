# 硬编码 / 过拟合残留——可撤清单（Cursor）

> **性质**：对照审查，不写码。  
> **对照**：刚闭环的「施教硬编码清理」（文案墙）+ N5-a/b。  
> **时刻**：2026-08-07  

图例：

| 标记 | 含义 |
|------|------|
| **撤** | 建议下一包动刀（同类债） |
| **收** | 保留机制，收成通用（别删闸） |
| **留** | 存在论/N5 铁律或产品契约，不当事故补丁清 |
| **另** | 另一条线（R2 prompt 瘦身等），勿与施教债混装 |

---

## A. 施教 / 助眠专病（下一包优先）

| 优先级 | 文件 | 点 | 建议 | 说明 |
|--------|------|----|------|------|
| P0 | `qi/core/expression.py` | `_TEACH_INVERSION_FALLBACK`「那件事，是我教你的」 | **撤→中性** | 无卡/方向不明时仍宣称「我教你」；改为「方向以记忆为准，我不会跟着说反」或按 `recall_relation` 分支 |
| P0 | `qi/core/intention.py` | `detect_teach_inversion` + `_INVERT_TOPIC_RE` | **收** | 有 `card.recall_relation==taught_by_qi` 时只靠反转句式；无卡才用 topic 启发式（交叉审查曾提选项 B） |
| P0 | `qi/core/intention.py` | `_SLEEP_ADVICE_RE`（躺着/不强迫/看天花板/允许自己） | **收** | 7-26 原话特征表；改为：近聊里栖侧含「教」类句 / 或 facts `taught_by_qi`，勿绑助眠词面 |
| P1 | `qi/core/intention.py` | `_anchor_from_facts`「关于**入睡方法**：是你（栖）教…」(~L279–281) | **撤文案** | 文案清理漏网；改「关于方法」+ 不写死入睡 |
| P1 | `qi/core/intention.py` | `_TOPIC_RE` / `_SLEEP_TOPIC_RE` 偏睡 | **收** | 可保留「教\|方法」；「入睡\|睡」降为可选加权，非唯一门 |
| P1 | `qi/prompts/consciousness_stream.txt` L30 | 点名「…/入睡」 | **撤窄化** | →「方法/教过的事」即可；方向交给 `{relation_hint}` |
| P1 | `qi/relationship/culture.py` | `_TEACH_TOPIC_MARKERS` 含睡* | **留/轻收** | 只决定要不要加方向锤；锤本身已通用。可改为以 `teach_direction` 字段为准、markers 仅兜底 |
| P2 | `qi/core/intention.py` | `detect_sleep_teach_inversion` 别名 | **留→日后删** | 兼容包；测全切新名后可删 |
| P2 | `qi/core/expression.py` | `_TEACH_INVERSION_CONSTRAINT`「若卡标明栖教用户…」 | **留/轻收** | 已比焊死入睡好；重生时可只注入当拍 `recall_relation` 中文一句，少用全局禁令 |

**本块不要删**：`infer_recall_relation` / `recall_relation` 字段 / `_TAUGHT_BY_QI_RE` 等通用方向正则 / `assert_reply_respects_card` 里「卡内 taught_by_qi ∧ 反转句式」主路径。

---

## B. N5 语言器官闸（机制，不是专病焊死）

| 文件 | 点 | 建议 | 说明 |
|------|----|------|------|
| `intention.py` | `_DECLARATIVE_MEMORY_RE`、共同回忆 HARD | **留** | 卡外共同史铁律；例词是违规类，不是某段史 |
| `intention.py` | `_MUST_NO_FABRICATE_SHARED` 举例 | **留/可瘦** | 护栏举例可短，勿扩成不准说清单 |
| `expression.py` | `_FACT_CONSISTENCY_CONSTRAINT` | **留** | 重生约束；与 N5 一致 |
| `intention.py` | `_ENTITY_WHITELIST`（深水/珍惜/…） | **留/另观** | 防实体误杀；白名单膨胀时另审，非助眠债 |
| `brain_context.py` | N5-b 双字交 + `_STOP_WORDS` | **留** | 刚落地；停用词表可调参，勿当事故清单清 |

---

## C. Prompt 护栏 / R2（另包）

| 文件 | 点 | 建议 | 说明 |
|------|----|------|------|
| `qi/prompts/conversation.txt` | 【硬规则】【陌生期硬约束】重启≠白纸 等多条 | **另·瘦身** | 存在论级可留 2–4 条；「不要想念/不要评论作息」等属剧本墙，阶段性交给关系状态注入 |
| `qi/prompts/conversation.txt` | 「你是栖…不是工具」开篇 | **另·R2** | 人格长文主战场；与施教清理分开 |
| `qi/prompts/consciousness_stream.txt` L27 | 禁止字面在场（坐着/呼吸/窗外…） | **留** | 本体护栏；举例可收成「不写字面身体在场」 |
| `qi/prompts/self_reflection.txt` | 纠正「白纸/无连续记忆」 | **留→可迁** | 纠历史误解；长期靠身份快照真值，少靠禁令 |
| `qi/prompts/story_weaving.txt` | 不虚构见面/时差 | **留** | 事实诚实，窄且对 |
| `qi/inner_life/consciousness.py` | 模板外再嘱「不要写呼吸、坐着…」 | **收** | 与 stream 重复；留一处即可 |

---

## D. 注释 / 非运行时（不动）

| 文件 | 点 | 建议 |
|------|----|------|
| `brain_delivery.py` | 「放了好几天 /《凌晨五点》」实证注释 | **留**（注释） |
| `intention.py` / 各处 | `#1020` 等实证注释 | **留** |
| `brain_context.py` | 注释里「助眠」「烤鸭」 | **留** |

---

## E. 建议打包顺序（若开任务）

1. **包·施教机制收束**（A 的 P0–P1）：FALLBACK 中性化；`detect_*` 卡感知；弱化 `_SLEEP_ADVICE_RE`；补 `_anchor_from_facts` 入睡文案；stream L30 去「入睡」。  
2. **包·conversation R2 瘦身**（C）：另立 HITL/验收，勿与 1 混。  
3. 实体白名单、N5 句式闸——无新事故则不主动动。

---

## 一句话

文案墙已拆；**助眠专用检测与「我教你」兜底已于 2026-08-07 收束**（见 `包-施教机制收束-Cursor编码回执.md`）。`conversation.txt` 开篇人格仍属 R2 另包。
