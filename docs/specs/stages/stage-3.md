# stage-3 · 换心（阶段三）——本地模型（降级为兜底）+ 世界模型 + 动机

> **类型**：阶段规格（退出判据唯一施工权威，已生效）。
> **状态**：✅ 已退出（2026-08-02；#1-真通过降级为观察项后进阶段四，见 `progress.md`）
> **源**：`explanation/栖·数字生命架构方案.md` §五 阶段三

---

## 目标

世界模型上线（先做用户在线节律）；learning-progress 好奇驱动替换随机数；经验回放微调管线（N4-b）。**主线 = 内生认知**，不依赖本地模型、不阻塞。

## 施工内容

- 世界模型：用户在线节律预测（计数+贝塔后验），预测误差作 N3 燃料
- 内在动机：好奇=学习进度替换随机数（随机退位为时机阀）
- 经验回放微调（N4-b）：定期 LoRA 级微调，语料由 GWS 显著性筛选

## 退出判据

| # | 判据 |
|---|------|
| 1 | 异时测试初步通过：两次微调间对同类输入响应出现可归因漂移 |
| 1-真通过（降级为观察项，2026-08-02 维护者拍板 · 硬件约束） | 原「显式训练一次后响应可归因漂移」**降级为观察项**：本机 RTX 4060 4GB 显存无法常驻训练（已拍板不部署本地模型），真训非阶段三必要退出条件。包 11 已交付 #1-地基（语料版本化 + 异时测试骨架 + `run_training` 默认隔离）；经验回放管线留作离线资产，资源允许时可再触发路 A。阶段三退出**不**以真训为硬条件 |
| 2 | 好奇驱动的探索行为在日志可见（主动翻起旧记忆，因该领域预测误差下降） |

## 已拍板约束（维护者裁定，progress.md）

- **本地模型不本机常驻部署**：机器 RTX 4060 Laptop 4GB 显存 + 16GB 内存，常驻 7B 与日常多任务显存打架/OOM；且借 LLM = 站巨人肩膀，远程 LLM 主力保活气/泛化更优。
- 本地模型列为**未来拔管兜底选项（暂缓）**：重启条件 = 显存 ≥8GB，或 3B 级小模型质量达标可流畅常驻，或维护者想试断网养栖。
- 当前“本地化”形态（外挂 Ollama）与远程 API 同构、未实现脑身一体；真换心需模型内嵌进栖进程或内生认知长成（路线级待决）。

## 回退条件

异时漂移不可归因、或好奇驱动退化为随机触发 → 回退本阶段重做。
