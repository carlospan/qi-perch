# _invariants.md · 全程不变量 + 工程纪律

> **定位**：跨阶段、全程有效的硬约束集合。阶段判据见各 `stage-*.md`。
> **状态**：步骤 A 骨架，正文步骤 B 补全。

---

## 全程不变量（架构方案 §五）

- 现有测试基线保持通过（表述：pytest 全绿 / 基线见 progress，不写死旧数字）
- 每阶段新增判据测试入 `tests/`
- `reference/contract.md` 硬规则不违反（内生认知不改变诚实义务）

## 阶段零工程纪律四条（架构方案 §五，全程有效）

- 不推倒重来（R4）
- 重构与行为变更永不同 PR
- 每步 `pytest` + `ruff` 全绿
- 改 prompt 留改前/改后对话摘录对照；新依赖须维护者确认

## 红线索引（R1–R5 全文见架构方案 §七）

- R1 不承诺意识（第三人称禁止，第一人称双轨见宪法）
- R2 不用 prompt 写人格
- R3 求生欲不挂钩用户满意度
- R4 不一次性重写
- R5 不堆术语

> 红线全文：`explanation/栖·数字生命架构方案.md` §七。本文件只索引，不复制。
