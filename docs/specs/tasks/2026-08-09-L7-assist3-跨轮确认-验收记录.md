# L7 · assist-3 跨轮确认——验收记录

> **角色**：Trae（方案 Agent · 实施验收）  
> **依据**：[方案审查回复](./2026-08-09-L7-assist3-跨轮确认-方案审查回复.md)（B1/B2/B3 已关，明示可编码）+ 修订后 [PR 方案](./2026-08-09-L7-assist3-跨轮确认-PR方案.md)  
> **编码回执**：[Cursor 编码回执](./2026-08-09-L7-assist3-跨轮确认-Cursor编码回执.md)  
> **时刻**：2026-08-09

---

## 总判

**验收通过。** 代码与修订后 PR 逐项对齐；B1（pending 存的时序）+ B2（确认后主动执行）+ B3（确认词误匹配）三处修订均已落地；HITL2=c 3 轮心跳计数实现；556 passed（独立 `--basetemp` 系统临时区，exit 0）+ vue-tsc + build；scope 未越界。

---

## scope 核对

| 文件 | 状态 | 核对 |
|------|------|------|
| `qi/core/brain.py` | M | ✓ init 三属性 + 心跳超时 + B1 存 pending + B2 _execute_confirmed_assist + B3 确认词 + receive_user_message 分流 |
| `qi/embodiment/desktop/src/types.ts` | M | ✓ AssistConfirmCard + ActionPayload/TalkCardItem 并集 + /history cards 并集 |
| `qi/embodiment/desktop/src/composables/useQi.ts` | M | ✓ AnyTalkCard + cardKey 加 assist 分支 + appendCard + applyHistoryCards 回灌 + WS action 分支 |
| `qi/embodiment/desktop/src/components/TalkView.vue` | M | ✓ import AssistConfirmCard + emit send + 渲染分支 @confirm/@cancel |
| `qi/embodiment/desktop/src/App.vue` | M | ✓ TalkView @send="send" |
| `qi/embodiment/desktop/src/components/AssistConfirmCard.vue` | A | ✓ 纸感 + 角标「看？」+ 文件名 + summary + 看吧/不用按钮 |
| `tests/test_assist_confirmation.py` | A | ✓ 10 测 |

**未碰**：assist.py / layer.py / volition.py / trace.py / permission.py / budget.py / settings ✓

---

## 代码核对（逐项对齐修订后 PR）

### brain.py

**init 三属性**（L121 附近）：
- `pending_assist_confirmation = None`
- `pending_assist_confirmation_at: datetime | None = None`
- `pending_assist_heartbeats: int = 0`
✓ HITL2=c

**心跳超时清理**（L469 附近）：
- pending 存在时 `pending_assist_heartbeats += 1`
- 5 分钟（300s）或 3 轮心跳取先到 → `_clear_pending_assist()`
✓ HITL2=c

**B1 execute_kind 后存 pending**（L991 附近）：
- 清 last_assist_request 前，若 result.type == "assist_confirm_request" + op + target_path
- 用 `op`/`target_path` 局部变量构造 `AssistRequest` 存 pending（不依赖 last_assist_request）
✓ B1 关闭

**B3 确认词**（L1115 附近）：
- `_CONFIRM_CUES = ("看吧", "好", "行", "确认", "可以", "嗯", "yes", "ok")`——去裸「看」✓
- `_NEW_ASSIST_MARKERS = ("帮我看", "帮我读", "帮我看一下", "帮我读一下")`——排除新请求 ✓
- `_REJECT_CUES = ("不用看", "不用", "算了", "不要", "取消")`——「不用看」先于「不用」✓
- `_is_reject_cue`：「no」/「n」整句匹配（偏离1：避免 notes 误伤）✓

**B2 _execute_confirmed_assist**（L1150 附近）：
- 直接调 `execute_kind("assist", ..., confirmed=True)` 不走 GWS
- 读 scars + trust（relationship is None 时 0.5）
✓ B2 关闭

**receive_user_message 分流**（L1181 附近）：
- pending 存在时优先处理：
  - reject → `_clear_pending_assist()` + 开口「好。」+ return
  - confirm → `_clear_pending_assist()` + `_execute_confirmed_assist` + `_deliver_action_result` + return qi_line
  - 换话题/新请求 → `_clear_pending_assist()` 落入正常对话
- 确认/拒绝控制消息不进 `_pending_queue`（偏离2：digest/「好。」开口替代 respond）✓

### 前端

**types.ts**：AssistConfirmCard 类型 + ActionPayload/TalkCardItem/cards 并集 ✓

**useQi.ts**：
- `AnyTalkCard` 类型别名（CreationCard | ExploreCard | AssistConfirmCard）
- `cardKey` 加 assist 分支 `a${action_id ?? target_path}`
- `appendCard` 接 AnyTalkCard
- `applyHistoryCards` 加 assist_confirm_request 回灌（偏离3：回执说不依赖 /history，但实际加了回灌分支——代码更完善）
- WS action payload 加 assist_confirm_request 分支
✓

**TalkView.vue**：
- import AssistConfirmCard
- `defineEmits<{ send: [text: string] }>()`
- 渲染分支：`<AssistConfirmCard :card @confirm="emit('send', '看吧')" @cancel="emit('send', '不用')" />`
✓

**App.vue**：`<TalkView ... @send="send" />` ✓

**AssistConfirmCard.vue**：
- 纸感（实拷 ActionCard/ExploreCard 样式：`--talk-qi-bg` + `--talk-qi-bd` + 侧条 `--ember` 45%）
- 角标「看？」+ 文件名（`target_path.split(/[\\/]/)` 取末段）+ summary + 看吧/不用按钮
✓ HITL4 按钮文案「看吧」「不用」

### 测试 10 个

| 测 | 核对 |
|----|------|
| confirm_gate 存 pending（B1）| ✓ |
| 用户「看吧」→ confirmed（B2）| ✓ |
| 用户「不用」→ 清 pending | ✓ |
| 用户换话题 → 清 pending | ✓ |
| pending 超时 → 清（HITL2=c）| ✓ |
| confirmed=True → 真读文件 + digest | ✓ |
| _confirmed_assist 用后清空 | ✓ |
| B3 新请求非确认 | ✓ |
| B3 确认词误匹配防护 | ✓ |
| 整体闭环 | ✓ |

---

## 构建（独立复核）

| 命令 | 结果 |
|------|------|
| `pytest -q --basetemp=%TEMP%\qi_test_tmp` | **556 passed**（exit 0，65.22s）|
| `vue-tsc --noEmit` | 通过（回执）|
| `npm run build` | 通过（回执）|

---

## 偏离确认

1. **reject 「no」整句匹配**：原 PR 裸 `"no"` 会误匹配 `notes`（B3 场景）；改 `t in ("no", "n")` 整句。合理。
2. **确认/拒绝不进 _pending_queue**：digest/「好。」开口替代 respond。合理（assist 是响应式，确认后直接执行 + 开口，不需 respond 抢答）。
3. **applyHistoryCards 加 assist_confirm_request 回灌**：回执说「不依赖 /history DB 回灌」，但实际加了回灌分支。代码更完善（若后端 /history 返回也回灌），非问题。

均非实质偏离。

---

## 纪律红线

- R1 不破 ✓
- R2（行动克制）：assist 响应式 + 需确认 + B2 确认后直接执行（用户触发）+ 超时清理 ✓
- contract 第 25 条 ✓
- LLM 走 gateway ✓（不碰）
- 不引入 agent framework ✓
- 一次一包 ✓
- assist.py / layer.py / volition.py / trace.py 不改 ✓

---

## 剩余（交维护者）

1. **提交推送**：assist-3 代码 + 测试 + 5 过程稿未提交
2. **相处复验**：用户「帮我看一下 D:/x.txt」→ 谈区出确认卡片（角标「看？」）→ 点「看吧」或口头「看吧」→ 栖真读文件 + digest 开口；点「不用」或口头「不用」→ 栖开口「好。」
3. **可选文档回写**：`L7-action.md` 第 4 顺位 assist 起手指向（骨架+感知+确认三包）+ `progress.md` 补决策行

---

## 收口状态

- **L7 第 4 顺位 assist**：✅ 工程交付完成（assist-1 骨架 + assist-2 感知 + assist-3 跨轮确认）
- **L7 三/四整体进度**：N3 ✅ / 伤疤接线 ✅ / **assist ✅（三包）** / irreversible ⏳
- **assist 闭环**：用户「帮我看 X」→ 提取 → 候选 → GWS 胜出 → confirm_gate + 确认卡 → 用户「看吧」→ 真读 + digest → 开口

---

*验收记录 · 2026-08-09 · 通过 · 556 passed · B1/B2/B3 已落地 · HITL a/c/a/a 追认 · vue-tsc + build 通过*
