# 2026-08-30 · P2：连接失败排错（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.19；承 §三-#6。钥匙失败见 P0「安静地坏掉」；本刀只管 **WS/具身通道连不上**。  
> **编码放行**：**已放行 A（2026-08-30）**。

---

## Spec（外部行为）

1. **通道未接上时，用户看得见原因档**（非仅「未连上」三字）：至少区分「还没接上」与「断了、正在重连」。  
2. **附一句下一步**；入口为 **StatusBar**（及状态页可同句）；**不加**「再连一次」；**不**走 system_notice。  
3. **两档固定文案**：  
   - 从未接上：`通道还没接上` · `请确认栖后端在跑`  
   - 断线重连中：`断了一下` · `正在重连`  
4. **本刀不做**：改 WS 端口/地址 UI、设置页只读露地址、整页诊断、LLM 钥匙失败（已有 system_notice）。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] `connectionStatus` 文案 + useQi 区分从未接上 / 重连中  
3. [x] StatusBar + StateView  
4. [x] 针对性验收  

## 验收（判据）

- [x] 后端未起时 StatusBar 为「通道还没接上 · 请确认栖后端在跑」（非仅「未连上」）  
- [x] 曾连上后断线：显示「断了一下 · 正在重连」  
- [x] 状态页离线文案与 StatusBar 同档  
- [x] 无「再连一次」按钮；不走 system_notice  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做连接失败排错（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围 | **状态条说清楚 + 下一步** | **已拍板 A（2026-08-30）** |
| 2 | 文案与入口形态 | **StatusBar 加长**；不加再连、不用 system_notice | **已拍板 A（2026-08-30）** |
| 3 | 两档固定文案 | **白话最短** | **已拍板 A（2026-08-30）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-08-30）** |

## 不含

- 再连一次按钮  
- 改 WS 端口/地址  
- 设置页露地址  
- 整页诊断  
- LLM 钥匙失败  
