# 2026-09-01 · P3：正式包默认带 BGE（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.29；承 §〇.1 #12、§〇.24、§〇.27–〇.28。  
> **编码放行**：已放行 A（2026-09-01）。

---

## Spec（外部行为）

1. 正式 Windows 包装的**推荐路径**默认纳入离线 BGE，使安装后语义检索可用。  
2. `pack_windows` **默认带 BGE**：无模则自动 fetch；缺模且 fetch 失败 → **硬失败**（退出非 0）；显式 `--skip-bge` 才允许无模。  
3. 升版 **`0.1.1` / `v0.1.1`** 发带 BGE 的 Release；**不覆盖** `v0.1.0`。  
4. **不做**强制把权重提交进 git；不改记忆算法本身。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] 调整 `pack_windows` / how-to 默认行为；bump `0.1.1`  
3. [x] 打带 BGE 包并发 `v0.1.1` Release  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做正式包默认带 BGE（最小）** | **已拍板 A（2026-09-01）** |
| 1 | 「默认带」落到哪 | **`pack_windows` 默认带**；`--skip-bge` 可跳过 | **已拍板 A（2026-09-01）** |
| 2 | 无网 / fetch 失败时 | **硬失败**；须 `--skip-bge` 才允许无模 | **已拍板 A（2026-09-01）** |
| 3 | 是否立刻重发 Release | **升版发 Release（带 BGE）**；不覆盖 `v0.1.0` | **已拍板 B（2026-09-01）** |
| 4 | 新版本号 | **`0.1.1` / `v0.1.1`** | **已拍板 A（2026-09-01）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-09-01）** |

## 不含

- 改向量算法 / 权重入库 / CI 自动发版  
