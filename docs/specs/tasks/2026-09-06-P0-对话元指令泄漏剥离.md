# 2026-09-06 · P0 · 对话元指令泄漏剥离

> Cursor 单线。承对话体检：2094 / 2074；复现：`doubao-seed-evolving` 意向卡问候约 1/5 偶发。  
> **状态**：**已编码放行并实现（2026-09-06）** · HITL #0–#1 齐。  
> **本刀范围**：**仅 A**——出口剥离元指令前缀。不含 B/C/D。

## Spec（外部行为）

1. 栖对用户可见的回复（落库 / 流式定稿）若以**元指令/导演备忘**起头，且后文仍有正常对白，则**剥掉该前缀**，只保留对人说的话。
2. 识别口径（启发式，宜窄）：`意图：` / `回应…：` / `依据…` / `不添新事实` 等；不误剥无备忘词的括号舞台指示。
3. 定稿路径剥离（expression 交付前）；流式中途 delta 仍可能闪一下，done 定稿为净文。
4. **不含**：改默认模型、改意向卡、开 TTS、改口型。

## 实现（步骤）

1. [x] `qi/core/speech_sanitize.py` · `strip_meta_preamble`
2. [x] `expression.py` 交付前 `_clean_speech`
3. [x] `tests/test_speech_sanitize.py`
4. [x] 回写本包

## 验收（判据）

- [x] 「意图：…\\n\\n晚上好…」→ 只剩对白
- [x] 「回应问候：…」→ 只剩对白
- [x] 「依据…不补画面…」→ 只剩对白
- [x] 正常问候 / 括号舞台指示不误伤
- [x] `tests/test_speech_sanitize.py` + `test_expression.py` 绿
- [ ] 感受：维护者偶发再遇时无备忘前缀（勾）

## HITL 批点

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 修法 | **A**：出口剥离 | **已拍板 A（2026-09-06）** |
| 1 | 编码放行 | **A** | **已拍板 A（2026-09-06）** |
