# 2026-08-30 · P2：设置页（最小钥匙）

> Cursor 单线。HITL 真源：审计简报 §〇.14；形态边界承 §〇.1 #4/#6。  
> **编码放行**：**已放行 A（2026-08-30）**。

---

## Spec（外部行为）

1. **应用内设置页（最小钥匙）**  
   - 可粘贴 API key；可选 base_url / 模型名  
   - **写入本地用户密钥文件**（gitignore；不写进 settings.yaml 明文）  
   - **保存后热重载** LLM 配置（尽量立刻可用；失败则系统态说明）  
   - **入口**：左侧 nav **底部齿轮**；打开后为**整页替换主区**（不进左栏 tab；可返回）；缺 key 系统态可一键打开（接已有 `open_settings`）  
   - 不要求会找 `.env`  
2. **不含**：数据目录迁移、导出/删除记忆、VRM 更换、盘符白名单、首启引导整页、免费云脑、系统凭据库、强制重启才能生效。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] 本地密钥文件读写 + 重载  
3. [x] 设置页 UI + 入口 + open_settings  
4. [x] 单测 + 相关 pytest  

## 验收（判据）

- [x] 可在应用内保存 key，无需手改 `.env`  
- [x] key 不落 settings.yaml 明文  
- [x] 有常驻入口；缺 key 提示可进设置  
- [x] 本刀未误做导出/VRM/盘符等  
- [x] 相关测绿；感受待维护者  

## HITL 批点

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀范围 | **最小钥匙页**（key + 可选 base_url/模型）；其余设置另刀 | **已拍板 A（2026-08-30）** |
| 2 | 钥匙落盘 | **本地用户密钥文件**（gitignore）；不写 settings.yaml 明文 | **已拍板 A（2026-08-30）** |
| 3 | 入口位置 | **左侧 nav 底部齿轮**；`open_settings` 直达 | **已拍板 B（2026-08-30）** |
| 4 | 保存后生效 | **热重载 LLM 配置**；失败系统态说明 | **已拍板 A（2026-08-30）** |
| 5 | 打开形态 | **整页替换主区**（不进左栏 tab；齿轮打开/可返回） | **已拍板 B（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-08-30）** |
