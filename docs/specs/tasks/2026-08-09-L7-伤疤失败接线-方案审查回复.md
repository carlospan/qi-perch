# L7 · 伤疤失败接线——方案审查回复

> **角色**：Trae（方案 Agent · 方案审查回复）  
> **依据**：[Cursor 交叉审查](./2026-08-09-L7-伤疤失败接线-Cursor交叉审查.md)  
> **时刻**：2026-08-09

---

## 总判

**路 B 维持；B1/B2 已关闭，明示可编码。** Cursor 两处都对：B1 是我 PR 读 `result["outcome"]` 与现网脱节（return dict 没此字段）；B2 是 trust 来源改 brain 传参（ActionLayer 不持有 relationship）。

**HITL 追认**：维护者追认 a/a/b（骨架触发源 / severity 0.3+0.7 / brain 传 trust）。

---

## B1 关闭：action result dict 补 outcome 字段

**核实**：share/tend/explore 的 return dict 现状——
- [share.py L109-120](file:///d:/qi-perch/qi/action/share.py#L109)：`type/creation_id/creation_type/content/emotion_context/qi_line/action_id/season`，**无 outcome**
- [explore.py L373-386](file:///d:/qi-perch/qi/action/explore.py#L373)：`type/found/summary/action_id/season/curiosity/source/sandbox`，**无 outcome**
- outcome 只写进 `insert_action(summary, ..., outcome=OUTCOME_SUCCESS, ...)`，**不回传 result dict**

按 PR 原写法 `result.get("outcome")` 永远 None → `_maybe_save_scar` 空转。

**裁定**：按 Cursor 建议——**可成疤行动必须在 result dict 带 outcome 字段**。

**修订回写**（PR 方案新增 §0 改 action return dict）：

### §0 action return dict 补 outcome（B1 关闭前置）

| 文件 | 改动 |
|------|------|
| `qi/action/share.py` L109-120 | return dict 加 `"outcome": OUTCOME_SUCCESS` |
| `qi/action/tend.py`（return dict）| 同上 |
| `qi/action/explore.py` L373-386 | return dict 加 `"outcome": outcome`（explore 有 outcome 变量，success/failed_capability 都补）|
| `qi/action/self_ops.py`（return dict ×3）| 同上 |

> 注：现状 outcome 都写死 SUCCESS / failed_capability；本包只补回传字段，不产出 failed_judgment/overstepped（触发源留 assist）。explore 已有 `outcome` 变量，直接补 `"outcome": outcome`。

### §1 layer `_maybe_save_scar` 读 result.get("outcome")

原 PR 不变，但读法现在有效（result dict 已带 outcome）。

B1 已关闭。

---

## B2 关闭：trust 来源改 brain 传参

**核实**：ActionLayer `__init__` 现状不注入 relationship（[layer.py ActionLayer.__init__](file:///d:/qi-perch/qi/action/layer.py)）。我 PR 写的 `self._relationship` 不存在。

**裁定**：按 HITL3 b——**brain 调 tick/execute_kind 时多传 `trust` 参**。

**修订回写**（PR 方案 §2 layer + §3 brain）：

### §2 `layer.py`

**`tick` + `execute_kind` 签名加 `trust: float = 0.5`**：

```python
async def tick(
    self, emotion, relationship_stage, season, now, *,
    mode, user_online=True, scars=None, sensing=None,
    pressure=None,
    trust: float = 0.5,  # B2 新增
) -> dict | None:
    ...
```

**`_maybe_save_scar` 改用 trust 参数**（不挂 relationship）：

```python
async def _maybe_save_scar(
    self, result: dict, kind: str, now: datetime, *,
    trust: float = 0.5,
) -> None:
    """Step 5 收尾：失败行动形成伤疤。"""
    outcome = result.get("outcome")
    if not outcome_creates_scar(outcome):
        return
    severity = 0.7 if outcome == OUTCOME_OVERSTEPPED else 0.3
    origin = f"[action:{kind}] {(result.get('summary') or '')[:80]}"
    try:
        scar_id = await self.db.save_scar(
            origin_event=origin,
            severity=severity,
            trust_before=float(trust or 0.5),
        )
        result["created_scar"] = scar_id
        logger.info(
            "行动形成伤疤 kind=%s outcome=%s scar_id=%s",
            kind, outcome, scar_id,
        )
    except Exception:
        logger.debug("save_scar 失败", exc_info=True)
```

**`tick` / `execute_kind` 调 `_maybe_save_scar` 时传 trust**：

```python
# tick（L262 前）
if result is not None:
    await self._maybe_save_scar(result, chosen.kind, now, trust=trust)
self.last_result = result

# execute_kind（L374 前）
if result is not None:
    await self._maybe_save_scar(result, kind, now, trust=trust)
self.last_result = result
```

### §3 `brain.py` 调 tick/execute_kind 传 trust

[brain.py L856 tick 调用](file:///d:/qi-perch/qi/core/brain.py#L856) + [L948 execute_kind 调用](file:///d:/qi-perch/qi/core/brain.py#L948) 加 `trust=self.relationship.state.trust`：

```python
# L856 tick
action_result = await self.action.tick(
    ...,
    pressure=self.last_pressure_response,
    trust=float(getattr(self.relationship.state, "trust", 0.5) or 0.5),  # B2 新增
)

# L948 execute_kind
action_result = await self.action.execute_kind(
    kind=kind,
    ...,
    pressure=self.last_pressure_response,
    trust=float(getattr(self.relationship.state, "trust", 0.5) or 0.5),  # B2 新增
)
```

> 注：`self.relationship` 是 brain 持有的 RelationshipEngine 引用，`.state.trust` 读取；失败降级 0.5。

B2 已关闭。

---

## 核对清单（修订后）

| 项 | 修订 |
|----|------|
| **§0** action return dict 补 outcome（share/tend/explore/self_ops）| **新增** B1 关闭前置 |
| **§1** `_maybe_save_scar` 读 result.get("outcome") | ✓ 原 PR（读法现有效）|
| **§2** layer tick/execute_kind 加 `trust` 参 + `_maybe_save_scar` 用 trust | **修订** B2 关闭 |
| **§3** brain 调 tick/execute_kind 传 `trust` | **新增** B2 关闭 |
| severity 0.3/0.7 | ✓ HITL a |
| origin 含 `[action:{kind}]` | ✓ |
| 触发源 A（骨架，不产出 failed_judgment/overstepped）| ✓ HITL a |
| 测试构造 outcome 验证接线 | ✓ |

---

## 纪律红线

- R1 不破 ✓
- R2（行动克制）：伤疤只阻断，不增 ✓
- LLM 走 gateway（不碰）✓
- 不引入 agent framework ✓
- 一次一包 ✓
- permission.py 规则不改（已收口）✓
- save_scar 签名不改（已够用）✓

---

## 阻塞项

**无（Bx = 0）。** B1/B2 已关闭。

---

## 非阻塞备注

- R1（原 R1）ActionLayer 不持有 relationship：已按 B2 改 brain 传 trust，不挂 relationship
- R2 现状无触发源：接受；骨架通了，assist 落地时自然产出
- R3 trust 读取失败：降级 0.5（getattr + or 0.5）
- R4 相处复验难：接受；验收用测试构造 outcome 验证接线

---

## HITL

维护者已追认 a/a/b（骨架触发源 / severity 0.3+0.7 / brain 传 trust）。无待拍。

---

## 下一拍交 Cursor

按修订后 PR 落地：
1. **§0** action return dict 补 outcome（share/tend/explore/self_ops 四文件）
2. **§1** `_maybe_save_scar` 方法（读 result.get("outcome")，origin 含 [action:{kind}]，severity 0.3/0.7）
3. **§2** layer tick/execute_kind 签名加 `trust` + 调 `_maybe_save_scar` 传 trust
4. **§3** brain 调 tick/execute_kind 传 `trust=self.relationship.state.trust`（降级 0.5）
5. 测试 `test_action_scar_wiring.py`：failed_judgment/overstepped → save_scar + success/failed_capability 不调 + scar_blocks_kind 生效 + severity 分级
6. 全量 ≥515 passed
7. 回执落盘；交 Trae 验收

---

*方案审查回复 · 2026-08-09 · 路 B 维持 · B1/B2 已关闭 · HITL 追认 a/a/b · 明示可编码*
