# L7 · N3 动机接线——验收记录

> **角色**：Trae（方案 Agent · 实施验收）  
> **依据**：[方案审查回复](./2026-08-09-L7-N3动机接线-方案审查回复.md)（B1/B2 已关，明示可编码）+ 修订后 [PR 方案](./2026-08-09-L7-N3动机接线-PR方案.md)  
> **编码回执**：[Cursor 编码回执](./2026-08-09-L7-N3动机接线-Cursor编码回执.md)  
> **时刻**：2026-08-09

---

## 总判

**验收通过。** 代码与修订后 PR 逐项对齐；B1（force 路径压力软门）+ B2（collect_contenders 接线）两处修订均已落地；515 passed（独立 `--basetemp` 系统临时区，exit 0）；scope 未越界。

---

## scope 核对（git diff）

| 文件 | 状态 | 核对 |
|------|------|------|
| `qi/core/brain.py` | M | ✓ init `last_pressure_response=None` + heartbeat 存 + tick/execute_kind 传 |
| `qi/action/layer.py` | M | ✓ `tick` + `execute_kind` 两处签名加 `pressure`，传 drift/action_intentions |
| `qi/action/explore.py` | M | ✓ 常量 + drift 签名 + force 路径压力软门（B1）|
| `qi/action/volition.py` | M | ✓ action_intentions 签名加 pressure + explore priority 软调制 |
| `qi/core/trace.py` | M | ✓ collect_contenders 读 brain.last_pressure_response 传 action_intentions（B2）|
| `tests/test_explore_pressure_modulation.py` | A | ✓ 9 测含 B1 force 软门拦/放 |

**diff stat**：5 改 + 1 新（+51/-3）。**未碰** pressure.py / brain_delivery / 前端 / settings / digest / 门控 ✓

---

## 代码核对（逐项对齐修订后 PR）

### 1. `brain.py`

- L117 `__init__`：`self.last_pressure_response = None` ✓
- L615 heartbeat：`self.last_pressure_response = resp`（resp 算出后立即存）✓
- L856 `_heartbeat_legacy_idle` 调 tick：`pressure=self.last_pressure_response` ✓
- L948 `_heartbeat_gws` 调 execute_kind：`pressure=self.last_pressure_response` ✓

### 2. `layer.py`

- `tick`（L172-273）：签名加 `pressure: Any | None = None` + 传 action_intentions + 传 drift（force=False 路径）✓
- `execute_kind`（L274-340）：签名加 `pressure` + 传 drift（force=True 路径）✓

### 3. `explore.py`（B1 关闭）

- 常量 `PRESSURE_THROTTLE_K = 0.5` / `PRESSURE_REST_K = 0.6` ✓ HITL a
- `drift` 签名加 `pressure: Any | None = None` ✓
- curiosity 硬阈改 `if curiosity < 0.65 and not force:`（force 跳过硬阈）✓
- force=False 路径：`p *= (1 - 0.5*throttle) * (1 - 0.6*rest)` 软调制 ✓
- **B1 force=True 路径单独软门**：`p_force = (1 - 0.5*throttle) * (1 - 0.6*rest)`，throttle=rest=1 时下限 `0.5 * 0.4 = 0.2`（不归零，保 contemplative drift 本质）✓
- pressure=None 时 force 路径不拦（原 GWS 行为不回归）✓

### 4. `volition.py`

- `action_intentions` 签名加 `pressure: object | None = None` ✓
- explore 候选 priority：`pri *= (1 - 0.3 * throttle)` ✓ HITL a
- 不调 rest（仅 throttle 弱调制候选 priority）✓

### 5. `trace.py`（B2 关闭）

- `collect_contenders` L421：`pressure = getattr(brain, "last_pressure_response", None)` ✓
- L440 传 `action_intentions(..., pressure=pressure)` ✓ B2 关闭

### 6. 测试 `test_explore_pressure_modulation.py`（9 测）

| 测 | 核对 |
|----|------|
| `test_drift_pressure_none_unchanged` | pressure=None force=False 通过 ✓ |
| `test_drift_throttle_reduces_probability` | throttle=1 p*=0.5，random=0.6 拦 ✓ |
| `test_drift_rest_reduces_probability` | rest=1 p*=0.4，random=0.5 拦 ✓ |
| `test_drift_curiosity_threshold_unchanged` | curiosity<0.65 force=None 仍硬阈 ✓ |
| `test_drift_force_pressure_soft_gate_blocks` | force=True p_force=0.2 random=0.21 拦 ✓ B1 |
| `test_drift_force_pressure_soft_gate_allows` | force=True p_force=0.2 random=0.19 放行 ✓ B1 |
| `test_drift_force_pressure_none_skips_soft_gate` | force=True pressure=None 不拦 ✓ |
| `test_volition_explore_priority_throttle` | priority *= (1-0.3*1) ✓ |
| `test_brain_last_pressure_response_updated` | heartbeat 后 brain.last_pressure_response 非 None ✓ |

使用 `random.random` patch 做确定性断言（避免统计抖动）——合理。9 测全过 ✓

---

## 构建（独立复核）

| 命令 | 结果 |
|------|------|
| `pytest -q --basetemp=%TEMP%\qi_test_tmp`（系统临时区）| **515 passed**（exit 0，76.79s）|

---

## 纪律红线

- R1 不破 ✓
- R2（行动克制）：压力只降概率/优先级，不增 ✓
- LLM 走 gateway（不碰 LLM）✓
- 不引入 agent framework（pressure 阶段四既有）✓
- 一次一包（N3 单独）✓
- pressure 计算逻辑不改（阶段四已收口，只读 `last_pressure_response`）✓
- 软调制非硬门控（force 路径下限 0.2，不归零）✓

---

## 偏离确认

无实质偏离。B1 按审查回复 force 路径单独软门落地；B2 按审查回复 collect_contenders 接线；HITL a/a/a 按追认值落地（0.5/0.6 drift + 0.3 volition + 走常量）；测试用 random patch 做确定性断言合理。

---

## 剩余（交维护者）

1. **相处复验**：观察 throttle/rest 高（穷/累/不安）时 explore 是否变少（软调制，不归零）。N3 效果偏长期观察，难单次验证。
2. **可选文档回写**：`L7-action.md` 演进指向补 N3 已落地；`progress.md` 补决策行。
3. **提交推送**：本次改动（5 代码 + 1 测试 + 6 文档，含润色小刀验收记录未提交）

---

## 收口状态

- **N3 动机接线工程交付**：✅ 完成
- **L7 三/四整体进度**：N3 ✅ / 伤疤接线 ⏳ / assist ⏳ / irreversible ⏳

---

*验收记录 · 2026-08-09 · 通过 · 515 passed · B1/B2 已落地 · HITL a/a/a 追认*
