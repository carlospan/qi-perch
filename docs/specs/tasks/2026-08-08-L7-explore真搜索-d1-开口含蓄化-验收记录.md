# L7 explore 真搜索 d-1 · 开口含蓄化——验收记录

> **角色**：方案 Agent（Trae）实施验收
> **依据**：[Cursor 编码回执](./2026-08-08-L7-explore真搜索-d1-开口含蓄化-Cursor编码回执.md) + [方案审查回复](./2026-08-08-L7-explore真搜索-d1-开口含蓄化-方案审查回复.md) + [PR 方案](./2026-08-08-L7-explore真搜索-d1-开口含蓄化-PR方案.md)
> **方式**：`git diff HEAD` 逐文件核对 + Read 确认常量值

---

## 验收结论

**通过。** 开口含蓄化落地未偏离；L26 TEMP 收回；额外 3 处验证期 TEMP 一并收回（透明说明，恢复设计原值）；487 passed。

---

## 一、scope 核对

### 审查回复范围内（全过）

| 项 | 核对 | 状态 |
|----|------|------|
| `explore.py` L26 `EXTERNAL_CURIOSITY_MIN` | Read 实际值 = `0.8`（原设计）；`git diff HEAD` 无 L26 改动（= HEAD，TEMP 已清） | ✓ |
| `explore.py` L290-296 drift 拆 qi_line | 成功 `f"我刚才看了看 {found['query']}。"`、空手 `qi_line = summary`；与 PR 一致 | ✓ |
| `test_external_when_gates_pass` 断言 | `qi_line == f"我刚才看了看 {query}。"` + N3 显式 `"窗边的鸟" not in qi_line` + `in summary` | ✓ |
| 空手测 `test_external_empty_failed_capability_still_speaks` | 未改 | ✓ |
| 未碰 `_fetch_external`/`_make_query`/`brain_delivery`/`settings.yaml` | `git diff` 仅 drift + test | ✓ |

### 额外 TEMP 收尾（偏离，已透明说明）

回执偏离段透明说明：验证期另有 volition/layer/rhythm TEMP（会导致全量测红），同次收回恢复设计原值。

| 文件 | 收回 | `git diff HEAD` |
|------|------|----------------|
| `qi/action/volition.py` | 取消压 share；explore 阈/pri 恢复 `≥0.65` 与原公式 | **空**（= HEAD 原值） |
| `qi/action/layer.py` | 去掉 `explore.drift(..., force=True)` | **空**（= HEAD 原值） |
| `qi/core/rhythm.py` | `AMBIENT_TO_SOLITARY_MINUTES` `1` → `30` | **空**（= HEAD 原值） |

三处 `git diff HEAD` 均空 = working tree 已回到 HEAD（5531eeb）原值，无残留 TEMP、无夹带新行为。属 TEMP 清理，合理同 commit。

---

## 二、B3 仍守 ✓

- qi_line 模板化，不二次 LLM ✓
- 不改 `brain_delivery`（走现有 `elif speak and qi_line`）✓
- 内部沙箱分支不设 speak/qi_line ✓
- 外部成功/空手均 `speak=True` ✓

---

## 三、测试

- `test_external_when_gates_pass`：成功 qi_line 不含 title、summary 含 title（N3 显式断言落实）✓
- 空手测不动 ✓
- 现有内部/门控/冷却/web=None 不回归 ✓
- 定向 48 passed；**全量 487 passed**（≥487，零红）✓

---

## 四、偏离评估

Cursor 主动收回 volition/layer/rhythm 三处验证期 TEMP——超出审查回复 scope，但：
1. 透明说明在回执偏离段（未隐瞒）
2. `git diff HEAD` 证实三处均回 HEAD 原值（无残留、无新行为）
3. 不收回会导致全量测红（share 被压/explore pri=1/solitary 1 分钟），验收无法过
4. 属 TEMP 清理恢复原值，非新行为变更

**可接受。** 后续若再有验证期 TEMP，建议验证完立即收回或 stash，避免跨包遗留。

---

## 五、交接

- **维护者（相处复验）**：本机启动栖，独处攒 curiosity≥0.8、过 6h 冷却触发外部，听成功开口应为「我刚才看了看 {query}。」（**不含 search title**，像走神）；空手仍「我看了看外面，没查到什么。不假装看见了。」
- **维护者（settings TEMP 改回）**：[settings.yaml](file:///d:/qi-perch/qi/config/settings.yaml) `cooldown_hours` → `6`、`probability` → `0.05`（代码侧 L26/L27/L28 已由 Cursor 改回 0.8/0.05/6.0，不用动）
- **提交推送**：验收通过，可让 Cursor commit/push（explore.py drift + test 两文件改动）

**状态：实施验收通过。待维护者相处复验 + settings TEMP 改回 + 提交推送。**
