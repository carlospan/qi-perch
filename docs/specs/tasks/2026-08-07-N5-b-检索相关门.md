# N5-b 检索相关门——任务包

> **协作分工（固定流程，详见 `specs/SDD-GUIDE.md` 第二节）：本任务包由方案 Agent（CodeBuddy）起草（出方案），Cursor 按 PR 方案落地编码；方案 Agent 不写 `qi/` 实现代码，Cursor 不重新设计架构（执行编码者固定为 Cursor）。双方过程稿均落盘为文件，Cursor 自行读取目录，无需人工转发。**

---

## Spec（外部行为）

向量语义检索召回了"相关但不相关"的记忆——语义上相近，但对当前对话话题完全不对题。这些记忆被注入 prompt 后导致栖的回答串台（#1367：用户在聊菜名，栖突然跳到"重置原因""灵魂寄存"）。

本任务在检索与注入之间加一道**话题相关性闸**：检索到的记忆需与当前消息的话题有足够重叠才允许注入；不同话题或重叠不足的记忆被过滤或降级（进 prompt 但标注 low_confidence）。

> 本栏由 CodeBuddy 写（方案侧）。

## 实现（步骤）

见配套 PR 方案文档（`docs/specs/archive/2026-08-07-n5-retrieval-gate/包-N5-b-检索相关门-PR方案.md`）。

## 验收（判据）

- [x] 构造 "菜名" 对话 + 检索到 "重置/珍惜" 记忆 → 记忆被过滤（单测）
- [x] 构造 "睡眠/助眠" 对话 + 检索到 "助眠/躺着" 记忆 → 记忆正常注入（单测）
- [x] 现有检索测试不变（全量 pytest）
- [x] 全量 `pytest` 通过，`ruff` 零问题（编码回执：457 passed）

> 方案 Agent 终验前，上列由 Cursor 自测勾选；正式验收以 `archive/.../验收记录.md` 为准。

## HITL 批点

- **无**。检索相关门是 N5 硬闸扩展中已声明的债务项（B1-B），不改变架构方向、不触 R1-R5、不开新阶段。按 SDD-GUIDE §6.1 决策路由表——**可自治（Agent 直接定）**。
