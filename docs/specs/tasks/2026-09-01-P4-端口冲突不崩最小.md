# 2026-09-01 · P4：端口冲突不崩（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.31；承审计 §五-#1。  
> **编码放行**：**已放行 A（2026-09-01）**。

---

## Spec（外部行为）

1. 具身通道绑定端口（默认 9527）若已被占用：进程**不**以难懂堆栈作为主反馈。  
2. 给出固定白话：含端口号 +「请先退出旧的栖（托盘→退出栖），再重开。」  
3. 写入日志 ERROR，控制台可见；进程以**非 0**退出。  
4. **不做**：自动换端口、改壳探端口、端口设置 UI。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] `EmbodimentServer.start` 捕获占用 → `PortInUseError`；cli 打印并 exit 1  
3. [x] 单测 + 针对性 pytest  

## 验收（判据）

- [x] 占用时抛 `PortInUseError` 带固定白话（单测）  
- [x] cli 捕获后 `SystemExit(1)`  
- [x] 相关 pytest 绿（`tests/test_embodiment.py` 15 passed）  
- [ ] （感受）维护者：故意留旧脑再开，能看懂提示  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P4 端口冲突不崩（最小）** | **已拍板 A（2026-09-01）** |
| 1 | 本刀范围 | **只可见退出**；不换端口/改壳/UI | **已拍板 A（2026-09-01）** |
| 2 | 提示文案与落点 | 固定白话带端口 + 托盘退出；日志+控制台；非 0 | **已拍板 A（2026-09-01）** |
| 3 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-09-01）** |

## 不含

- 自动换端口 / 壳改探 / 端口 UI  
