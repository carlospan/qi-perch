# L7 explore 真搜索 d-3-2（沙箱深读）——验收记录

> **角色**：Trae（方案 Agent · 实施验收）  
> **依据**：[方案审查回复](./2026-08-08-L7-explore真搜索-d3-2沙箱深读-方案审查回复.md)（B1 已关，明示可编码）+ 修订后 [PR 方案](./2026-08-08-L7-explore真搜索-d3-2沙箱深读-PR方案.md)  
> **编码回执**：[Cursor 编码回执](./2026-08-08-L7-explore真搜索-d3-2沙箱深读-Cursor编码回执.md)  
> **时刻**：2026-08-08

---

## 总判

**验收通过。** 代码与修订后 PR 逐项对齐；B1 八处测试迁移落地；死码已删；500 passed（独立 `--basetemp` 复跑 exit 0）；vue-tsc + build 通过；scope 未越界。**C 方案整体收口**（d-1/d-2/d-3-1/d-3-2 工程交付全过）。剩余 = 维护者相处复验 + 文档回写 + 提交推送。

---

## scope 核对（git diff）

| 文件 | 状态 | 核对 |
|------|------|------|
| `qi/action/explore.py` | M | ✓ 新增 `INTERNAL_SOURCE_LIMIT`/`_clip_entry`/`_digest_internal`/`_read_internal`；drift 内部 speak+journal；删死码 |
| `qi/embodiment/desktop/src/composables/useQi.ts` | M | ✓ 门控扩 `web\|\|journal && entries.length>0` |
| `tests/test_action_l7.py` | M | ✓ `test_explore_never_fabricates_found` 迁移 |
| `tests/test_explore_external_branch.py` | M | ✓ 5 处内部路径用例迁移（source→journal、speak True、删 sandbox setup）|
| `tests/test_self_ops.py` | M | ✓ 2 测改写重命名（reads_journal_narratives / no_narratives_found_none）|
| `tests/test_explore_internal_digest.py` | 新建 | ✓ _digest_internal/_read_internal/drift 内部 成功/降级/无源 |

**diff stat**：5 改 + 1 新，+119/-175（净 -56，死码删多于新增）。**未碰** brain_delivery / layer / brain / settings / ExploreCard.vue / types.ts ✓

---

## 代码核对（逐项对齐 PR）

### 后端 `explore.py`

1. **新增常量**：`INTERNAL_SOURCE_LIMIT = 3` ✓
2. **`_clip_entry(s, limit=40)`**：截断叙事正文 ≤40 + "…"，见闻卡 title 用 ✓
3. **`_digest_internal(narratives)`**：
   - 无 llm → "我翻了翻自己记得的事。" ✓
   - system prompt 含 `_QUERY_PRIVACY_LINE`（N3 吸收）✓
   - `purpose="consciousness"` ✓
   - except → 降级 ✓；空 digest → 降级 ✓
4. **`_read_internal()`**：
   - `list_recent_narratives(limit=3)` ✓
   - 无源 → `(None, "我翻了翻自己这边，没有记得的事。", OUTCOME_SUCCESS)`（含「没有」✓）
   - 有源 → `found={entries:[{title:_clip_entry(content)}], source:"journal"}` + digest ✓
5. **drift 内部分支**：`_read_internal()` + `speak=True` + `qi_line=summary` + `source="journal"` ✓
6. **`source` 初值** `"journal"`（偏离2：避免短暂误标 sandbox；外部仍覆盖 "web"）✓ 合理
7. **删死码**：`_scan_finding` / `_scan_sandbox` / `_config_key_names` + `_SKIP_DIR_NAMES`/`_MAX_ENTRIES`/`_MAX_DEPTH`/`_SECRET_KEY_FRAGMENTS` ✓
8. **保留**：`resolve_sandbox_root` / `root` / `result["sandbox"]`（metadata，shape 不变）✓

### 前端 `useQi.ts`

- 门控：`okSource = source === "web" || source === "journal"` + `entries.length > 0` ✓
- ExploreCard.vue / types.ts 未改（内部 entries title=截断 content、query 空→不显、角标「看」同）✓

### B1 测试迁移（8 处）

| 用例 | 迁移 |
|------|------|
| `test_curiosity_below_08_stays_internal` | source→journal、speak True、删 sandbox ✓ |
| `test_cooldown_blocks_external` | source→journal、speak True ✓ |
| `test_web_none_or_llm_none_internal` | 两例 source→journal + speak True ✓ |
| `test_internal_success_outcome_unchanged` | speak True、outcome SUCCESS、删 sandbox ✓ |
| probability/random→内部用例 | source→journal、speak True ✓ |
| `test_explore_never_fabricates_found` | 删 qi.db、source→journal、speak True ✓ |
| `test_explore_reads_sandbox_entries` → `test_explore_reads_journal_narratives` | seed narrative + journal entries ✓（偏离1：重命名，PR 授权「改写或并入」）|
| `test_explore_empty_sandbox_found_none` → `test_explore_no_narratives_found_none` | fresh db 无 narratives → found None ✓ |

### 新增 `test_explore_internal_digest.py`

_digest_internal 成功/降级/失败；_read_internal 有源/无源；drift 内部 force（有/无 llm）✓

---

## 构建（独立复核）

| 命令 | 结果 |
|------|------|
| `pytest -q --basetemp=…`（独立，规避 Win tmpdir PermissionError） | **500 passed**（exit 0，51.27s）|
| `npx vue-tsc --noEmit`（desktop） | **通过**（exit 0）|
| `npm run build`（desktop） | **通过**（✓ built 10.46s）|

> 注：默认 tmpdir 跑出 WinError 5 清理错误（d-2 同已知问题），换 `--basetemp` 干净复跑；测试本身全绿。

---

## 纪律红线

- R1 不破（相处验证维护者做）✓
- LLM 走 gateway `purpose=consciousness`（同 d-2；不污染 last_outcome）✓
- 不碰 brain_delivery / 外部门控 / `_digest_hits` / d-3-1 ExploreCard.vue/types ✓
- 内部 speak 为 HITL2=a 授权行为变更 ✓
- 死码删干净（无双内部源并存）✓

---

## 偏离确认

| 项 | 处置 |
|----|------|
| 偏离1 · `test_self_ops` 两测改写并**重命名**（reads_journal_narratives / no_narratives_found_none）| PR 授权「改写或并入」择一；重命名更达意，非实质偏离 ✓ |
| 偏离2 · `source` 初值 `"sandbox"`→`"journal"` | 避免内部分支短暂误标；外部仍覆盖 "web"；合理，非偏离 ✓ |
| `_digest_internal` prompt 措辞 `不编造。红线：{_QUERY_PRIVACY_LINE}` vs PR `不编造、{_QUERY_PRIVACY_LINE}` | 含同串 `_QUERY_PRIVACY_LINE`，功能等价 ✓ |

无实质偏离。

---

## C 方案整体收口

| 阶段 | 工程交付 | 相处复验 |
|------|---------|---------|
| d-1 联网地基 | ✅（已收口）| ✅ 栖会主动检索 |
| d-2 内部深读（外部 hits 消化）| ✅ | ⏳ 待复验 |
| d-3-1 见闻卡片 | ✅ | ⏳ 待复验 |
| d-3-2 沙箱深读（内部记忆深读）| ✅ | ⏳ 待复验 |

**C 方案工程交付完成**。相处复验三项（d-2/d-3-1/d-3-2）合并做：独处攒 curiosity≥0.8 听外部开口+digest+见闻卡；curiosity 0.65-0.8 听内部翻记忆开口+见闻卡。

---

## 剩余（交维护者）

1. **相处复验**：外部探索（digest+见闻卡像「看懂外面」）；内部探索（翻记忆 digest+见闻卡像「看懂内在」）；观察内部开口是否过频（N5，嫌吵再加内部冷却）
2. **文档回写**（N5/验收后另拍）：`L7-action.md` 演进指向补 d-2/d-3-1/d-3-2 收口；`progress.md` 补 d-3-1/d-3-2 决策行；内部 explore 描述从「沙箱列目录」改为「深读记忆叙事 + speak + 见闻卡」
3. **提交推送** d-3-2（本次改动：explore.py + useQi.ts + 测试 + 文档）

---

## 收口状态

- **d-3-2 工程交付**：✅ 完成
- **C 方案工程交付**：✅ 完成（d-1/d-2/d-3-1/d-3-2 全过）
- **相处复验**：⏳ 待维护者
- **整体收口**：待相处复验 + 文档回写 + 提交推送后宣告

---

*验收记录 · 2026-08-08 · 通过 · 500 passed · vue-tsc + build 通过 · C 方案工程交付完成*
