# 2026-09-02 · P1：look 深路径二期 · 记忆检索（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.35；承 `2026-08-29-look-所见走心-情绪冲击.md`（open_loops 仍二期另包）。  
> **编码放行**：**已放行 A（2026-09-02）**。

---

## Spec（外部行为）

1. 成功 look 主观短说前，**检索相关记忆**并传入 `expression.express`（不再固定 `memories=[]`）。  
2. **对齐对话路径**：query = impression；邀瞥且有 `user_question` 时 query 拼两者 → `retrieve_for_prompt(top_k=3)` → 话题相关过滤；`recent_messages` 仍空。  
3. 检索失败或空列表 → 仍 express（不挡 look）。  
4. **本刀不做**：open_loops、fact_noticing、recent_messages 进 express。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] `brain_context.retrieve_filtered_memories` + `look_heart` 接线  
3. [x] 单测 + pytest  

## 验收（判据）

- [x] look express 可收到过滤后的 memories（单测）  
- [x] 无 memory / 检索空时行为与现有一致  
- [x] `tests/test_look_heart.py` 绿（7 passed）  
- [ ] （感受）维护者：瞥后短说能带上相关旧事  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **P1 look 深路径二期** | **已拍板 A（2026-09-02）** |
| 1 | 本刀范围 | **先只做记忆检索** | **已拍板 B（2026-09-02）** |
| 2 | 检索怎么接 | **对齐对话** top_k=3 + 话题过滤 | **已拍板 A（2026-09-02）** |
| 3 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-09-02）** |

## 不含

- open_loops / fact_noticing / recent_messages  
