# 2026-09-01 · P3：检查更新（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.32；承 §〇.1 #13、§〇.26、§〇.28–〇.29。  
> **编码放行**：**已放行 A（2026-09-01）**。

---

## Spec（外部行为）

1. 设置 → 关于区有 **「检查更新」**（手动，不后台轮询）。  
2. 请求 GitHub `releases/latest`，用 Latest **tag**（去 `v`）与本机壳版本比对（开发壳去掉「（开发）」后再比；可提示「当前是开发壳」）。  
3. 三态白话：已是最新 / 有新版（短提示 + 可打开 Releases 列表）/ 查失败。  
4. **不做**：下载安装包、静默/强制升级、自建更新站、CI。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] `about.ts` 检查逻辑；CSP 允许 `api.github.com`；关于区 UI  
3. [x] 单测（常量 + 版本比对口径）  

## 验收（判据）

- [x] 关于区「检查更新」+ 三态文案（工程）  
- [x] 有新版时「打开 Releases 下载」链  
- [x] 相关测试绿（`tests/test_about_page_constants.py`）  
- [ ] （感受）维护者手测  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 检查更新（最小）** | **已拍板 A（2026-09-01）** |
| 1 | 本刀范围 | 手动检查 + API 比对；不下载/轮询/强制 | **已拍板 A（2026-09-01）** |
| 2 | 比对与有新版动作 | Latest tag vs 壳版本；提示 + 开 Releases 列表 | **已拍板 A（2026-09-01）** |
| 3 | 开发壳 | 也可检查；可注明开发壳 | **已拍板 A（2026-09-01）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-09-01）** |

## 不含

- 自动下载安装 / 静默升级 / updater 插件 / CI  
