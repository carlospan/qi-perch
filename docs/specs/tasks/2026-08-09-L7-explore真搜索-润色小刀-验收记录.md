# L7 explore 真搜索 · 润色小刀——验收记录

> **角色**：Trae（方案 Agent · 实施验收）  
> **依据**：[方案审查回复](./2026-08-09-L7-explore真搜索-润色小刀-方案审查回复.md)（B1/B2 已关，明示可编码）+ 修订后 [PR 方案](./2026-08-09-L7-explore真搜索-润色小刀-PR方案.md)  
> **编码回执**：[Cursor 编码回执](./2026-08-09-L7-explore真搜索-润色小刀-Cursor编码回执.md)  
> **时刻**：2026-08-09

---

## 总判

**验收通过。** 代码与修订后 PR 逐项对齐；B1（config 扁平读法）+ B2（ExploreCard 卡脚映射）两处修订均已落地；506 passed（独立 `--basetemp` 系统临时区，exit 0）；vue-tsc + build 通过；scope 未越界。

---

## scope 核对（git diff）

| 文件 | 状态 | 核对 |
|------|------|------|
| `qi/action/explore.py` | M | ✓ `_SEASON_ZH` 常量 + `_make_query` 季节段中文 +「纯中文」软提示 |
| `qi/action/explore_web.py` | M | ✓ `_exclude_domains` 扁平读 + payload 条件加字段 |
| `qi/config/settings.example.yaml` | M | ✓ `exclude_domains: []` + 注释 |
| `qi/embodiment/desktop/src/components/ExploreCard.vue` | M | ✓ `SEASON_ZH` 映射 + `seasonLabel` 改映射（B2） |
| `tests/test_explore_external_branch.py` | M | ✓ 2 测：季节中文 + 未知码兜底 |
| `tests/test_explore_web.py` | M | ✓ 2 测：exclude_domains 空/非空（扁平 config） |

**diff stat**：6 改，+109/-3。**未碰** season.py / digest / 门控 / drift / brain_delivery / layer / brain ✓

---

## 代码核对（逐项对齐修订后 PR）

### 1. `explore.py`（B1 不涉及，原 PR 落地）

- `_SEASON_ZH = {"spring":"春","summer":"夏","autumn":"秋","winter":"冬"}` 模块级常量 ✓
- `_make_query`：`season_zh = _SEASON_ZH.get(season, season)` + user prompt `f"季节={season_zh}；..."` + 末尾「（≤30字，纯中文）」✓
- 未知码兜底原样（`_SEASON_ZH.get(season, season)`）✓

### 2. `explore_web.py`（B1 修订）

- `_exclude_domains(self)`：`raw = self.config.get("exclude_domains") or []`（**扁平读法，对齐 layer.py L90 传 `ext` 子段**）✓ B1 关闭
- 非空才入 payload：`if exclude: payload["exclude_domains"] = exclude` ✓
- 小写化 + strip：`[str(d).strip().lower() for d in raw if str(d).strip()]` ✓

### 3. `settings.example.yaml`

```yaml
    # 可选域名过滤（默认空=不改变 Tavily 现状；嫌噪声可加如 music.apple.com / mojim.com）
    exclude_domains: []
```
✓

### 4. `ExploreCard.vue`（B2 修订）

- `SEASON_ZH` 前端常量（与后端 `_SEASON_ZH` 同构）✓
- `seasonLabel` 改映射：`s ? SEASON_ZH[s] || s : ""`（未知码兜底原样）✓ B2 关闭
- `<footer>{{ seasonLabel }}</footer>` 模板不变（computed 内部改）✓

### 5. 测试

- `test_make_query_uses_chinese_season`：season=autumn → prompt 含「秋」不含「autumn」+「纯中文」+ purpose=consciousness ✓
- `test_make_query_unknown_season_fallback`：season="unknown" → prompt 含「unknown」（兜底不崩）✓
- `test_tavily_payload_exclude_domains_empty`：config={} → payload 不含 `exclude_domains` ✓
- `test_tavily_payload_exclude_domains_configured`：**扁平 config** `{"exclude_domains":["Music.Apple.com"," mojim.com "]}` → payload 含小写化 `["music.apple.com","mojim.com"]` ✓ B1 测试对齐

---

## 构建（独立复核）

| 命令 | 结果 |
|------|------|
| `pytest -q --basetemp=%TEMP%\qi_test_tmp`（系统临时区，规避 Win tmpdir PermissionError）| **506 passed**（exit 0，74.23s）|
| `npx vue-tsc --noEmit`（desktop） | **通过**（exit 0）|
| `npm run build`（desktop） | **通过**（✓ built 9.12s）|

---

## 纪律红线

- R1 不破 ✓
- LLM 走 gateway `purpose=consciousness`（`_make_query` 不变）✓
- 不碰 season.py 全局季节码表示 ✓
- 不碰 digest / 门控 / drift / brain_delivery / layer / brain ✓
- 一次一包（N6/N7/N8 + B2 同属季节中文/搜索质量润色）✓
- 前端仅 ExploreCard.vue 一处映射 ✓

---

## 偏离确认

无实质偏离。B1 按审查回复扁平读法落地；B2 按裁定 (b) 前端卡脚映射落地；测试 config 形对齐扁平结构。

---

## 剩余（交维护者）

1. **相处复验**：外部探索时看 query 是否仍出英文季节码；见闻卡脚是否显示「秋」而非「autumn」
2. **可选配置**：若 Tavily 噪声仍扰，本机 `settings.yaml` 填 `exclude_domains: [music.apple.com, mojim.com]`（勿写进 example 默认非空）
3. **提交推送**：本次改动（4 代码 + 2 测试 + 5 文档）

---

## 收口状态

- **润色小刀工程交付**：✅ 完成
- **C 方案 + 润色小刀整体**：✅ 工程交付完成
- **相处复验**：⏳ 待维护者（看 query/卡脚英文码是否消除）

---

*验收记录 · 2026-08-09 · 通过 · 506 passed · vue-tsc + build 通过*
