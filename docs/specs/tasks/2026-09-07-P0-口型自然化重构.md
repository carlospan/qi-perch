# 2026-09-07 · P0 · 口型自然化重构（无 TTS）

> Cursor 单线。承 `2026-09-06-P0-口型与idle输出链路.md` 感受未勾；维护者反馈「张嘴不自然」，要参考成熟方案重做。  
> **状态**：**已编码放行并实现（2026-09-07）** · HITL #0–#2 齐。  
> **不含**：默认开 TTS / 真发声；新增 WS 语音事件；改左右分栏；音素级 ASR；胸肩呼吸。

## Spec（外部行为）

1. **说话时嘴自然开合**：流式出字期间可见、连贯的口型变化；打断/收回立刻合上。观感参考成熟方案的 **平滑过渡**（attack/release），而非 delta 乱换形。
2. **驱动仍跟文字输出节奏**（今日 `speech_delta` / 非流式 `speech`）；**不开默认语音**。口型入口保持可替换，日后 TTS/音频分析可换时钟源（契约同前包 #3=A）。
3. **全身取景可辨但不吓人**：轻开合、硬顶；不抬颌成黑洞；不永久张嘴。
4. **idle 不变**：既有 idle/blink/视线漂移保留；说话时不抢视线。
5. **布局不变**。

## 实现（步骤）

1. [x] 重写前端口型模块：目标口型 + 每帧 lerp（winner/runner）；废弃每 pulse 硬切轮换
2. [x] 文字 → 口型目标：**韵母粗映射**（#1=A）；接 `speech_delta` / 非流式 / 打断同构
3. [x] 可见性：全身取景 amp+硬顶；单测/契约更新
4. [x] 回写前包感受栏与临时批次；手测口径

## 验收（判据）

- [x] 工程：契约单测绿；韵母映射 + attack/release 入口面检查
- [ ] 流式出字：嘴随说而动，过渡平滑，无诡异高频抖换形（手测）
- [ ] 打断后立刻合上；说完不久合上，不永久张嘴（手测）
- [ ] 非流式整段：有简版自然起伏（手测）
- [x] 默认语音仍关；无新 WS 语音事件；入口仍可替换（`pulse({ text })` / TTS 注释）
- [ ] 感受：维护者勾

## HITL 批点

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 重构路线 | **A**：无 TTS；文字流 → viseme + 每帧 lerp；保留可替换入口 | **已拍板 A（2026-09-07）** |
| 1 | 文字→口型映射粒度 | **A**：韵母粗映射 → aa/ih/ee/oh/ou；无把握用中性开合 + 平滑 | **已拍板 A（2026-09-07）** |
| 2 | 编码放行 | **A**：按 #0/#1 开工 | **已拍板 A（2026-09-07）** |


### 浏览器实测调参（2026-09-07）

- 问题：浅笑 `Fcl_MTH_Fun` 等与开口 morph 抢戏；`aa` 表过宽；幅度两极跳。
- 改：语气词才大开；日常字走轻唇形轮换；说话时清冲突嘴 morph；cap/amp 折中。
- 工程：契约单测绿。感受仍待维护者勾。
