# 临时 · 产品化续做批次（2026-09-05）

> **临时工作单**，方便对照，不是施工权威。谈定/放行仍以各任务包 HITL 为准。  
> 用完可删或迁 archive。  
> 依据：`2026-08-29-产品化审计简报.md`（代码复核）+ `2026-09-05-交互形态-从对话到共处.md`。

---

## 已否决（不做）

| 项 | 说明 |
|----|------|
| 空间重构：对话降为抽屉 | 保留现行左右分栏 |
| 心跳外显（呼吸） | 曾放行后收回；代码已回退 |
| 时间痕迹连续化 | 静态痕迹已够用；不做褪色事件旁白（2026-09-06） |

---

## 待做批次（建议序）

| 批 | 做什么 | 备注 |
|----|--------|------|
| **1** | 主动开口 → 信笺通道 | **已编码放行** · `2026-09-06-P0-主动开口信笺通道.md` |
| **2** | 口型 + idle（+ TTS 分片设计同包） | 解 `lockMouth`；与流式同包想，避免返工 |
| **3** | look 截屏注入防护（最小） | 所见直进 LLM 的护栏/审计 |
| **4** | LLM 调用超时 + 429 退避 | 主 call 无 timeout；无 429 专用退避 |
| **5** | 开源收尾 | README 品牌句、内容分层 LICENSE、journal 私密另包 |
| **6** | P2 / 工程边角（可拆插） | 改数据路径 UI、换 VRM、markdown、500 字提示、历史搜索；secrets 凭据库、`httpx`/`numpy` 入依赖、CI 发版、E2E、pong 存活 |
| **7** | 数据模型去对话化 | 大改；等前面感受验后再立 |

**排期**：1 可先开；2 可插队但须与输出链路同设计；3–5 可与体验线错开；7 最后。

---

## 已收口主脊（不必重做）

P0 交互底楼 · 方向 D 静态三刀（痕迹/褪色/动向）· P2 设置→托盘 · P3 发版至 v0.1.2 · P4 日志/端口/SQLite · look 深路径 · 真名脱敏最小刀

---

## 怎么用

对 Cursor 说：「开批 N」或「先拍批 N」→ 按一块一拍开/续任务包；**未编码放行不动码**。
