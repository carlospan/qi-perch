# 2026-08-30 · P0：情绪事件推送

> Cursor 单线。HITL 真源：审计简报 §〇.9。  
> **编码放行**：维护者 2026-08-30 拍板 A。

---

## Spec（外部行为）

1. 前端**去掉 8s 情绪轮询**；连上时仍拉一次 `/state`；之后只听 `emotion_update`。
2. 后端在情绪快照相对上次有**可见变化**时推送；约 **1 秒**合并窗口。
3. 不含方向 D 呼吸/微动外显；不改情绪动力学公式。

## 实现（步骤）

1. [x] 落本包；§〇.9 收口
2. [x] 快照构建 + 变化比较 + 1s debounce 推送
3. [x] 心跳末调度推送；`/state` 复用同一快照
4. [x] 前端去 interval；发言后不再主动 `/state`
5. [x] 单测 + 相关 pytest

## 验收（判据）

- [x] 无 8s setInterval
- [x] 情绪变化会推 `emotion_update`（单测）
- [x] 合并约 1s（单测）
- [x] 相关测绿；感受待维护者

## HITL 批点

| 点 | 结论 |
|----|------|
| 范围 | 推送替代轮询（A） |
| 何时 | 可见变化 + 可合并（A） |
| 前端 | 连上一次 / 去轮询（A） |
| 合并 | ~1s（A） |
| 编码放行 | **已放行（A）** |

## 改动摘要

- `emotion_push.py`：快照 + 指纹；server 1s debounce
- 心跳末 `schedule_emotion_push`
- 前端去掉 8s 轮询与发言后主动 `/state`

