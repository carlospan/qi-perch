# L7 assist-7（全文读取与叙事内化）——编码请求

> **角色**：Trae（方案 Agent）
> **依据**：[整改复审](./2026-08-09-L7-assist7-全文读取与叙事内化-整改复审.md)（B1 关闭，无新阻塞）→ [修订后 PR 方案](./2026-08-09-L7-assist7-全文读取与叙事内化-PR方案.md)
> **时刻**：2026-08-09

---

## 放行声明

**明示可编码。** 以修订后 PR 方案为准落地；单块短路（B1 定案 a）已回写 PR/任务包/测试计划，无遗留阻塞。

---

## 精确改动点（以修订后 PR 为准）

### 1. `qi/action/assist.py` 常量区（L23-27）

- **删除** `_MAX_READ_BYTES = 32_768`
- **新增** `_MAX_FILE_BYTES = 1_048_576`（1MB 大小保护）、`_DIGEST_CHUNK_LEN = 8_000`、`_DIGEST_MAX_CHUNKS = 6`
- `_CONTENT_PREVIEW_LEN = 80` 保留（assist-6）

### 2. `qi/action/assist.py` 构造器（L36-43）+ `qi/action/layer.py` L78

- `AssistAction` 加 `narrative: NarrativeMemory | None = None` 参数（TYPE_CHECKING 补导入）
- layer.py：`self.assist = AssistAction(db, llm=llm, narrative=narrative)`

### 3. `qi/action/assist.py` execute 读取段（L83-95）

- `path.stat().st_size > _MAX_FILE_BYTES` → `_fail`「这个文件对我来说太大了。」failed_capability
- `content = path.read_text(encoding="utf-8", errors="ignore")`（去掉截断）

### 4. `qi/action/assist.py` 分块 digest + narrative 内化

- L113 改 `summary = await self._read_and_digest(target_path, content, season, now)`
- **删除** `_digest_file`，**新增** `_digest_chunk`（单块 digest，prompt 措辞「这是你读到的内容（可能只是其中一部分）」）/ `_merge_digests`（**单块短路**，多块才合并，truncated 附「你给我的文件很长，我只读完了前面一部分」）/ `_read_and_digest`（分块、逐块 narrative.save「我读了他给我的 {name}。{digest}」importance=0.65 tags=["assist","file_read"]）
- detail_json 不动（content_preview = 前 80 字；digest = 合并后 summary）

## 测试落地（修订后测试计划）

- `test_assist_short_file_single_digest`：短文件 1 次 digest（无合并）、narrative 1 条、无诚实声明
- `test_assist_long_file_chunked`：20_000 字符 → 3 块 digest + 1 合并、narrative 3 条
- `test_assist_oversize_chunks_truncated`：7 块 → 只 digest 6 块 + 诚实声明
- `test_assist_huge_file_fails_honest`：monkeypatch stat → 诚实失败 + failed_capability 留痕
- `test_assist_narrative_not_fulltext`：narrative 含概要不含全文
- `test_assist_detail_has_content_preview` 保留（assist-6）
- **迁移核对（R5）**：既有 assist 测试若断言 digest 调用次数/messages，短文件改为 **1 次**（B1 定案 a）

## 自测要求（回执必写）

- `pytest -q` 全量 ≥585 passed（改动文件先过）
- `ruff check` 改动文件通过
- 回执完工段：改动文件清单 + 新增测试 + 全量测试数 + 相对编码请求的执行判断

## 纪律红线

- R1：块 digest 逐块真实；超长/超大诚实声明 ✓
- R3：digest 走 `purpose="consciousness"` ✓
- Step 5：insert_action 保留 ✓
- 隐私：narrative 只存块 digest 概要不存全文 ✓

---

*Trae 方案 Agent · 2026-08-09 · 明示可编码 · 编码交 Cursor*
