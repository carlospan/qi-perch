# 2026-08-30 · P0 第三刀：队列满告知

> Cursor 单线。HITL 真源：审计简报 §〇.4。  
> **编码放行**：维护者 2026-08-30 拍板 A。

---

## Spec（外部行为）

1. 待处理用户句队列上限改为 **3**（原 8）。
2. 队列已满时：**拒收最新一句**（队内已有句保留）；**不**再静默丢最早。
3. 拒收时走 **系统态** 提示（与失败可见同通道），不走她的 speech 装懵。
4. 不含：发送 ACK、流式、打断逻辑改动。

## 实现（步骤）

1. [x] 落本任务包；§〇.4 收口
2. [x] `PENDING_QUEUE_MAX = 3`；满则不入队 + 挂起 system_notice
3. [x] 具身经既有 `take_pending_system_notice` 广播
4. [x] 单测 + pytest 864；未跑全感受批

## 验收（判据）

- [x] 上限为 3
- [x] 满时最新句不入队；旧句仍在
- [x] 有系统态 `queue_full`
- [x] 相关测绿

## HITL 批点

| 点 | 结论 |
|----|------|
| 满时策略 | 拒收最新 + 系统态（B） |
| 容量 | 3 |
| 编码放行 | **已放行（A）** |

## 改动摘要

- `PENDING_QUEUE_MAX=3`；去掉 deque maxlen 静默丢弃
- 满则 `queue_full` 系统态；`test_pending_queue` 契约回写
- L1 / code-wiki 常量同步
