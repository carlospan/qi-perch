# 2026-09-02 · P0：交互底楼补全（#2 LLM + #3 typing 边角）

> Cursor 单线。HITL 真源：审计简报 §〇.38；承 `2026-08-29-P0-安静地坏掉.md`（主体已做）。  
> **编码放行**：**已放行 A（2026-09-02）**。

---

## Spec（外部行为）

1. **WS 断线**：清 `typing` 与 typing 定时器（60s 主体不动）。  
2. **轮次结束**无 speech 且无挂起 `system_notice` → 兜底 notice：有 `last_outcome.failure` 用对应 kind，否则 `empty`。  
3. **简报** §一 #2/#3 回写「主体已接，本刀补角」。  
4. **不做**：服务端轮次超时、流式中途失败专项、改四类文案体系。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] `useQi` close → `setTyping(false)`  
3. [x] `server._run_user_turn` 静默兜底 + `fallback_notice_for_silent_turn`  
4. [x] 简报回写 + 单测  

## 验收（判据）

- [x] 断线清 typing（源码契约 + 逻辑）  
- [x] 无 speech 无 notice → 按 failure / empty 发 notice（单测）  
- [x] 有 pending notice 时不重复兜底（单测）  
- [x] 有 speech 时不发兜底（单测）  
- [ ] （感受）维护者：断线后输入可立即用；失败轮不再静默  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **一包补全 #2 + #3 边角** | **已拍板 A（2026-09-02）** |
| 1 | 本刀范围 | **最小补角** | **已拍板 A（2026-09-02）** |
| 2 | 兜底 notice 规则 | **分级 / 否则 empty** | **已拍板 A（2026-09-02）** |
| 3 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-09-02）** |

## 不含

- 服务端轮次超时 / 流式专项 / 改文案  
