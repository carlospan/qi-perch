# 2026-08-31 · P3：本机 Windows 安装包证伪（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.27；承 §〇.1 #1–#3。  
> **编码放行**：**已放行 A（2026-08-31）**。  
> **本机验收**：**已通过（2026-09-01）**。

---

## Spec（外部行为）

1. 仓库有可跟随的 Windows 打包说明（及包装脚本）：`build_qi_brain` →（建议 `fetch_bge_resource`）→ `tauri build`。  
2. 维护者本机编出安装包并安装后：壳能开、托盘在、能拉 bundled 脑（或沿用已开）、WS 可连；设置「关于」能见版本。不必自装 Python。  
3. BGE：正式包应带；本刀证伪不以包内已有 BGE 为关门条件。  
4. **不做** CI、不强制发 GitHub Release、不借刀改交互功能。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] `docs/how-to/Windows安装包-本机证伪.md` + `tools/pack_windows.py`；`tauri` bundle 目标改为 `nsis`  
3. [x] 修复安装落盘：`qi-brain.zip` 单文件进包 + 壳解压到 `%LOCALAPPDATA%\Qi\runtime\qi-brain`；NSIS hooks；完整性校验拒半套树  
4. [x] 安装壳体验：VRM 贴图（TextureLoader + `base: './'`）；退出不堵/`CREATE_NO_WINDOW`；卸载 `ExecShellWait`+`SW_HIDE`  
5. [x] **维护者本机**：编装验收通过（2026-09-01）

## 验收（判据）

- [x] 文档/脚本步骤可跟随  
- [x] 本机 `tauri build` 产出安装包（维护者）  
- [x] 安装后：开窗 / 托盘 / 脑可连；形象正常；退出/卸载无明显黑框（维护者 2026-09-01）  
- [x] 未做 CI / 强制 Release  
- [x] 不再把整棵 onedir 作为 NSIS resources（改 zip）

### 维护者命令摘要

```powershell
python tools/build_qi_brain.py          # 若尚无 qi-brain
python tools/fetch_bge_resource.py      # 建议
python tools/pack_windows.py            # → nsis 安装包
```

详见 `docs/how-to/Windows安装包-本机证伪.md`。

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 本机 Windows 安装包证伪（最小）** | **已拍板 A（2026-08-31）** |
| 1 | 本刀范围 | **最小证伪** | **已拍板 A（2026-08-31）** |
| 2 | 本刀是否必须带 BGE | **建议带、不硬卡** | **已拍板 A（2026-08-31）** |
| 3 | 本刀交付与验收 | **说明 + 脚本 + 本机绿** | **已拍板 A（2026-08-31）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-08-31）** |

## 不含

- CI / 强制发 Release / 借刀改交互  
