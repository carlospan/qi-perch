# 2026-08-25 · 委托检索 · P2 深读

> 总 Spec：`2026-08-25-委托检索对齐Agent.md`。本包只做 **P2**。

## Spec（外部行为）

1. 检索命中后，对 **1～2 个**高相关 URL **拉取页面正文**（超时/失败则跳过，不编造）。
2. 摘要时 **优先用深读正文**，搜索 snippet 作对照；仍遵守 P1：冲突不下死结论、轻出处、不混时态套话。
3. 选页启发式：优先非百科类（有其它源时）；仅 `http(s)`；可配置开关与条数。
4. **不做** P3 编排模块。

## 实现（步骤）

1. [x] `fetch_page_text`：httpx 拉 HTML → 抽纯文本（限长、超时）
2. [x] `pick_deep_read_hits`：选 1～2 URL
3. [x] `execute` / `_digest_hits`：注入深读材料；`found` 留痕 deep_read
4. [x] 配置 `delegate_deep_read` / `delegate_deep_read_max`
5. [x] 单测

## 验收（判据）

- [x] 工程单测绿（`tests/test_delegate_search.py` 18 passed）
- [ ] 感受：大会类问题能读到比纯 snippet 更具体的时间/地点（维护者）

## HITL

| 批点 | 结论 |
|------|------|
| 开 P2 | 维护者「开P2」（2026-08-25） |
| 编码放行 | 是 |
