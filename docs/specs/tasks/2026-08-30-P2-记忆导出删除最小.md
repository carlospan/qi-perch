# 2026-08-30 · P2：记忆导出 / 删除（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.20；承 §〇.1 #8。  
> **编码放行**：**已放行 A（2026-08-30）**。

---

## Spec（外部行为）

1. **设置页**提供「导出备份」与「删除全部记忆」；保留既有「打开数据文件夹」。  
2. **导出备份包**含记忆本体：`qi.db` + `chroma`（及若有的 `checkpoint` / `corpus`）；**不含** `user_secrets.env`、**不含** models（如 BGE）。  
3. **导出交付**：在数据根下 `backups/` 写入带时间戳的 zip，并**打开该文件夹**（便于拷走）。  
4. **删除**：清空与导出同集的记忆文件；**保留** `user_secrets.env` 与 models；设置内**两步确认**（说明不可恢复 →「确认删除」），不要求打字。删后应能继续相处（空记忆），不必为删记忆而重贴 key。  
5. **本刀不含**：应用内导入/恢复向导、自定义数据路径 UI、安装包、托盘、备份加密。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] 后端：`qi/data_lifecycle.py` 打包 `backups/` + 打开目录；删除同集；server 重建空库  
3. [x] 设置页：导出入口 + 两步删除确认与结果提示  
4. [x] 单测 / 针对性验收  

## 验收（判据）

- [x] 导出后 `backups/` 下有 zip，且打开文件夹；包内有 db/chroma 等、无 secrets/models（`tests/test_data_lifecycle.py`）  
- [x] 两步确认后记忆被清；secrets 仍在；设置/对话仍可用（wipe 单测 + server 重建空库）  
- [x] 未误做导入向导 / 改路径 UI  
- [x] 全量 pytest 绿  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做记忆导出 / 删除（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围 | **同刀**：导出备份 + 二次确认删除；保留打开文件夹；不含导入/改路径 | **已拍板 A（2026-08-30）** |
| 2 | 备份包装什么 | **记忆本体**；不含 secrets、不含 models | **已拍板 A（2026-08-30）** |
| 3 | 删除清什么 | **与导出同集**；保留 secrets 与 models | **已拍板 A（2026-08-30）** |
| 4 | 删除确认形态 | **设置内两步**；不要求打字 | **已拍板 A（2026-08-30）** |
| 5 | 导出怎么交给用户 | **`backups/` + 打开文件夹** | **已拍板 B（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-08-30）** |

## 不含

- 应用内导入 / 恢复向导  
- 自定义数据路径 UI / 迁移向导  
- 安装包 / 托盘  
- 备份包内的 API 钥匙与模型权重  
- 备份加密  
