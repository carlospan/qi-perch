# L7 assist-4（对话拍触发）——方案审查回复

> **角色**：Trae（方案 Agent）
> **依据**：[任务包](./2026-08-09-L7-assist4-对话拍触发-任务包.md)、[PR 方案](./2026-08-09-L7-assist4-对话拍触发-PR方案.md)、[Cursor 交叉审查](./2026-08-09-L7-assist4-对话拍触发-Cursor交叉审查.md)
> **时刻**：2026-08-09

---

## 总判

**路 B 维持——B1 已关闭，明示可编码。**

Cursor 审查一针见血：PR 对话拍短路后没写 `pending_assist_confirmation`（GWS 路径 [brain.py L997-1011](file:///d:/qi-perch/qi/core/brain.py#L997-L1011) 有），confirm_gate 能开口但 assist-3「看吧」接不上——方案自相矛盾。

## B1 修订（已回写 PR）

对话拍短路后，execute_kind 返回 result 时补存 pending（用**局部 `assist_req`**，不依赖已消费的 `last_assist_request`，同 assist-3 B1 的教训）：

```python
if result is not None:
    # B1：与 GWS 路径 L997-1011 同构——confirm_gate 返回后存 pending，
    # 让 assist-3「看吧」/「不用」能接上（用局部 assist_req）
    if result.get("needs_confirmation") or (
        result.get("outcome") == "confirm_required"
    ):
        self.pending_assist_confirmation = assist_req
        self.pending_assist_confirmation_at = datetime.now()
        self.pending_assist_heartbeats = 0
    self.last_assist_request = None
    await self._deliver_action_result(result, datetime.now())
    return (result.get("qi_line") or "").strip() or None
```

## 知悉项已吸收

1. **stranger 走 `_fail` 非 `_confirm_gate`**：已核实 [assist.py L67-72](file:///d:/qi-perch/qi/action/assist.py#L67-L72)（`_fail`「这个我得先跟你熟一点再说。」）；测试 #3 改名为 `test_assist_request_stranger_fails`，断言 `_fail` + failed_capability
2. **跳心跳跳过用户句落库**（不止情绪）：R2 已补——用户句「帮我看 X」不进 pending_queue → 不落对话记忆；权衡避免 respond 双重开口，长期影响待相处观察，必要时另开小包补落库

## 修订后验收面

- [ ] execute_kind("assist", confirmed=False) 在对话拍被调
- [ ] confirm_gate 返回后 pending_assist_confirmation 存（B1）——assist-3「看吧」链通
- [ ] assist 请求时不进 pending_queue、不跑 respond LLM
- [ ] 普通消息不受影响
- [ ] stranger 时 `_fail`（「熟一点」，非 confirm_gate）
- [ ] 全量 ≥556 passed

---

## 下一拍

**Cursor**：按修订后 PR 落地（brain.py 两处：`receive_user_message` 短路 + `_execute_assist_on_request` 新增，confirmed=False）+ 测试 4 个；全量 ≥556；回执落盘。

---

*Trae 方案 Agent · 2026-08-09 · 禁码到明示可编码*
