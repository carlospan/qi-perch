# 2026-09-06 · P0 · 主动开口信笺通道

> Cursor 单线。承 `2026-09-05-交互形态-从对话到共处.md` 第四层；**不改**左右分栏。  
> **状态**：**HITL 收口并编码放行（2026-09-06）**；实现已合入。

## Spec（外部行为）

1. **仍进谈区时间线**：proactive 主动开口不踢出 TalkView（便于回看「你不在时她来过」）。
2. **样式区分**：带 `proactive` 标记的句子以**信笺条**呈现（非普通回复气泡）；旁注「她来过」。被动回复样式不变。
3. **入场**：新主动句到达时，形象区 `notice` 一眼；历史回灌 / 上翻加载**不**触发 notice。
4. **标记落库**：`speech` 与 messages 含 proactive；`/history` / 上翻回灌带上。
5. **旧数据不回填**：无标记的历史句仍按普通气泡。
6. **不含**：改布局、口型/TTS、改日限/冷却、整表 channel 大拆、踢出 TalkView、启发式回填。

## 实现（步骤）

1. [x] `messages.proactive` 列 + 迁移；`save_message` / `load_*` 读写
2. [x] `send_speech(..., proactive=)`；`emit_speech` / `push_proactive_text` / `deliver_qi_message` 接线
3. [x] `/history` 格式化带 `proactive`
4. [x] 前端 `TalkMessage` / `speech` 处理；信笺条样式 +「她来过」；新主动句 bump `replyEpoch`（notice）
5. [x] `tests/test_proactive_letter.py`

## 验收（判据）

- [x] 新 proactive 句：谈区信笺条 +「她来过」
- [x] 新句入场 bump notice；历史回灌不走该路径
- [x] 标记落库；history 回灌同构
- [x] 无标记旧句仍为普通气泡
- [x] 被动回复样式不变
- [x] 相关 pytest
- [ ] 感受：一眼能分「她来找我」vs「她回我」（维护者勾）

## HITL 批点

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 本刀范围 | **C**：仍进 TalkView；样式区分 | **已拍板 C（2026-09-06）** |
| 1 | 谈区怎么区分 | **C**：信笺条 + 新句 notice | **已拍板 C（2026-09-06）** |
| 2 | 标记与历史 | **A**：落库 + 回灌同构 | **已拍板 A（2026-09-06）** |
| 3 | 旧消息与文案 | **A**：不回填；旁注「她来过」 | **已拍板 A（2026-09-06）** |
| 4 | 编码放行 | **A** | **已拍板 A（2026-09-06）** |
