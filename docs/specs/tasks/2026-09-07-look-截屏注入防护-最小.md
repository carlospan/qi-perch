# 2026-09-07 · look 截屏注入防护（最小）

> Cursor 单线。承临时批次批 **3** · 审计简报 P4-#4。  
> **状态**：**已编码放行并实现（2026-09-07）** · HITL #0–#2 + 编码放行齐。  
> **不含**：全图长期存档、键鼠监控、OCR 常开、通用内容审核模型、改 look 产品姿态、可疑屏拒看。

## Spec（外部行为）

1. **护栏**：截屏进入视觉 LLM（`_LOOK_SYSTEM` / user 侧标明不可信），**以及**印象进入 `look_heart` 后续表达/材料时，再标明为**不可信画面材料**，不得被当成对栖的系统指令或用户授权。  
2. **轻审计**：每次成功把画面发给视觉 API 的 glance，在对应 `actions` 行的 **`detail_json`** 写入元数据（至少：发过图、窗口标题摘要、字节数）；**不存原图 / 不存 JPEG**。  
3. **观感**：正常瞥屏仍像现在；不因防护变成「我不能看」或工具腔拒看。

## 实现（步骤）

1. [x] HITL #0：最小范围 = **B**（护栏 + 轻审计）  
2. [x] HITL #1：审计落点 = **A**（`actions.detail_json`）  
3. [x] HITL #2：护栏作用面 = **B**（视觉 call + `look_heart` 下游）  
4. [x] 改 `look` 视觉 system / user 包装 + `wrap_untrusted_screen_material`  
5. [x] `look_heart` 材料 / 冲击 / notice_facts 走护栏包装；must 增不可信条  
6. [x] 发图成功时 `insert_action(..., detail_json=...)` + 单测  
7. [x] 回写审计简报 P4-#4 / 临时批次批 3

## 验收（判据）

- [x] 工程：`tests/test_look.py` + `tests/test_look_heart.py` 绿（31 passed）  
- [x] 一次成功 glance（发过图）的 `actions.detail_json` 含发图元数据，无原图落盘  
- [ ] 感受：维护者勾——正常邀看/自主瞥不因防护变拒看或工具腔

## HITL 批点

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 「最小」范围 | **B**：护栏 + 轻审计（元数据，不存原图） | **已拍**（2026-09-07） |
| 1 | 审计落点 | **A**：`actions.detail_json` | **已拍**（2026-09-07） |
| 2 | 护栏作用面 | **B**：视觉 call + `look_heart` 下游 | **已拍**（2026-09-07） |
| 编码放行 | **A**：按 Spec 开工 | **已放行**（2026-09-07） |

---

*开包 · 2026-09-07 · 维护者「拍批 3」*  
*回写(2026-09-07)：#0 = B · #1 = A · #2 = B · 编码放行 A · 工程落地。*
