# L7 assist-8（记忆整体叙事）——PR 方案

> **角色**：Trae（方案 Agent）；编码交 Cursor
> **依据**：[任务包](./2026-08-09-L7-assist8-记忆整体叙事-任务包.md)
> **对照代码**：`qi/action/assist.py`（`_read_and_digest` L226-252 / execute 调用 L125 / `_merge_digests` L186-224 / `_CONTENT_PREVIEW_LEN` L29-30）
> **时刻**：2026-08-09

---

## 外部可观测行为

1. 读文件后 narrative 只新增 **1 条**「我读了他给我的 {name}。{整体感受}（里面写着：{preview}）」
2. explore 内部深读 limit=3 不再拿到同一文件的 3 条碎片
3. 被问「我之前给你看的文件」→ 栖从 narrative 可回忆概要（感受 + preview 事实锚点）

## 精确改动点

### 1. `qi/action/assist.py` `_read_and_digest`（L226-252）重构

**现状**：循环内逐块 `narrative.save`（最多 6 条碎片）+ `_ = (season, now)` 占位。

**改为**：
```python
    async def _read_and_digest(
        self, target_path: str, content: str
    ) -> str:
        """分块消化全文。返回开口 qi_line / summary。"""
        name = Path(target_path).name
        chunks = [
            content[i : i + _DIGEST_CHUNK_LEN]
            for i in range(0, len(content), _DIGEST_CHUNK_LEN)
        ]
        truncated = len(chunks) > _DIGEST_MAX_CHUNKS
        chunks = chunks[:_DIGEST_MAX_CHUNKS]

        digests: list[str] = []
        for chunk in chunks:
            d = await self._digest_chunk(name, chunk)
            if d:
                digests.append(d)
        if not digests:
            return f"我看了看 {name}。"
        summary = await self._merge_digests(name, digests, truncated)
        if self.narrative is not None:
            # assist-8：读一个文件 = 一个记忆事件（整体感受 + 内容概要锚点）
            preview = content[:_CONTENT_PREVIEW_LEN].replace("\n", " ")
            await self.narrative.save(
                f"我读了他给我的 {name}。{summary}（里面写着：{preview}）",
                importance=0.65,
                tags=["assist", "file_read"],
            )
        return summary
```

> 块 digest 只作合并输入，不再落 narrative；`season/now` 参数顺带清理（assist-7 验收知悉项）。

### 2. `qi/action/assist.py` execute 调用处（L125）

**改为**：
```python
        summary = await self._read_and_digest(target_path, content)
```

## 纪律红线对照

- **R1**：merged 基于真实块 digest；preview 为真实原文截断 ✓
- **隐私**：narrative 存 preview（80 字概要）属「记得」；explore 不引用内容由隐私小刀包兜底 ✓
- **Step 5**：insert_action 保留（actions preview 当下窗口不变）✓

## 测试计划与验收清单

### 新增/修改测试（`tests/test_assist_fulltext.py` / `test_assist_action_rewrite.py`）

1. `test_assist_long_file_chunked` **迁移**：narrative 从「3 条」改为「**恰好 1 条**」，内容含 merged + 「里面写着：{preview}」
2. `test_assist_short_file_single_digest`：narrative 1 条不变，补断言内容含 preview 概要
3. 新增 `test_assist_narrative_single_event`：多块文件 → 1 条记忆事件（显式断言 len==1 + 含 preview）
4. `test_assist_narrative_not_fulltext` 保留（preview 80 字 = 概要不算全文）

### 验收清单

- [ ] 多块文件：narrative 恰好 1 条，含 merged + preview
- [ ] 短文件：1 条不变，含 preview
- [ ] 碎片断言迁移无遗漏
- [ ] `_read_and_digest` 签名去 season/now + 调用处同步
- [ ] 全量 ≥585 passed

## 风险 / 不确定点

- **R1**：`test_assist_fulltext.py` 现断言「narrative 3 条」等碎片断言——Cursor grep `narrative` 断言逐一迁移
- **R2**：preview 拼接可能超行——`_CONTENT_PREVIEW_LEN=80` 固定，无风险

## 需维护者拍板项

- HITL 三项（内化粒度 / 概要锚点 / 签名清理）a/a/a——按维护者扩权采纳

---

*Trae 方案 Agent · 2026-08-09 · 编码交 Cursor · 本方案 Agent 不写 qi/ 代码*
