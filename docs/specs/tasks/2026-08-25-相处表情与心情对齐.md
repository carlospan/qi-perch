# 2026-08-25 · 相处页表情与心情对齐

## Spec（外部行为）

1. 心情尚可（valence 中高）时，脸上优先浅笑/开心，不因好奇值偏高拧成「发愁」。
2. 浅笑可见但仍睁眼；勿长期瞪眼、勿 Joy 眯眼闭眼笑。
3. 顶栏心情白话与脸不必逐字同构，但正向心情不应长期像难过。

## HITL

| 批点 | 结论 |
|------|------|
| 边测边调 | 维护者「边测边调…修完把数据调回」（2026-08-25） |
| 编码放行 | 是 |

## 实现

1. [x] `AvatarController`：valence>0.2 优先 soft_smile，curious 后置；arousal 阈值略抬
2. [x] VRM `EXPR_TARGETS`：soft_smile 满量 + 轻 happy；勿混 relaxed（本模型会眯眼）；curious 压低
3. [x] notice：0.35s 后恢复眨眼
4. [x] DEV `?avatar_expr=` 可预览；临时 `__qiFace` 探针已撤
5. [x] 单测：心情尚可时不因 curious 压过 soft_smile

## 验收

- [x] `tests/test_embodiment.py` 绿（12）
- [x] 页面预览 soft_smile 有浅笑、睁眼；清掉 URL 覆盖后走后端映射
- [x] 活库 valence≥0 + 高好奇 → soft_smile（非 curious）
