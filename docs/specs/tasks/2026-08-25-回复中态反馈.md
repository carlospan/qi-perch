# 2026-08-25 · 回复中态反馈（A 档）

> 桌面壳：用户发送后不易察觉栖是否仍在生成回复。HITL 拍板 **A**：全局「她在回你」。
> 等待气泡文案 HITL：**固定「在想怎么回你」**（2026-08-25）。

## Spec（外部行为）

1. **顶栏**：`typing` 为真时，StatusBar 明确显示「在回你」（可与心境并存，优先可辨认）。
2. **相处时间线**：等待气泡比现状更醒目（呼吸 + 点点动画），仍用既有 `typing` 信号，不引入流式。
3. **输入区**：回复中 placeholder/提示为「栖还在想……」类文案；**暂缓连发**（不可再点发送 / Enter 发出），通道未连仍优先「未连上」。
4. **范围**：仅 `qi/embodiment/desktop` 前端表现；不改 WS 协议、不接流式吐字（C 档另包）。

## 实现（步骤）

1. [x] HITL：A 档（2026-08-25）
2. [x] `StatusBar.vue`：`replying` → 「在回你」
3. [x] `TalkView.vue`：等待气泡加强（dots + 更明显呼吸）
4. [x] `InputBox.vue`：`busy` → 提示 + 禁发
5. [x] `App.vue`：把 `typing` 接到顶栏与两处 InputBox

## 验收（判据）

- [ ] 发送后顶栏出现「在回你」，收到 `speech` 后消失（维护者感受）
- [ ] 相处页等待气泡可见点点/呼吸（维护者感受）
- [ ] 回复中无法连发；可继续改草稿；掉线时仍显示未连上（维护者感受）
- [ ] 切到回顾/内在/状态页时，顶栏仍能提示「在回你」（维护者感受）
- [x] 工程：相关 Vue 组件接线完成，无协议改动

## HITL 批点

| 批点 | 结论 |
|------|------|
| 手感档位 A/B/C | **A**（2026-08-25 维护者） |
| 编码放行 | **放行**（拍 A 即放行本包） |
| 流式吐字 | 不做，归后续包 |
