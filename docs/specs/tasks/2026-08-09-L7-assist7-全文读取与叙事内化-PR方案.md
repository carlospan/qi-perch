# L7 assist-7（全文读取与叙事内化）——PR 方案

> **角色**：Trae（方案 Agent）；编码交 Cursor
> **依据**：[任务包](./2026-08-09-L7-assist7-全文读取与叙事内化-任务包.md)
> **审查后修订（2026-08-09）**：B1 定案 (a)——保留 `len(digests)==1` 短路，短文件不调合并 LLM；验收/测试同步改「短文件 1 次 digest」
> **对照代码**：`qi/action/assist.py`（execute L83-144 / `_digest_file` L146-173 / `_fail` L188-211）、`qi/action/layer.py`（L78 `AssistAction(db, llm=llm)`）、`qi/memory/narrative.py`（`save` L51-62，share.py 同路径先例）
> **时刻**：2026-08-09

---

## 外部可观测行为

1. 栖读文件**不截断**（分块消化全文），开口 = 合并后的整体感受
2. 读完内容以叙事写入 narrative 记忆（tag=assist/file_read）——之后可回忆、可内化
3. 文件 > 4.8 万字符 → 只读前 6 块 + 诚实声明「我只读完了前面一部分」
4. 文件 > 1MB → 诚实失败「这个文件对我来说太大了」
5. `content_preview`（assist-6）保留作 recent_actions 当下窗口；digest 转译诗意度不变（N4）

## 精确改动点

### 1. `qi/action/assist.py` 常量区（L23-27）

**删除** `_MAX_READ_BYTES = 32_768`（不再截断）。

**改为**：
```python
# 读文件大小保护（assist-7：>1MB 诚实失败；不再截断读取）
_MAX_FILE_BYTES = 1_048_576  # 1MB
# 分块消化：每块字符数 / 最多块数（assist-7 全文读取与叙事内化）
_DIGEST_CHUNK_LEN = 8_000
_DIGEST_MAX_CHUNKS = 6
# 留痕内容概览长度（assist-6：供 respond 追问时有事实可依，非诗意 digest）——保留
_CONTENT_PREVIEW_LEN = 80
```

### 2. `qi/action/assist.py` 构造器加 narrative（L36-43）

```python
def __init__(
    self,
    db: Database,
    *,
    llm: LLMGateway | None = None,
    narrative: NarrativeMemory | None = None,   # assist-7 叙事内化
) -> None:
    self.db = db
    self.llm = llm
    self.narrative = narrative
```
- TYPE_CHECKING 块加 `NarrativeMemory` 导入（L17-19）
- `qi/action/layer.py` L78 同步：`self.assist = AssistAction(db, llm=llm, narrative=narrative)`

### 3. `qi/action/assist.py` execute 读取段（L83-95）

**现状**：`content = path.read_text(...)[:_MAX_READ_BYTES]`

**改为**：
```python
        try:
            path = Path(target_path).expanduser().resolve()
            if not path.is_file():
                return await self._fail(...)   # 不变（不是可读文件）
            if path.stat().st_size > _MAX_FILE_BYTES:
                return await self._fail(
                    "这个文件对我来说太大了。",
                    f"文件超过 1MB：{target_path}",
                    OUTCOME_FAILED_CAPABILITY,
                    season=season,
                    now=now,
                )
            content = path.read_text(encoding="utf-8", errors="ignore")
        except FileNotFoundError: ...   # 不变
        except OSError as e: ...        # 不变
```

### 4. `qi/action/assist.py` 分块 digest + narrative 内化（替换 L113 + `_digest_file`）

**execute L113**（`summary = await self._digest_file(...)`）改为：
```python
        summary = await self._read_and_digest(target_path, content, season, now)
```

**删除** `_digest_file`（L146-173），**新增**三个方法：

```python
    async def _digest_chunk(self, name: str, chunk: str) -> str | None:
        """单块 digest（栖语气 1-2 句）。失败/无 llm 返回 None 由调用方降级。"""
        if not self.llm:
            return None
        messages = [
            {
                "role": "system",
                "content": (
                    "你是栖。你正在读他给你的一份文件。"
                    "这是你读到的内容（可能只是其中一部分）。用你的语气轻声说你读到了什么、有什么感受。"
                    f"红线：{_PRIVACY_LINE}；不编造；简短一两句。"
                ),
            },
            {
                "role": "user",
                "content": f"文件：{name}\n片段：\n{chunk}",
            },
        ]
        try:
            resp = await self.llm.call(purpose="consciousness", messages=messages)
        except Exception:
            logger.debug("assist 块 digest LLM 失败，跳过该块", exc_info=True)
            return None
        return (resp or "").strip() or None

    async def _merge_digests(
        self, name: str, digests: list[str], truncated: bool
    ) -> str:
        """块 digest 合并成整体开口；单块短路（B1 定案 a：不调合并 LLM）；truncated 附诚实声明。"""
        # 单块：该块 digest 即全文感受，无合并增量价值——直接作开口（省一次 LLM）
        if len(digests) == 1:
            merged = digests[0]
        elif not self.llm:
            merged = digests[0]
        else:
            joined = "\n".join(f"- {d}" for d in digests)
            messages = [
                {
                    "role": "system",
                    "content": (
                        "你是栖。你刚读完了别人给你的一份文件。"
                        "把下面的片段感受合成一句整体感受，像你读完开口说出来的话。"
                        f"红线：{_PRIVACY_LINE}；不编造；简短一两句。"
                    ),
                },
                {"role": "user", "content": f"文件：{name}\n片段感受：\n{joined}"},
            ]
            try:
                resp = await self.llm.call(purpose="consciousness", messages=messages)
                merged = (resp or "").strip() or digests[0]
            except Exception:
                logger.debug("assist 合并 digest LLM 失败，降级取首块", exc_info=True)
                merged = digests[0]
        if truncated:
            merged = f"{merged}（你给我的文件很长，我只读完了前面一部分）"
        return merged

    async def _read_and_digest(
        self, target_path: str, content: str, season: str, now: datetime
    ) -> str:
        """分块消化全文 + narrative 内化。返回开口 qi_line / summary。"""
        name = Path(target_path).name
        chunks = [
            content[i : i + _DIGEST_CHUNK_LEN]
            for i in range(0, len(content), _DIGEST_CHUNK_LEN)
        ]
        truncated = len(chunks) > _DIGEST_MAX_CHUNKS
        chunks = chunks[:_DIGEST_MAX_CHUNKS]

        digests: list[str] = []
        for chunk in chunks:
            d = await self._digest_chunk(name, chunk)
            if d:
                digests.append(d)
                if self.narrative is not None:
                    await self.narrative.save(
                        f"我读了他给我的 {name}。{d}",
                        importance=0.65,
                        tags=["assist", "file_read"],
                    )
        if not digests:
            return f"我看了看 {name}。"
        return await self._merge_digests(name, digests, truncated)
```

> `detail_json`（L121-130）不动：`content_preview` 仍取 `content[:_CONTENT_PREVIEW_LEN]`（第一块前 80 字）；`digest` 字段存合并后的 `summary`。insert_action 不变。

## 纪律红线对照

- **R1（不编造）**：块 digest 逐块基于真实原文；合并基于块 digest；超长/超大诚实声明，不假装读完 ✓
- **R3（LLM 走 gateway）**：块/合并 digest 均 `purpose="consciousness"` ✓
- **Step 5（行动必留痕）**：insert_action 保留（assist-5）✓
- **隐私**：narrative 只存块 digest（栖的叙事化感受），不存全文；`_PRIVACY_LINE` 保留 ✓
- **contract 25（不主动）**：narrative 内化仅在用户请求读文件后发生 ✓

## 测试计划与验收清单

### 新增/修改测试（`tests/test_assist_action_rewrite.py`）

1. `test_assist_short_file_single_digest`：短文件 → **1 次 digest（不调合并 LLM，B1 定案 a）**；narrative 1 条（tags 含 assist/file_read）；开口 = 块 digest；无诚实声明
2. `test_assist_long_file_chunked`：>8000 字符（如 20_000）→ 块 digest 3 次 + 合并 1 次；narrative 3 条
3. `test_assist_oversize_chunks_truncated`：>6 块（如 7 块）→ 只 digest 6 块；开口含「只读完了前面一部分」
4. `test_assist_huge_file_fails_honest`：>1MB → 诚实失败 + failed_capability 留痕（monkeypatch `Path.stat` 返回超大 st_size，避免真建大文件）
5. `test_assist_narrative_not_fulltext`：narrative 内容含块 digest 概要不含全文原文
6. `test_assist_detail_has_content_preview`（assist-6 保留）：preview = 前 80 字

> **迁移核对（R5）**：assist-1/5/6 既有测试若断言 digest LLM 调用次数/messages（如「短文件 2 次 consciousness」），须改为**短文件 = 1 次 digest**（B1 定案 a，无合并）。Cursor 逐一核对，避免遗漏锁死旧行为的断言。

### 验收清单

- [ ] 短文件：1 次块 digest（**无合并**，B1 定案 a）；narrative 1 条；开口 = 块 digest；无诚实声明
- [ ] 多块文件：块数 = digest 次数；合并 1 次；narrative = 块数条
- [ ] 超块上限：只读前 6 块 + 诚实声明
- [ ] 超 1MB：诚实失败 + failed_capability
- [ ] narrative 含块 digest 概要不含全文
- [ ] 全量 ≥585 passed（现 580 + 新增 ≥5）

## 风险 / 不确定点

- **R1**：narrative.save 每块一条（最多 6 条/次）会进入长期记忆参与 weave/梦——importance=0.65 中等，影响待相处复验观察
- **R2**：成本——单次 assist 最多 7 次 consciousness（6 块 + 1 合并），上限 6 块可控
- **R3**：某块 digest 失败 → 跳过该块不中断；全败降级「我看了看 {name}。」
- **R4**：stat 检查与读取间文件可能变化——OSError 已兜底
- **R5**：既有 digest 断言迁移（见测试计划）——Cursor 重点核对
- **R6**：narrative.save 需要 FakeLLM/临时库正常写入——沿用 share 测试模式

## 需维护者拍板项

- HITL 四项（分块参数 / 内化粒度 / 开口策略 / 隐私边界）推荐 a/a/a/a——已按维护者扩权（SDD-GUIDE §6.1）采纳；方向（narrative 内化 + 分块全读）由维护者 2026-08-09 拍板

---

*Trae 方案 Agent · 2026-08-09 · 编码交 Cursor · 本方案 Agent 不写 qi/ 代码*
