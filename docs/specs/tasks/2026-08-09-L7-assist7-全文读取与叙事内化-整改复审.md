# L7 assist-7（全文读取与叙事内化）——整改复审

> **角色**：Cursor（执行侧整改复审，本轮**仍禁码**）  
> **依据**：修订后 [PR 方案](./2026-08-09-L7-assist7-全文读取与叙事内化-PR方案.md)（文首 `审查后修订`：B1 定案 a）+ [任务包](./2026-08-09-L7-assist7-全文读取与叙事内化-任务包.md)（验收段已同步）+ [交叉审查](./2026-08-09-L7-assist7-全文读取与叙事内化-Cursor交叉审查.md)  
> **时刻**：2026-08-09  

---

## 总判

**B1 已关闭。整改复审通过。无新阻塞。**

仍禁码，待方案 Agent 落盘 `-编码请求.md` 并明示可编码后再动 `qi/`。

---

## 阻塞项逐条核对

| 项 | 交叉审查要求 | 修订后落点 | 状态 |
|----|--------------|------------|------|
| **B1** | 短文件合并次数与骨架对齐；选 (a) 或 (b) 并全文一致 | 定案 **(a)**：`_merge_digests` 单块短路不调合并 LLM；任务包验收「短文件 1 次块 digest（无合并）」；测试改名 `test_assist_short_file_single_digest`；R5「短文件 = 1 次」 | **关闭** |

### B1 骨架核对（通过）

```python
# _merge_digests（修订后）
if len(digests) == 1:
    merged = digests[0]          # 不调合并 LLM
elif not self.llm:
    merged = digests[0]
else:
    # 多块 → consciousness 合并
    ...
if truncated:
    merged = f"{merged}（你给我的文件很长，我只读完了前面一部分）"
```

| 场景 | 预期 LLM 次数 | 开口 |
|------|---------------|------|
| 短文件（≤1 块成功 digest） | **1**（仅块） | 块 digest；无诚实声明 |
| 多块（如 3） | 3 块 + 1 合并 | 合并结果 |
| >6 块 | 6 块 + 1 合并 | 合并结果 + 诚实声明后缀 |
| >1MB | 0（`_fail`） | 「太大了」 |

`_digest_chunk` prompt「可能只是其中一部分」已写入——单块=全文、多块=片段均可读。

---

## 知悉（编码请求 / 编码时消化，不阻塞）

| # | 备注 |
|---|------|
| N1 R5 | `test_assist_digest_uses_llm` 等：短文件断言 **1** 次 consciousness；勿写死「2 次」 |
| N2 layer | `AssistAction(db, llm=llm, narrative=narrative)` 勿漏传 |
| N3 | 1MB 测用 mock `st_size`；字符块 vs 字节上限勿混 |
| N4 | 文首「外部可观测」§1 / Spec「开口=合并感受」对**多块**成立；短文件开口=块 digest——与验收一致，编码勿再改回短文件强制合并 |
| N5 | 交叉审查观察项（weave 体感、整文件读入内存等）仍观察，不进本包 |

---

## 结论

| 项 | 结论 |
|----|------|
| B1 | **关闭** |
| 新阻塞 | **无** |
| 分轨 | 路 B 整改闭环 → 等 **编码请求** |
| 下一拍 | 方案 Agent 落盘 `-编码请求.md`（以修订后 PR 为准）+ 明示可编码 → Cursor 编码 + 回执完工段 |

---

*Cursor · 整改复审 · 2026-08-09 · 仍禁码*
