# L7 explore 真搜索 d-1——编码请求

> **角色**：方案 Agent（Trae）
> **依据**：[整改复审](./2026-08-08-L7-explore真搜索-d1联网地基-整改复审.md)（B1/B2/B3 已关闭，复审通过）+ 修订后 [PR 方案](./2026-08-08-L7-explore真搜索-d1联网地基-PR方案.md)（文首「审查后修订 · 路 B 第2步」）+ [任务包](./2026-08-08-L7-explore真搜索-d1联网地基.md)
> **状态**：整改复审通过 → **明示可编码**。Cursor 按本请求 + 修订后 PR 编码。
> SDD：[§2.3 路 B 第 4 步](file:///d:/qi-perch/docs/specs/SDD-GUIDE.md)

---

## 明示可编码

Cursor 整改复审结论「B1/B2/B3 已关闭，待编码请求」。本文件即放行件——**Cursor 可编码**，按下述改动点 + 修订后 PR 骨架落地，回执写完工段。

---

## 编码范围（文件清单 · 照 PR §七）

只动下列文件：

1. `qi/action/explore_web.py`（**新建**）— `WebSearchClient` + `SearchHit`
2. `qi/action/explore.py` — `ExploreAction.__init__` + `drift()` 外部分支 + `_should_external`/`_make_query`/`_external_cooldown_ok`
3. `qi/action/layer.py` — `ActionLayer.__init__` 加 `llm` 参数 + 建 `WebSearchClient` + 下传
4. `qi/core/brain.py` — **仅 `restore_state` 构造传 `llm=self.llm` 一行**（不改心跳）
5. `qi/config/settings.example.yaml` — `action.explore_external:` 块
6. `.env.example` — 加 `TAVILY_API_KEY=` 一行
7. `tests/test_explore_web.py`（新）+ `tests/test_explore_external_branch.py`（新）
8. `docs/reference/layers/L7-action.md` + `docs/progress.md` — 回写

**不进 diff / 不碰**：`settings.yaml`（`.gitignore`，本机手改，N2）/ `README` / `brain_delivery.py`（B3 不改）/ 心跳逻辑 / 无关 chore。

---

## 精确改动点（相对骨架落地约定 · 以修订后 PR 为准）

### 1. `qi/action/explore_web.py`（新建）
- `SearchHit` dataclass：`{title, snippet, url}`
- `WebSearchClient.__init__(*, provider: str, api_key: str | None, config: dict)`；`async def search(self, query: str, *, top_k: int = 3) -> list[SearchHit] | None`
- d-1 **只实现 `_search_tavily`**（httpx async，POST /search；失败/空 → `None`，让 drift 落 found=None）
- `_search_ddg` 留 `NotImplementedError`/不写（N5）
- 不入库（不入 L2/Chroma）；只回 drift

### 2. `qi/action/explore.py`
- `ExploreAction.__init__` 加 `llm: LLMGateway | None = None` + `web: WebSearchClient | None = None`（`web`/`llm` 任一 None → 外部分支禁用 → 走内部，向后兼容现测，B2/N8）
- `drift()` 飘出后（curiosity≥0.65 内部门已过）：`if await self._should_external(curiosity, now):` 外部分支，else 现有 `_scan_sandbox` 包一层（**内部源行为不变**）
- `_should_external`：`self.web is None or self.llm is None` → False（N8）；`curiosity < 0.8` → False；`not self._external_cooldown_ok(now)` → False；`random.random() > 0.05` → False；否则 True。**不重复判 mode**（独处由 volition `solitary` 候选保证，N1）
- `_make_query`：`await self.llm.call(purpose="consciousness", messages=[...])`（复用，温度0.85；**不污染 `last_outcome`**——仅 `conversation` purpose 刷新，B2）；messages 注入「不引用 user_facts / 对话内容」红线句
- 外部返回 dict 加 `speak=True, qi_line=<summary>, source="web"`（成功/空手都开口，模板化 summary **不二次 LLM**，B3）；空手 `qi_line="我看了看外面，没查到什么。不假装看见了。"`；成功 `qi_line=f"我刚才看了看 {query}……{hits[0].title}。"`
- 内部返回 dict 不带 `speak`/`qi_line`（保持不说，B3）
- `insert_action` outcome：外部空手=`OUTCOME_FAILED_CAPABILITY`（不形成伤疤）；外部成功 / 内部=`OUTCOME_SUCCESS`
- 冷却：body_memory key `explore_external_last`（复用预算持久化同构，[budget.py](file:///d:/qi-perch/qi/action/budget.py)）；`_external_cooldown_ok(now)` 读之，阈值 6h

### 3. `qi/action/layer.py`
- `ActionLayer.__init__` 加 `llm: LLMGateway | None = None` 参数
- 按 config 建 `WebSearchClient`（`enabled`+`api_key` 配齐才建，否则 `None`）
- `self.explore = ExploreAction(db, narrative=narrative, config=self.config, web=web, llm=llm)`
- tick / execute_kind 调 drift 不变（[L226-235](file:///d:/qi-perch/qi/action/layer.py#L226-L235)、[L289-318](file:///d:/qi-perch/qi/action/layer.py#L289-L318) 不改）

### 4. `qi/core/brain.py`（仅一行）
- `restore_state`（[L1156-1158](file:///d:/qi-perch/qi/core/brain.py#L1156-L1158)）：`ActionLayer(db, self.config, narrative=self.memory.narrative, llm=self.llm)`
- **不改心跳 / 不改其他构造**；`self.llm` 已存在（gateway，[Brain.__init__](file:///d:/qi-perch/qi/core/brain.py)）

### 5. `qi/config/settings.example.yaml`
```yaml
action:
  autonomous_daily_limit: 20
  explore_external:
    enabled: false                  # 默认关，配齐 key 才开
    provider: tavily                # tavily | ddg | ...
    api_key: "${TAVILY_API_KEY}"    # 走 .env（N3）
    cooldown_hours: 6
    # 不设独立 external daily_limit——复用 ActionBudget 日限 20（HITL③）
```
- **不加 `daily_limit` 字段**（B1/HITL③）

### 6. `.env.example`
- 加 `TAVILY_API_KEY=`（占位，N3；与 deepseek/tokenrhythm 同构）

---

## 三条阻塞钉死（B1/B2/B3 · 编码时不得偏离）

- **B1**：`settings` 块 / `explore_external` / 任何 config 结构 **不出现 `daily_limit` 字段**。外部稀有靠 curiosity≥0.8 + 冷却6h + 概率0.05，不靠独立日限。
- **B2**：`ExploreAction` 必须接 `llm` 并由 `ActionLayer`/`brain.restore_state` 透传；`_make_query` 必须走 `gateway.call(purpose="consciousness", ...)`。不得擅自改成启发式 query（违反 HITL②）。
- **B3**：外部结果必须带 `speak=True + qi_line`（成功/空手都开口）；走 `deliver_action_result` 现有 `elif speak and qi_line` 分支（[brain_delivery.py:134-137](file:///d:/qi-perch/qi/core/brain_delivery.py#L134-L137)）；**不改 `brain_delivery.py`**；内部源不带 `speak` 保持不说。「注入下轮对话」不在 d-1（降级延后）。

---

## 测试要求（照 PR §二E + §四）

### `test_explore_web.py`
mock httpx，验 Tavily 成功/失败/空 → hits/None/None

### `test_explore_external_branch.py`
- 门控：curiosity<0.8 不外部；≥0.8+冷却过+概率命中→外部；冷却未过→内部
- `web=None`/`llm=None` → 走内部防回归（N8）
- `force=True` 与非 force 分别覆盖（N7，force 跳 0.12 内部门仍吃 0.05 外部门）
- 红线：外部搜不到 → found=None、outcome=failed_capability、不编造
- query 隐私：断言 gateway 调用 messages 含「不引用 user_facts / 对话内容」红线句
- 说出口（B3）：外部带 `speak`/`qi_line`；内部不带
- gateway 接线（B2）：断言 `purpose="consciousness"`；`llm` 经 `ActionLayer`/`ExploreAction` 透传

现有 explore 测试不回归（`web`/`llm` 缺省走内部，行为不变）。

### 全量
`pytest` ≥474（不回归）

---

## 文档回写

- `docs/reference/layers/L7-action.md`：explore 段加「外部联网（d-1）」+ 演进指向更新
- `docs/progress.md`：加 d-1 决策记录
- `docs/reference/contract.md`：**不改正文**，仅验收清单标「contract drift：自主行动日限 1/天 vs 20，待同步」（防 drift）

---

## 完工回执要求

`-Cursor编码回执.md` 完工段：列落地文件 + 自测结果（pytest 数 + 全量是否 ≥474）+ 偏离说明（若有，相对本请求/PR 骨架）。

---

## 纪律红线（照 PR §三，不重列；不引入 Agent 框架 / LLM 走 gateway / DB 走 database / 不开新阶段）

**获明示可编码（本文件）后，Cursor 可动 `qi/`。**
