# 2026-08-25 · 委托检索 · P3 编排收口

> 总 Spec：`2026-08-25-委托检索对齐Agent.md`。本包 **P3**：检索计划小模块，与闲聊/意图入口分开。

## Spec（外部行为）

1. P0–P2 的「改写→多搜→深读→对照摘要」收口为独立 **`DelegateSearchPlanner`**（`qi/action/search_plan.py`）。
2. `DelegateSearchAction` 只负责：意图已定后的落库 / 卡片 / 叙事；**编排不写在 brain / 闲聊路径里**。
3. 外部行为相对 P2 **不变**（仍多轮、时效、深读、对照）；可单测 planner。
4. 总 Spec 标记 P0–P3 完成。

## 实现（步骤）

1. [x] 新增 `search_plan.py`：`SearchPlanResult` + `DelegateSearchPlanner.run`
2. [x] 迁入：variants / merge / multi-search / deep-read / digest
3. [x] `DelegateSearchAction.execute` 改调 planner；`_digest_hits` 等保留薄委托以兼容旧测
4. [x] 单测 planner；总 Spec 勾完

## 验收（判据）

- [x] planner 可独立单测
- [x] `tests/test_delegate_search.py` 绿
- [ ] 维护者感受：行为不回退（可选）

## HITL

| 批点 | 结论 |
|------|------|
| 开 P3 | 维护者「开始做P3」（2026-08-25） |
| 编码放行 | 是 |
