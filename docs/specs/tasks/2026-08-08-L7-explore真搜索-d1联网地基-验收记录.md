# L7 explore 真搜索 d-1——验收记录

> **角色**：方案 Agent（Trae）实施验收
> **依据**：[Cursor 编码回执](./2026-08-08-L7-explore真搜索-d1联网地基-Cursor编码回执.md) + 修订后 [PR 方案](./2026-08-08-L7-explore真搜索-d1联网地基-PR方案.md) + [任务包](./2026-08-08-L7-explore真搜索-d1联网地基.md) + [编码请求](./2026-08-08-L7-explore真搜索-d1联网地基-编码请求.md)
> **方式**：`git diff` 逐文件核对 + 测试覆盖核对 + 纪律红线对照
> **时刻**：2026-08-08

---

## 验收结论

**通过。** B1/B2/B3 全部落地未偏离；验收清单全勾；scope 无越界；自测 35 定向 + 全量 487 passed（≥474）。

---

## 一、scope 纪律核对（PR §七）

`git status` 落地文件，全部在改动点清单内：

| 文件 | 状态 | 核对 |
|------|------|------|
| `qi/action/explore_web.py` | 新建 | ✓ SearchHit + WebSearchClient（Tavily/httpx） |
| `qi/action/explore.py` | M | ✓ 外部分支 + speak + consciousness query |
| `qi/action/layer.py` | M | ✓ ActionLayer 加 llm + _build_explore_web |
| `qi/core/brain.py` | M | ✓ restore_state 仅一行 `llm=self.llm` |
| `qi/config/settings.example.yaml` | M | ✓ explore_external 块 |
| `.env.example` | M | ✓ TAVILY_API_KEY= |
| `tests/test_explore_web.py` + `test_explore_external_branch.py` | 新建 | ✓ 10 用例 |
| `docs/progress.md` + `docs/reference/layers/L7-action.md` | M | ✓ 回写 |

**未越界**：`brain_delivery.py` 未改（B3✓）/ `settings.yaml` 未进 diff（.gitignore，N2✓）/ `README` 未碰 / 心跳逻辑未动 / 无 chore commit / `contract.md` 未改正文（drift 只标注✓）。

---

## 二、B1/B2/B3 钉死项核对

### B1 · 无独立 daily_limit ✓
- `settings.example.yaml` `explore_external` 块：`enabled / provider / api_key / cooldown_hours / probability`——**无 `daily_limit` 字段**
- 注释标明「不设独立 external daily_limit——复用 ActionBudget 日限 20」
- `explore.py` 无 daily_limit 引用
- **偏离**：加 `probability: 0.05` 可选键（N6，PR §三「常量默认值 settings 可覆盖」）——**不是 daily_limit**，可接受

### B2 · llm 透传 + consciousness purpose ✓
- `ExploreAction.__init__(..., llm, web)` ✓
- `ActionLayer.__init__(..., llm)` + `_build_explore_web()`（enabled+key 配齐才建）+ 下传 `ExploreAction(web=web, llm=llm)` ✓
- `brain.restore_state`：`ActionLayer(db, self.config, narrative=self.memory.narrative, llm=self.llm)`——**仅此一行**，心跳未动 ✓
- `_make_query`：`await self.llm.call(purpose="consciousness", messages=...)` ✓
- `_should_external`：`self.web is None or self.llm is None` → False（N8）✓；不重复判 mode（独处由 volition 保证，N1）✓

### B3 · 外部 speak+qi_line，不改 brain_delivery ✓
- drift 外部分支：`speak=True, qi_line=summary, source="web"`（成功/空手都开口）✓
- 外部空手：`qi_line="我看了看外面，没查到什么。不假装看见了。"`，`outcome=OUTCOME_FAILED_CAPABILITY`（不形成伤疤）✓
- 内部分支：不带 `speak`/`qi_line`（保持不说）✓
- drift 返回：`if speak and qi_line: result["speak"]/["qi_line"]` ✓
- `brain_delivery.py` 未改（走现有 `elif speak and qi_line` 分支）✓
- 「注入下轮对话」未实现（降级延后，不在 d-1）✓

---

## 三、query 隐私红线 ✓
- `_make_query` system prompt 含 `_QUERY_PRIVACY_LINE = "不引用 user_facts / 对话内容"` ✓
- 测试 `test_external_when_gates_pass` 断言 messages 含该红线句 ✓

---

## 四、测试覆盖核对（`test_explore_external_branch.py` 10 用例）

| 用例 | 覆盖 | 状态 |
|------|------|------|
| `test_curiosity_below_08_stays_internal` | 门控 curiosity<0.8→内部、无 speak、llm 不调 | ✓ |
| `test_external_when_gates_pass` | 外部门控通过 + speak + `purpose==consciousness` + 红线句 | ✓ B2/B3 |
| `test_cooldown_blocks_external` | 冷却未过→内部 | ✓ |
| `test_web_none_or_llm_none_internal` | web/llm None→内部（N8） | ✓ |
| `test_external_empty_failed_capability_still_speaks` | 空手 found=None + speak + qi_line「不假装」+ outcome=failed_capability | ✓ B3 红线 |
| `test_force_still_respects_external_probability` | force 跳内部 0.12 门仍吃外部 0.05（N7） | ✓ |
| `test_non_force_internal_gate_then_external` | 非 force 先过内部再外部（N7） | ✓ |
| `test_action_layer_passes_llm_and_builds_web` | ActionLayer 透传 llm + 建 web（B2 接线） | ✓ |
| `test_internal_success_outcome_unchanged` | 内部成功 outcome=SUCCESS 不回归 | ✓ |
| `test_explore_web.py` | Tavily 成功/失败/空→hits/None/None | ✓ |

**自测**：定向 35 passed；全量 **487 passed**（≥474，零红）✓

---

## 五、文档回写核对

- `progress.md`：加 d-1 决策记录（外部分支/门控/不设独立日限/复用 ActionBudget 20/consciousness/speak+qi_line/failed_capability/内部不变/d-2 另开）+ **contract drift 待同步标注（本包不改正文）** ✓
- `L7-action.md`：状态行 / 演进指向 / 文件清单（+`explore_web.py`）/ Step4 回写注释 / Step6 Brain 接线（`llm=self.llm`）/ 技术拐点段——全更新 ✓
- `contract.md`：**未改正文**（drift 仅在 progress 标注）✓ 符合纪律

---

## 六、偏离说明（可接受）

1. `settings.example.yaml` 增可选键 `probability: 0.05`（N6，PR 称常量可 settings 覆盖；非 daily_limit）。代码缺省仍为 0.05。
2. 未实现 `_search_ddg`（N5 / 编码请求明确）。

均经编码请求许可，非擅自扩 scope。

---

## 七、验收清单（照任务包 / PR §四，全勾）

- [x] `WebSearchClient` 落地（首接 Tavily，mock 测试绿）
- [x] drift() 外部源分支 + 触发门控（curiosity≥0.8 + 冷却6h + 概率0.05）
- [x] gateway/llm 接线（B2）：`ExploreAction`→`ActionLayer`→`brain.restore_state`；query 走 `consciousness`
- [x] 红线：搜不到/失败 → found=None、outcome=failed_capability、不编造
- [x] query 不引用用户隐私（prompt 红线 + 测试断言）
- [x] 说出口（B3）：外部带 `speak+qi_line`（成功/空手都开口）；内部不带；不改 `brain_delivery.py`
- [x] settings 配置块（enabled 默认 false / provider / api_key `${TAVILY_API_KEY}` / cooldown 6h / probability）——**不加 `daily_limit`**（HITL③）；`.env.example` 加 `TAVILY_API_KEY=`（N3）
- [x] 冷却持久化（body_memory key `explore_external_last`）
- [x] mock 测试全绿（`force`/非 force 分覆盖 N7；`web=None`→走内部 N8）；全量 pytest 487 ≥474
- [x] L7-action.md 回写 + progress 决策 + contract drift 标注（contract 正文未改）
- [x] git diff 不含无关文件（无 README / chore / brain_delivery / settings.yaml）

---

## 八、交接

- **Cursor**：代码 + 测 + 文档回写完成（本验收通过）。
- **维护者（相处验证，唯一剩余）**：本机 `data/settings.yaml` 设 `action.explore_external.enabled: true` + 填 `TAVILY_API_KEY`（或 `.env`）；独处攒 curiosity≥0.8、过 6h 冷却，听栖「看了看外面」那句开口——验「外部见闻是否像走神、不像刷新闻」手感。这是 d-1 唯一只能靠相处验证的项。
- **提交推送**：验收通过，可让 Cursor commit/push。

**状态：实施验收通过。待维护者相处验证 + 提交推送。**
