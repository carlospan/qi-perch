# 2026-08-23 · 空卡实质问句兜底（关系问等）

> Cursor 单线。承接实证：用户问「我们两个现在是什么关系」→ 空意向卡 + `outcome=template` → 只回「……嗯。」  
> 邻包：`2026-08-17-催答空回复兜底.md`（已闭环，仅覆盖催答句式，**非催答空卡仍可为「……嗯。」**）。  
> **一块一拍已编码**（HITL #1–#4）；感受验收待维护者。

## 改动摘要

- `qi/core/expression.py`：`_EMPTY_CARD_SAFE`、`_empty_primary_fallback`、`render_template` 空素材全局兜底
- `tests/test_expression.py`：关系问实证 + 空卡断言
- `tests/test_intention.py`：空卡 answer / brain 集成断言更新

---

## Spec（外部行为）

1. 空素材模板兜底（`render_template` 及同类路径）：`primary` 为空时，**不得**最终输出「……嗯。」或等价敷衍；统一改为维护者定稿句（见 HITL #2）。
2. 适用范围：**全局**——不论用户问的是关系、闲聊还是其他；与 `2026-08-17` 催答包不冲突（催答仍走催答安全句优先）。
3. 非催答、非空返回的 LLM 正常回复路径不变；本包先只改**模板兜底**（HITL #3 再定是否扩 ellipsis 重试）。

## 实现（步骤）

1. [x] HITL 关键块齐 → 维护者编码放行
2. [x] 常量 `_EMPTY_CARD_SAFE`（`_FREE_TALK_SAFE` 同句）= HITL #2 定稿句
3. [x] `render_template`：`primary` 为空且非催答专用分支 → 用定稿句
4. [x] 单测：非催答空卡 + 关系问实证
5. [x] `pytest tests/test_expression.py tests/test_intention.py` → 56 passed

## 验收（判据）

- [x] 空素材 + 非催答 → `render_template` 输出 =「……刚才那句，我没接好。能再说一次吗？」
- [x] 催答路径仍走 `_ANSWER_CHASE_SAFE`，不被本包覆盖
- [x] `tests/test_expression.py` / `test_intention.py` 相关绿（56 passed）
- [ ] 感受：维护者复现「我们两个现在是什么关系」类问句，若落模板则不再是「……嗯。」

## HITL 批点

| # | 议题 | 结论 |
|---|------|------|
| 1 | **范围**：这轮治哪类问句？ | **C — 全局改模板**：不论问什么，空素材兜底一律不得最终只回「……嗯。」**（维护者 2026-08-23）** |
| 2 | 空卡兜底定稿句 | **「……刚才那句，我没接好。能再说一次吗？」**（维护者 2026-08-23）** |
| 3 | LLM 首答若直接产出「……嗯。」（非催答）：是否也强制重试一拍？ | **否 — 本包只改模板**；若改后仍见 `outcome=llm` 直出敷衍，再单开「仅空卡重试」小包**（维护者采纳 Agent 建议 2026-08-23）** |
| 4 | 编码放行 | **已放行（「那就根据你的建议来」2026-08-23）** |
| 5 | 感受验收 | 待维护者 |

### 实证锚点

- 用户句：`我们两个现在是什么关系`（2026-08-23 00:26）
- `last_intention`：`act=free_talk`，`materials=none`，`stage=bonded`，`outcome=template`
- 前一条回复正常（模型/API 当拍可用）
