# 2026-08-31 · P3：默认可分发 VRM（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.25；承 §〇.1 #9。  
> **编码放行**：**已放行 A（2026-08-31）**。

---

## Spec（外部行为）

1. 开箱/clone 后，`npm run dev` / `build` / 安装壳存在页能加载默认 VRM，不必另找文件。  
2. 默认源文件：`qi/embodiment/assets/qi-avatar.vrm`（维护者自有，已确认可随包分发）**入库**（本仓已跟踪该文件）。  
3. `sync-avatar` 从 assets 同步到 `public/avatars/qi-avatar.vrm`（public 副本仍 gitignore）。  
4. 仓内有简短许可/归属说明（`assets/NOTICE.md`）。  
5. **本刀不含**：设置页换 VRM UI、关于页、CI、桌宠第二窗。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] 确认 `assets/qi-avatar.vrm` 已在版本库；补 `NOTICE.md`  
3. [x] 回写 README / 换机搭建 / desktop README（开箱自带）  
4. [x] 本地 `sync-avatar` 证伪通过  

## 验收（判据）

- [x] `assets/qi-avatar.vrm` 在版本库中（`git ls-files`）  
- [x] `node …/sync-avatar.mjs` 能产出 `public/avatars/qi-avatar.vrm`  
- [x] 文档写明开箱自带；`NOTICE.md` 存在  
- [x] 未做换模 UI / 关于页 / CI  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 默认可分发 VRM（最小）** | **已拍板 A（2026-08-31）** |
| 1 | 本刀范围 | **最小可看见**；不做换模 UI/关于页/CI | **已拍板 A（2026-08-31）** |
| 2 | 默认用哪套模型与许可 | **维护者自有现模**；可随包分发 | **已拍板（2026-08-31）** |
| 3 | 默认文件怎么进仓/进包 | **直接入库** + 许可说明 | **已拍板 A（2026-08-31）** |
| 4 | 本刀交付与验收 | **入库 + 同步证伪** | **已拍板 A（2026-08-31）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-08-31）** |

## 不含

- 设置换模 UI / 关于页 / CI / 桌宠第二窗  
