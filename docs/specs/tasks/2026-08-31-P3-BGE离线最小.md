# 2026-08-31 · P3：BGE 离线进资源（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.24；承 §〇.1 #12。  
> **编码放行**：**已放行 A（2026-08-31）**。

---

## Spec（外部行为）

1. 有可用 BGE 文件时用语义向量；查找顺序：**`QI_BGE_DIR`** → 显式配置路径 → **数据根** `models/bge-small-zh-v1.5` → **资源离线** `…/resources/bge-small-zh-v1.5` → 皆无则 **n-gram**（不自动 HF）。  
2. 仓库提供 `tools/fetch_bge_resource.py`：把官方 ONNX 拉到 `src-tauri/resources/bge-small-zh-v1.5/`；**权重不进 git**。  
3. `ensure_bge_model` 仍可供换机/手动拉到数据根；运行时 `VectorStore` **不**自动调用下载。  
4. Tauri bundle 挂上该资源目录。  
5. **本刀不含**：默认 VRM、关于页、CI、换嵌入模型族、强制拷贝到 AppData。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] 解析链（env / 显式 / 数据根 / 资源）+ 单测夹具  
3. [x] fetch 脚本 + resources README + gitignore + tauri resources  
4. [x] 回写换机搭建 / desktop README  

## 验收（判据）

- [x] 解析链单测：优先序正确；皆无 → n-gram；显式缺失不串扰  
- [x] fetch 脚本目标路径正确；权重被 gitignore  
- [x] 运行时不自动 HF  
- [x] `tests/test_vector_store_bge.py` 绿（含原有 + 新解析测）  
- [x] 未做 VRM/关于页/CI  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 BGE 离线进资源（最小）** | **已拍板 A（2026-08-31）** |
| 1 | 本刀范围 | **最小离线**：脚本 + 查找链 + n-gram；权重不进 git | **已拍板 A（2026-08-31）** |
| 2 | 无离线时是否仍允许 HF | **运行时不自动下**；`ensure_bge_model` 仅手动 | **已拍板 A（2026-08-31）** |
| 3 | 离线资源放哪 | **资源只读直读**；不强制拷 AppData | **已拍板 A（2026-08-31）** |
| 4 | 本刀交付与验收 | **脚本 + 解析 + 单测**；真模可选 | **已拍板 A（2026-08-31）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-08-31）** |

## 不含

- 默认 VRM / 关于页 / CI / 换模型族 / 强制拷 AppData  
