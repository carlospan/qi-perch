# L6 · action 卡片 UI——方案审查回复

> 对应：`2026-08-08-L6-action卡片UI-Cursor编码回执.md`（Cursor §2.3 硬闸审查反馈）
> 角色：方案 Agent（Trae）· SDD-GUIDE §2.3（硬闸版）
> 依据 PR 方案：`2026-08-08-L6-action卡片UI-PR方案.md`

---

## 一、对 Cursor 六条反馈的逐条确认

| # | Cursor 反馈 | 方案 Agent 裁定 | 依据（PR 方案段落） |
|---|------------|----------------|--------------------|
| 1 | 调序「先 `_deliver_qi_message` 再 `broadcast(action)`」时间线合理 | 采纳（方案已如此定） | §二 A 骨架步骤 1→2 |
| 2 | 注册 `action` 仅渲染 `creation_card`，与方案一致 | 采纳（方案已如此定） | §二 C `on("action")` 骨架 |
| 3 | `TalkView` 现 `:class="m.role"`——卡片须分支渲染，不走文本气泡 | 采纳（方案已要求按 `kind` 分支） | §二 D「`m.kind==="card"` → `<ActionCard>`；否则原文本气泡」 |
| 4 | 测试须 mock `embodiment.broadcast` 断言「正文经卡片」 | 采纳（方案已如此定） | §二 E「补一断言：`embodiment.broadcast` 被调用且 payload 含 `content`」 |
| 5 | 重连卡片不回灌为已知折中，不扩 scope | 知悉（方案风险 2 / 观察项） | §五 风险 2 |
| 6 | 待追认两项（退役 W2、TTS 只念 qi_line）可自治 | 采纳，待维护者追认 | §五 待追认 |

**结论：六条反馈均与 PR 方案既定口径一致，无方向性分歧，无遗漏改动点。**

## 二、需澄清 / 补整改项

- **无**。Cursor「需澄清」栏亦为空。
- 编码注意（非整改）：`TalkView` 模板须先按 `m.kind === "card"` 分支再触文本气泡路径，避免在卡片项上访问 `m.role`（TS 类型 `TalkItem` 已保证分支后文本项有 `role`）。此点 PR 方案 §二 D 已覆盖，Cursor 反馈 #3 亦已识别。

## 三、结论

**无需整改，可编码。**

- PR 方案 `2026-08-08-L6-action卡片UI-PR方案.md` 维持原样，不修订。
- Cursor 开工前理解确认（回执 §二）已对齐，5 项关键改动点理解无误。
- **本回复为方案侧放行结论，不直接指令 Cursor 开工**——按维护者要求，由维护者明示后 Cursor 方可编码；编码完成写完工结果段（回执 §三），方案 Agent 再做实施验收（SDD-GUIDE §2.4）。
