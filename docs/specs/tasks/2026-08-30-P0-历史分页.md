# 2026-08-30 · P0：历史分页（上翻加载）

> Cursor 单线。HITL 真源：审计简报 §〇.8。  
> **编码放行**：维护者 2026-08-30 拍板 A。

---

## Spec（外部行为）

1. 首屏仍拉最近约 **200** 条（现有 `/history`）。
2. 谈区滚到顶部附近 → 自动请求更早一页，约 **50** 条文本消息。
3. 插入后**保持阅读位置**；没有更早时轻提示「没有更早的了」（非她 speech）。
4. **本刀只翻文本**；创作/见闻卡仍靠首屏 cards。
5. 不含：搜索、按日跳转、导出。

## 实现（步骤）

1. [x] 落本包；§〇.8 收口
2. [x] `load_messages_before` + `/history_before` → `history_page`
3. [x] 前端滚顶加载、prepend、保位置、到头提示
4. [x] 单测 + 相关 pytest

## 验收（判据）

- [x] 首屏行为与现网一致（约 200）
- [x] 上翻可加载更早文本（工程）
- [x] 保位置；到头有提示（前端已接）
- [x] 相关测绿；感受待维护者

## HITL 批点

| 点 | 结论 |
|----|------|
| 范围 | 仅上翻（A） |
| 触发/页 | 滚顶自动 / 50（A） |
| 首屏 | 仍 ~200（A） |
| 位置/到头 | 保位置 + 轻提示（A） |
| 卡片 | 本刀不做（A） |
| 编码放行 | **已放行（A）** |

## 改动摘要

- `Database.load_messages_before`；`/history_before` → `history_page`
- 前端滚顶自动请求；prepend 保位置；「没有更早的了」
- 本刀只翻文本，卡片仍首屏
