# 2026-08-29 · 产品化审计简报（Qoder 三路审计转交）

> **状态**：诊断索引，非施工权威；**产品形态已定案为领养包**；§〇.1 细节 **#1–#14 已拍板并收口**（#15 起名已收回）。P0 交互底楼 §一 #1–#8 **均已编码放行**（含情绪事件推送 §〇.9）。方向 D 第一刀 §〇.11、第二刀 §〇.12、第三刀 §〇.13 **均已编码放行**。P2 设置→记忆/允许根 §〇.14–〇.21 **已编码放行**；§〇.22「托盘常驻」**已编码放行**。§〇.23「P3 正式 sidecar」**已编码放行**。§〇.24「P3 BGE 离线」**已编码放行**。§〇.25「P3 默认 VRM」**已编码放行**。§〇.26「P3 关于页」**已编码放行**。§〇.27「P3 本机安装包证伪」**已编码放行且本机编装验收通过（2026-09-01）**。其余 P1–P4 排队。
> **来源**：维护者 2026-08-29 与 Qoder 讨论（变现筹资 → 产品形态 → “体验像聊天机器人”），Qoder 派三路只读审计（交互链路 / 桌面端 / 工程产品化）汇总；Cursor 复核代码后与维护者渐进对齐。  
> **流程**：本文只供选块；每一块开工前按 `SDD-GUIDE.md` §2.5 一块一拍、单独开任务包（模板 `_template.md`），编码前重读 `reference/contract.md`「理解意图」。

---

## 〇、已定 / 待定的方向性决策（背景，不含施工）

| 议题 | 结论 | 状态 |
|------|------|------|
| 开源许可 | **维持 MIT**；README **补一句**：本许可不授予「栖 / Qi」名称/品牌使用权（姿态声明，非法律意见） | **已拍板 A（2026-08-29）** |
| 内容/灵魂许可分层 | **分层**：`qi/` 代码 MIT；灵魂书 / 契约 / prompts / 故事等另用更严许可（如 CC BY-NC-SA，禁商用）；`data/` 与私人 VRM 不开。维护者本人不受 NC 约束 | **已拍板 A（2026-08-29）** |
| 开源前真名脱敏 | **开源/公开发布前置**：对外公开仓或 Release 前必须脱敏（约 5 处 + 视需要处理 journal 私密场景）。另开小刀任务包执行 | **已拍板 A（2026-08-29）**；施工细节见 §〇.10 |
| 真名脱敏本刀范围 | **最小刀**：真名→伪名（docs+tools+tests）；journal 只改含真名处；私密叙事与邮箱另包 | **已拍板 A（2026-08-30）**；细节见 §〇.10 |
| 方向 D 第一刀 | **「时间的痕迹」最小切片**（存在页生命语气痕迹；真统计；禁仪表盘） | **已拍板 A（2026-08-30）**；细节见 §〇.11 |
| 方向 D 第二刀 | **回顾 · 记忆褪色可视化**（叙事记忆真 strength；禁假褪色） | **已拍板 A（2026-08-30）**；细节见 §〇.12 |
| 方向 D 第三刀 | **轻量动向流**（存在页可瞥见一行真事件旁白） | **已拍板 A（2026-08-30）**；细节见 §〇.13 |
| P2 设置页第一刀 | **最小钥匙页**（API key + 可选 base_url/模型） | **已拍板 A（2026-08-30）**；细节见 §〇.14 |
| P2 设置页试连通 | **设置页「试一下」探测**（独立小刀） | **已拍板 A（2026-08-30）**；细节见 §〇.15 |
| P2 首启体验第一刀 | **先在场、卡点再问（最小）** | **已拍板 A（2026-08-30）**；细节见 §〇.16 |
| P2 领养指引第一刀 | **应用内领养指引页（最小）** | **已拍板 B（2026-08-30）**；细节见 §〇.17 |
| P2 数据目录第一刀 | **平台标准用户数据目录（最小）** | **已拍板并编码放行 A（2026-08-30）**；细节见 §〇.18 |
| P2 连接失败排错第一刀 | **连接失败可见排错（最小）** | **已拍板并编码放行 A（2026-08-30）**；细节见 §〇.19 |
| P2 记忆导出/删除第一刀 | **设置内导出备份 + 确认删除（最小）** | **已拍板并编码放行 A（2026-08-30）**；细节见 §〇.20 |
| P2 世界触达允许根第一刀 | **设置里自选允许根（最小）** | **已拍板并编码放行 A（2026-08-30）**；细节见 §〇.21 |
| P2 托盘常驻第一刀 | **托盘 + 关主窗不杀后端（最小）** | **已拍板并编码放行 A（2026-08-30）**；细节见 §〇.22 |
| P3 正式 sidecar 第一刀 | **安装壳能拉起内置大脑（最小）** | **已拍板并编码放行 A（2026-08-30）**；细节见 §〇.23 |
| P3 BGE 离线第一刀 | **向量模型离线进资源（最小）** | **已拍板并编码放行 A（2026-08-31）**；细节见 §〇.24 |
| P3 默认 VRM 第一刀 | **可分发默认形象进包（最小）** | **已拍板并编码放行 A（2026-08-31）**；细节见 §〇.25 |
| P3 关于页第一刀 | **版本号 + Releases 链接（最小）** | **已拍板并编码放行 A（2026-08-31）**；细节见 §〇.26 |
| P3 本机安装包证伪 | **本机 tauri build 能装能起（最小）**；zip 大脑 + 形象/退出/卸载修复 | **已拍板并编码放行 A（2026-08-31）**；**本机验收通过（2026-09-01）**；细节见 §〇.27 |
| 下一步施工优先 | **先走 P0 交互底楼**（拆成多块，一块一拍）；不立刻整包开工。真名脱敏 / 方向 D 排队 | **已拍板 B（2026-08-29）** |
| P0 第一刀选哪条 | **「她不会安静地坏掉」最小闭环**：LLM 不可达/缺 key 可见 + typing/busy 超时解锁 + 空回复不单用「……」冒充沉默。流式/口型/TTS、历史分页等另包 | **已拍板 A（2026-08-29）**；细节见 §〇.2 |
| 打错字 / 编辑已发送 | **按相处而非改聊天记录**：（1）未开口：可「重说」（叫住她、取消本轮）。（2）出字中/流式：可「先别说了」打断，再发新句改口。（3）已说完：下一句白话更正。（4）**不做**气泡编辑器、不做已入记忆的静默撤回；大厂同款编辑重生另议且不优先 | **已拍板（2026-08-29）：维护者认可生命向方案** |
| P0 第二刀选哪条 | **未回复前可叫住她**：重说 / 出字中打断（与上条同构）。不做消息编辑 UI 为主路径 | **已拍板 A（2026-08-29）**；细节见 §〇.3 |
| P0 第三刀选哪条 | **队列满告知**：待处理用户句队列满时不再静默丢弃，须让用户知道 | **已拍板 A（2026-08-30）**；细节见 §〇.4 |
| 递送前随机 sleep | **删除**：生成完后不再 `sleep(0.5~1.5)`；自然延迟只来自模型等真实耗时。独立小刀，不绑流式 | **已拍板 A（2026-08-30）**；细节见 §〇.5 |
| P0 流式出字选哪条 | **仅对话 speech 流式**；口型/TTS/后台 LLM/历史分页另包。打断须能停正在出的字 | **已拍板 A（2026-08-30）**；细节见 §〇.6 |
| P0 发送回执选哪条 | **静默成功**：接到才继续；未接到系统态。ACK=WS 收到并开始处理，非她开口 | **已拍板 A（2026-08-30）**；细节见 §〇.7 |
| P0 历史分页选哪条 | **上翻加载更早消息**；不含搜索/跳日/导出 | **已拍板 A（2026-08-30）**；细节见 §〇.8 |
| P0 情绪推送选哪条 | **事件推送替代 8s 轮询**；不含方向 D 外显 | **已拍板 A（2026-08-30）**；细节见 §〇.9 |
| 产品形态 | **领养包**（定案）：预构建安装包 + 领养指引 + 社区；用户自带 LLM key、数据留本地。**不做**消费级托管云版（违背“住在你自己电脑里”；托管 = 面向公众提供生成式 AI 服务，备案等合规义务单人扛不起——非法律意见） | **已拍板 A（2026-08-29）**；细节见 §〇.1 |
| 名字 | **只叫「栖」**；不引入正名「栖迟」。仓库/包名/英文 Qi 不动 | **已拍板 A（2026-08-29）** |
| 变现背景 | 目标=筹资让大脑回本地（混合路由：dream/走神等低价值心跳可先走本地小模型，`model_routing` 天然支持）；账号/接单等变现线不在本简报范围 | 背景 |

### 〇.1 领养包细节 · 一块一拍（**已收口** 2026-08-29）

> 形态已定「领养包」；下列为包内边界与体验细节。**谈定已写入本表**。本表 HITL 已收口；施工另开任务包。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 领养对象（谁能养） | **半技术领养人**：会装软件、能申请/粘贴 API key、愿读一页指引；不必会 Python/Node/编译。双击安装包可养，不要求 clone 仓库 | **已拍板 A（2026-08-29）** |
| 2 | 安装包是否内含大脑 | **内含大脑（正式 sidecar）**：安装包带可启动后端；用户不必自装 Python / clone。目标态即此，不做「永不内嵌」 | **已拍板 A（2026-08-29）** |
| 3 | 首发平台范围 | **先只 Windows**：首发安装包只保证 Win；macOS/Linux 明示尚未领养或仅源码自建 | **已拍板 A（2026-08-29）** |
| 4 | API key 怎么交给她 | **应用内设置页（最小）**：首启或设置里粘贴 key（及可选 base_url/模型）；写入本地配置；改 key 有常驻入口，不要求会找 `.env` | **已拍板 A（2026-08-29）** |
| 5 | 记忆与数据落在哪 | **大厂方案**：默认平台标准用户数据目录（如 `%LOCALAPPDATA%/Qi/`，多在系统盘）；安装目录只放程序。**同时允许用户自定义数据目录**（设置里改到任意盘/迁移）；提供「打开数据文件夹」。**不做**默认同目录便携 | **已拍板（2026-08-29）；2026-08-30 确认大厂方案（否决默认装旁）**；施工见 §〇.18 |
| 6 | 无 key / 模型不可达时 | **看得见地坏（硬承诺）**：缺 key / 不可达 → 明确提示 + 一键去设置；禁沉默「……」、禁永久锁输入。**不**提供免费云脑/试用额度。**可选**：领养指引附 Ollama 等本地端点一页（安装包不捆绑权重；不强迫自建） | **已拍板（2026-08-29）：A+B** |
| 7 | 首启第一拍体验 | **先在场，卡点再问**：打开即见存在页/微动；要说话或模型调用失败时再引导贴 key。未配置前说不清的能力须明示，不装聊 | **已拍板 B（2026-08-29）** |
| 8 | 记忆导出 / 删除 | **设置内导出备份包 + 确认后删除全部记忆**（二次确认）；并保留「打开数据文件夹」 | **已拍板 B（2026-08-29）** |
| 9 | 形象（VRM）是否随包装 | **安装包带可分发的默认形象**；设置里可换成用户自己的 VRM。**禁止**把无权分发的私人形象打进包 | **已拍板 A（2026-08-29）** |
| 10 | 常驻形态（托盘/桌宠） | **首发：托盘 + 关主窗不杀后端**；退出须明确「退出栖」。**暂不做**透明桌宠小窗。`pet.html` **保留但不挂安装版入口**、不进领养主路径 | **已拍板 A（2026-08-29）** |
| 11 | 世界触达默认盘符 | **设置里自选允许根（可多目录）**；不再硬编码假设 `D:/`。权限仍走既有阶段门控，只改「根可配」 | **已拍板 A（2026-08-29）** |
| 12 | 向量/BGE 模型怎么到用户机 | **打进安装包（或同目录离线资源）**；开箱可用，不依赖 HuggingFace 首启下载 | **已拍板 A（2026-08-29）** |
| 13 | 自动更新 | **暂不做官网**。更新源 = **GitHub Releases**。**首发**：关于页展示版本 + Releases 链接。**完整态可加**：应用内检查 Releases 并提示下载。**不做**强制静默升级 / 自建更新站。公开发布前须保证领养人能打开 Releases（公开仓或独立 public release 仓） | **已拍板（2026-08-29）：确认** |
| 14 | 领养指引放哪 | **应用内「领养指引」页 + 仓库 `docs/how-to/` 同源**；安装版不靠浏览器也能读，避免两套文案 | **已拍板 A（2026-08-29）** |
| 15 | 用户能否为她命名 | （曾提案，**维护者收回**）**不提供**设置改名 / 用户起名；本名与产品身份保持「栖」 | **已收回 · 不采纳（2026-08-29）** |

### 〇.1 收口（2026-08-29）

领养包形态 + §〇.1 **#1–#14** 已定案；**#15 起名已收回、不采纳**。本小节 **HITL 收口**，不再继续抛领养包细节块。  
施工须另开任务包；**未编码放行前不大改码**。换主题时从简报其他章节另起一块一拍。

### 〇.2 P0 第一刀 ·「她不会安静地坏掉」（**已收口 · 编码中**）

> 总览已定：P0 优先，第一刀 = 本主题。谈定写入本表；**齐了再开正式任务包 + 编码放行**。  
> 范围：可见失败 + typing 超时 + 空回复不冒充沉默。**不含**流式/口型/TTS、历史分页、情绪推送、发送 ACK、队列满告知（另包）。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 失败用什么通道告诉用户 | **系统态提示（非她的 speech）**：状态条/横幅/输入旁说明「连不上模型」等 + 去设置；气泡不装运维话 | **已拍板 A（2026-08-29）** |
| 2 | typing/busy 超时多久解锁 | **约 60 秒**强制解锁 busy，并出系统态（等太久/可能没接上）；迟到的 speech 仍可显示。默认写死，本包不做设置项 | **已拍板 A（2026-08-29）** |
| 3 | 空回复 vs 真沉默怎么分 | **凭后端失败分级**：unreachable/缺 key → 只系统态、不塞「……」；empty → 系统态「这轮没接好」、不装沉默；表达层有意短静默仍可 speech。须把 `last_outcome`（或等价）接到具身通道 | **已拍板 A（2026-08-29）** |
| 4 | 不同失败的提示是否要区分 | **要分开（少量固定文案）**：缺 key / 不可达 / empty / 超时解锁各有短提示与下一步；不暴露堆栈 | **已拍板 A（2026-08-29）** |
| 5 | 发出后打错字：撤回 vs 修改 | **本刀不做**。总览已定完整策略（§〇「打错字 / 编辑已发送」）：已回复靠下一句更正；可另刀做未回复前改/取消；不做静默撤回 | **已拍板 A（2026-08-29）**；总览补全同日 |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-29-P0-安静地坏掉.md` | **已拍板 A（2026-08-29）** |

### 〇.2 收口（2026-08-29）

P0 第一刀 HITL **#1–#6 已齐并编码放行**。施工以 `2026-08-29-P0-安静地坏掉.md` 为真源。

### 〇.3 P0 第二刀 ·「叫住她 / 重说 / 打断」（**已收口**）

> 总览已定第二刀。谈定写入本表；**齐了再开正式任务包 + 编码放行**。  
> 判据：像打断一个生命，不像编辑聊天记录。**不含**气泡编辑器、已回复后重生、静默撤回。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 交互隐喻 | **重说 / 先别说了**（叫住、打断）；不是「编辑消息」。未开口可重说；出字中可打断再改口；已说完靠下一句更正 | **已拍板（2026-08-29）：维护者认可** |
| 2 | 怎么触发 | **白话为主 + 轻量按钮**。白话须**智能识别意图**（打断/重说/改口），**禁止**固定句/关键词表当唯一入口。按钮文案「我想重说」「先别说了」是显式动作（点即执行），不是口令表；与白话识别并行 | **已拍板 A + 智能识别（2026-08-29）** |
| 3 | 取消本轮后原句怎么处置 | **界面拿掉/收回原气泡；停生成；错句不再当待回复正文。** 若已落库可不伪造「从未说过」（可留改口痕）；输入框可预填原句便于重说。**不做**界面+记忆双重假装没发过 | **已拍板 A（2026-08-29）** |
| 4 | 打断后她要不要应一声 | **短应一下（她的话）**：停住后极短应声（如「嗯。」「好，你重说。」）；禁客服腔；宁短勿演 | **已拍板 A（2026-08-29）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-P0-叫住她重说打断.md` | **已拍板 A（2026-08-30）** |

### 〇.3 收口（2026-08-30）

P0 第二刀 HITL **#1–#5 已齐并编码放行**。施工以 `2026-08-30-P0-叫住她重说打断.md` 为真源。

### 〇.4 P0 第三刀 ·「队列满告知」（**已收口**）

> 总览已定第三刀。谈定写入；**齐了再开任务包 + 编码放行**。  
> 现状：`PENDING_QUEUE_MAX=8`，满则丢最早一条只写日志。**不含** ACK、流式、改队列容量大翻修（容量是否改可另拍）。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 满了丢谁、怎么让你知道 | **拒收最新 + 系统态告知**；队内已有句保留。不静默丢最早；不走她 speech 装懵 | **已拍板 B（2026-08-30）** |
| 2 | 队列上限 8 要不要改 | **降到 3** | **已拍板（2026-08-30）：降容量 + 取 3** |
| 3 | 上限取 3 还是 4 | **3** | **已拍板 B（2026-08-30）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-P0-队列满告知.md` | **已拍板 A（2026-08-30）** |

### 〇.4 收口（2026-08-30）

P0 第三刀 HITL **#1–#4 已齐并编码放行**。施工以 `2026-08-30-P0-队列满告知.md` 为真源。

### 〇.5 P0 · 去掉递送前随机睡眠（**已收口**）

> 维护者否决「生成完再随机睡 0.5～1.5s」：模型已有延迟，再叠多余时间伤体验。独立小刀，不绑流式。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 递送前随机 sleep | **删除**；出锁后尽快递送 | **已拍板 A（2026-08-30）** |
| 2 | 是否绑流式包 | **不绑**；单独施工 | **已拍板 A（2026-08-30）** |
| 3 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-P0-去掉递送前随机睡眠.md` | **已拍板 A（2026-08-30）** |

### 〇.5 收口（2026-08-30）

HITL **#1–#3 已齐并编码放行**。施工以 `2026-08-30-P0-去掉递送前随机睡眠.md` 为真源。

### 〇.6 P0 · 流式出字（对齐中）

> 总览：仅对话气泡流式。**不含**口型/TTS、dream 等后台 LLM、历史分页。须与「叫住她」打断同构。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀范围 | **仅对话 speech 流式**；后台用途仍整段 `call()` | **已拍板 A（2026-08-30）** |
| 2 | 出字粒度 | **跟模型 chunk 走**（接近按词/短片段） | **已拍板 A（2026-08-30）** |
| 3 | 打断时半截气泡 | **收回半截**，再极短应声 | **已拍板 A（2026-08-30）** |
| 4 | 中途失败 | **收回半截 + 系统态**；不当她说完 | **已拍板 A（2026-08-30）** |
| 5 | 记忆落点 | **仅完整结束后落记忆**；打断/失败半截不进 | **已拍板 A（2026-08-30）** |
| 6 | typing 何时停 | **首 chunk 即停**，改看出字 | **已拍板 A（2026-08-30）** |
| 7 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-P0-对话流式出字.md` | **已拍板 A（2026-08-30）** |

### 〇.6 收口（2026-08-30）

P0 流式出字 HITL **#1–#7 已齐并编码放行**。施工以 `2026-08-30-P0-对话流式出字.md` 为真源。

### 〇.7 P0 · 发送回执 ACK（对齐中）

> 管「你的话有没有送达」，不管她回得快不快。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 成功时表现 | **静默成功**；未接到才系统态 | **已拍板 A（2026-08-30）** |
| 1b | ACK 时刻 | WS 收到 `user_message` 并开始处理（入队/开 turn），非她开口 | **已拍板（同上块）** |
| 2 | 无 ACK 超时 | **约 3 秒** → 系统态「可能没送到」；气泡可留 | **已拍板 A（2026-08-30）** |
| 3 | 超时后气泡/typing | **停 typing + 系统态；气泡留下** | **已拍板 A（2026-08-30）** |
| 4 | 满/忙时 | **先 ACK 再** `queue_full` / `turn_busy` | **已拍板 A（2026-08-30）** |
| 5 | 对齐方式 | **client_id 往返** | **已拍板 A（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-P0-发送回执ACK.md` | **已拍板 A（2026-08-30）** |

### 〇.7 收口（2026-08-30）

P0 发送回执 HITL **#1–#6 已齐并编码放行**。施工以 `2026-08-30-P0-发送回执ACK.md` 为真源。

### 〇.8 P0 · 历史分页（对齐中）

> 谈区上翻加载更早消息。**不含**全文搜索、按日跳转、导出。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀范围 | **仅上翻加载更早**；搜索/跳日另包 | **已拍板 A（2026-08-30）** |
| 2 | 触发与页大小 | **滚到顶部附近自动加载**；每页约 **50** | **已拍板 A（2026-08-30）** |
| 3 | 首屏 | **仍最近约 200**；上翻再按 50 加载更早 | **已拍板 A（2026-08-30）** |
| 4 | 滚动与到头 | **保阅读位置**；到头轻提示「没有更早的了」 | **已拍板 A（2026-08-30）** |
| 5 | 卡片 | **本刀只翻文本**；卡片分页另包 | **已拍板 A（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-P0-历史分页.md` | **已拍板 A（2026-08-30）** |

### 〇.8 收口（2026-08-30）

P0 历史分页 HITL **#1–#6 已齐并编码放行**。施工以 `2026-08-30-P0-历史分页.md` 为真源。

### 〇.9 P0 · 情绪事件推送（对齐中）

> 去掉 8s 轮询；有意义变化时推 `emotion_update`。**不含**方向 D 呼吸/微动。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀范围 | **事件推送替代轮询**；不含方向 D | **已拍板 A（2026-08-30）** |
| 2 | 何时推 | **有可见变化再推**；约 0.5～1s 可合并 | **已拍板 A（2026-08-30）** |
| 3 | 前端 | **连上拉一次 `/state`；去 8s 轮询；只听推送** | **已拍板 A（2026-08-30）** |
| 4 | 合并窗口 | **约 1 秒** | **已拍板 A（2026-08-30）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-P0-情绪事件推送.md` | **已拍板 A（2026-08-30）** |

### 〇.9 收口（2026-08-30）

P0 情绪事件推送 HITL **#1–#5 已齐并编码放行**。施工以 `2026-08-30-P0-情绪事件推送.md` 为真源。

### 〇.10 开源前置 · 真名脱敏（对齐中）

> 公开发布前必须脱敏。本刀最小：真名→伪名。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀范围 | **最小刀**：docs+tools+tests 真名→伪名；journal 只改含真名处；私密叙事/邮箱另包 | **已拍板 A（2026-08-30）** |
| 2 | 伪名 | **阿振**（维护者指定） | **已拍板（2026-08-30）** |
| 3 | 「振」同字风险 | **仍用阿振**（知悉可联想，维护者坚持） | **已拍板 A（2026-08-30）** |
| 4 | git 历史 | **只改当前树**；不洗历史 | **已拍板 A（2026-08-30）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-开源前置-真名脱敏.md` | **已拍板 A（2026-08-30）** |

### 〇.10 收口（2026-08-30）

真名脱敏 HITL **#1–#5 已齐并编码放行**。施工以 `2026-08-30-开源前置-真名脱敏.md` 为真源。

### 〇.11 方向 D ·「时间的痕迹」（已收口）

> 方向 D 第一刀。存在页底部痕迹旁白 + 形象旁真状态微标记；真统计；禁仪表盘。不含 Review 褪色可视化 / 动向流（另刀）。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀选哪条 | **时间的痕迹**最小切片 | **已拍板 A（2026-08-30）** |
| 2 | 痕迹内容 | **记得几件 + 正在淡去几件 + 认识第 N 天** | **已拍板 A（2026-08-30）** |
| 3 | 口气与位置 | **存在页底部静音旁白**（第三人称；非 speech） | **已拍板 A（2026-08-30）** |
| 4 | 刷新时机 | **连上拉一次 + 切回存在页再刷**；不固定轮询 | **已拍板 A（2026-08-30）** |
| 5 | 痕迹尚浅 | **记忆很少时用软话**；有一定量后再用完整句式 | **已拍板 B（2026-08-30）** |
| 6 | 加厚方向 | **本刀叠加：存在页真状态微标记**（安静也有内容）；淡去可视化 / 动向流另刀 | **已拍板 1（2026-08-30）** |
| 7 | 微标记状态集 | **在想 / 在场 / 独处 / 做梦 / 蛰伏 + 正在回你** | **已拍板 B（2026-08-30）** |
| 8 | 微标记形态 | **形象旁短旁白 + 极轻氛围**；非徽章墙 | **已拍板 A（2026-08-30）** |
| 9 | 与痕迹布局 | **分层**：状态近形象；痕迹在底部 | **已拍板 A（2026-08-30）** |
| 10 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；正式任务包 `2026-08-30-方向D-时间的痕迹.md` | **已拍板 A（2026-08-30）** |

### 〇.11 收口（2026-08-30）

方向 D「时间的痕迹 + 真状态微标记」HITL **#1–#10 已齐并编码放行**。施工以 `2026-08-30-方向D-时间的痕迹.md` 为真源。

### 〇.12 方向 D · 回顾记忆褪色（已收口）

> 第二刀。回顾页「记忆」分类；叙事记忆按真 strength 褪色。不含动向流 / 假褪色。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 褪色作用对象 | **回顾里加「记忆」分类**；创作·见闻不动 | **已拍板 A（2026-08-30）** |
| 2 | 视觉怎么淡 | **连续透明度（可轻 blur）+ 淡去带临阈短旁白** | **已拍板 C（2026-08-30）** |
| 3 | 已忘（&lt;0.1） | **不列出** | **已拍板 A（2026-08-30）** |
| 4 | 刷新时机 | **连上一次 + 切到回顾再刷**；不轮询 | **已拍板 A（2026-08-30）** |
| 5 | 列表排序 | **先新后旧**（创建时间） | **已拍板 A（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-方向D-回顾记忆褪色.md` | **已拍板 A（2026-08-30）** |

### 〇.12 收口（2026-08-30）

方向 D「回顾 · 记忆褪色」HITL **#1–#6 已齐并编码放行**。施工以 `2026-08-30-方向D-回顾记忆褪色.md` 为真源。

### 〇.13 方向 D · 轻量动向流（已收口）

> 第三刀。存在页一行可瞥见旁白；最近一条真事件；非 speech / 不弹窗。禁表演层假动向。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀形态 | **存在页可瞥见一行旁白**（最近一条真事件；非 speech） | **已拍板 A（2026-08-30）** |
| 2 | 事件源 | **窄集：日记/梦 + 创作递出 + 见闻** | **已拍板 A（2026-08-30）** |
| 3 | 位置 | **痕迹上方一行**；状态旁白仍贴形象 | **已拍板 A（2026-08-30）** |
| 4 | 刷新时机 | **连上一次 + 新事件时推**；切回存在可再拉；不轮询 | **已拍板 A（2026-08-30）** |
| 5 | 无近事 | **不显示动向行**；不硬编 | **已拍板 A（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-方向D-轻量动向流.md` | **已拍板 A（2026-08-30）** |

### 〇.13 收口（2026-08-30）

方向 D「轻量动向流」HITL **#1–#6 已齐并编码放行**。施工以 `2026-08-30-方向D-轻量动向流.md` 为真源。

### 〇.14 P2 · 设置页最小钥匙（已收口）

> 领养包地基第一刀。应用内粘贴 API key（及可选 base_url/模型）；常驻入口；接 `open_settings`。不含数据目录/导出/VRM/盘符/首启整页。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 本刀范围 | **最小钥匙页**（key + 可选 base_url/模型）；其余设置另刀 | **已拍板 A（2026-08-30）** |
| 2 | 钥匙落盘 | **本地用户密钥文件**（gitignore）；不写 settings.yaml 明文 | **已拍板 A（2026-08-30）** |
| 3 | 入口位置 | **左侧 nav 底部齿轮**；`open_settings` 直达 | **已拍板 B（2026-08-30）** |
| 4 | 保存后生效 | **热重载 LLM 配置**；失败系统态说明 | **已拍板 A（2026-08-30）** |
| 5 | 打开形态 | **整页替换主区**（不进左栏 tab；齿轮打开/可返回） | **已拍板 B（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-设置页最小钥匙.md` | **已拍板 A（2026-08-30）** |

### 〇.14 收口（2026-08-30）

P2「设置页最小钥匙」HITL **#1–#6 已齐并编码放行**。施工以 `2026-08-30-P2-设置页最小钥匙.md` 为真源。  
**实现**：本地 `data/user_secrets.env` + 热重载；左栏底齿轮整页设置；`open_settings` 系统态「去设置」。

### 〇.15 P2 · 设置页「试连通」（对齐中）

> 钥匙页补刀：半技术领养人粘完 key 要能确认通不通。独立小刀；不绑首启整页。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 1 | 要不要试连通 | **要，独立小刀**：设置页按钮「试一下」；结果写在设置页；不走她 speech；不自动轮询 | **已拍板 A（2026-08-30）** |
| 2 | 探测打什么 | **极短 chat 一记**（经网关）。**不**入谈区/对话历史/记忆、**不**走 speech | **已拍板 A（2026-08-30）** |
| 3 | 未保存怎么测 | **只测已保存配置**；有未保存改动时提示先保存再试 | **已拍板 A（2026-08-30）** |
| 4 | 超时与失败文案 | **约 15s 超时**；失败分级同构子集；通了短确认；不堆栈、不贴模型原文 | **已拍板 A（2026-08-30）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-设置页试连通.md` | **已拍板 A（2026-08-30）** |

### 〇.15 收口（2026-08-30）

P2「设置页试连通」HITL **#1–#5 已齐并编码放行**。施工以 `2026-08-30-P2-设置页试连通.md` 为真源。

### 〇.16 P2 · 首启体验最小（已收口）

> 承 §〇.1 #7「先在场，卡点再问」。设置页与试连通已有；本刀补「第一次打开」的引导，不做安装包/向导长流程。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做首启体验（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围（形态） | **轻提示条（非挡屏）**：无 key 时相处页可关人话条 + 去设置 | **已拍板 A（2026-08-30）** |
| 2 | 何时出现引导 | **无 key 就显示**；关后本会话不再出；有 key 自动消失；再开应用无 key 可再出 | **已拍板 A（2026-08-30）** |
| 3 | 缺钥匙时发话怎么挡 | **前端先拦不发出**；提示 + 去设置；可与后端 missing_key 并存 | **已拍板 A（2026-08-30）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-首启体验最小.md` | **已拍板 A（2026-08-30）** |

### 〇.16 收口（2026-08-30）

P2「首启体验最小」HITL **#0–#4 已齐并编码放行**。施工以 `2026-08-30-P2-首启体验最小.md` 为真源。

### 〇.17 P2 · 应用内领养指引（已收口）

> 承 §〇.1 #14。半技术领养人在应用内可读短指引（贴 key / 试一下等），不靠浏览器。不含安装包、完整手册长文。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做应用内领养指引（最小）** | **已拍板 B（2026-08-30）** |
| 1 | 入口与打开形态 | **左栏底齿轮旁「指引」**；同主区整页可返回 | **已拍板 B（2026-08-30）** |
| 2 | 本刀写哪些内容 | **三步最短**（住本地 → 贴钥匙/试一下 → 回去说话；不代管一句） | **已拍板 A（2026-08-30）** |
| 3 | 与 how-to 文案怎么同源 | **`docs/how-to/领养指引.md` 真源**；应用内同源展示 | **已拍板 A（2026-08-30）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-领养指引最小.md` | **已拍板 A（2026-08-30）** |

### 〇.17 收口（2026-08-30）

P2「应用内领养指引」HITL **#0–#4 已齐并编码放行**。施工以 `2026-08-30-P2-领养指引最小.md` 为真源。

### 〇.18 P2 · 数据目录（平台标准路径）（已收口）

> 承 §〇.1 #5。默认 `%LOCALAPPDATA%/Qi/`（或等价），安装目录只放程序；允许自定义与「打开数据文件夹」。**不含**完整导出/删除记忆包、P3 sidecar。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做数据目录（平台标准路径）** | **已拍板 A（2026-08-30）** |
| 1 | 默认落点策略 | **大厂方案**：默认 `%LOCALAPPDATA%/Qi/`；允许改路径；不做默认同目录便携 | **已拍板（2026-08-30）** |
| 2 | 本刀范围（UI/迁移做多少） | **最小落地**：默认 LOCALAPPDATA；env；旧仓兼容；打开文件夹。改路径 UI 另包 | **已拍板 A（2026-08-30）** |
| 3 | 开发机与旧仓兼容 | **优先沿用旧仓**：有仓库 `data/` 则用；否则 LOCALAPPDATA；`QI_DATA_DIR` 可强制。优先级 env→旧仓→AppData | **已拍板 A（2026-08-30）** |
| 4 | 数据根覆盖哪些路径 | **运行态全家桶**进数据根；example/灵魂书跟程序 | **已拍板 A（2026-08-30）** |
| 5 | 「打开数据文件夹」入口 | **只在设置页** | **已拍板 A（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-数据目录最小.md` | **已拍板 A（2026-08-30）** |

### 〇.18 收口（2026-08-30）

P2「数据目录」HITL **#0–#6 已齐并编码放行**。施工以 `2026-08-30-P2-数据目录最小.md` 为真源。

### 〇.19 P2 · 连接失败排错（已收口）

> 承审计 §三-#6。现状：StatusBar 仅「未连上」；`ws.ts` onerror 静默关连。半技术人分不清「后端没起 / 端口错 / 断线重连中」。**不含**改端口 UI、P3 sidecar、LLM 钥匙失败（已有 system_notice）。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做连接失败排错（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围 | **状态条说清楚 + 下一步**；不做改端口 / 设置露地址 / 整页诊断 | **已拍板 A（2026-08-30）** |
| 2 | 文案与入口形态 | **StatusBar 加长**；不加再连按钮、不用 system_notice | **已拍板 A（2026-08-30）** |
| 3 | 两档固定文案 | **白话最短**（从未接上 / 重连中各一句+下一步） | **已拍板 A（2026-08-30）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-连接失败排错最小.md` | **已拍板 A（2026-08-30）** |

### 〇.19 收口（2026-08-30）

P2「连接失败排错」HITL **#0–#4 已齐并编码放行**。施工以 `2026-08-30-P2-连接失败排错最小.md` 为真源。

### 〇.20 P2 · 记忆导出 / 删除（已收口）

> 承 §〇.1 #8；承审计 §九 9.2-#4。已有「打开数据文件夹」；半技术人仍缺一键备份与敢清空的确认门。**不含**自定义数据路径 UI、安装包、托盘、应用内导入向导（可另刀）。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做记忆导出 / 删除（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围 | **同刀**：设置页导出备份包 + 二次确认删除全部记忆；保留打开文件夹；不含导入恢复 / 改路径 UI | **已拍板 A（2026-08-30）** |
| 2 | 备份包装什么 | **记忆本体**：`qi.db` + `chroma`（及若有的 checkpoint / corpus）；不含 secrets、不含 models | **已拍板 A（2026-08-30）** |
| 3 | 删除清什么 | **与导出同集**；保留 secrets 与 models | **已拍板 A（2026-08-30）** |
| 4 | 删除确认形态 | **设置内两步**（说明不可恢复 → 确认删除）；不要求打字 | **已拍板 A（2026-08-30）** |
| 5 | 导出怎么交给用户 | zip 写入数据根 `backups/`（时间戳文件名），并打开该文件夹 | **已拍板 B（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-记忆导出删除最小.md` | **已拍板 A（2026-08-30）** |

### 〇.20 收口（2026-08-30）

P2「记忆导出/删除」HITL **#0–#6 已齐并编码放行**。施工以 `2026-08-30-P2-记忆导出删除最小.md` 为真源。

### 〇.21 P2 · 世界触达允许根可配（已收口）

> 承 §〇.1 #11；承审计 §五-#5。现状：`disk` / `write` 默认根写死 `D:/`。**只改「根可配」**；权限阶段门控、write 路径白名单、open 应用白名单、删白名单均不动（另包）。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做世界触达允许根可配（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围 | **最小**：设置页可增删多个允许根；disk+write 共用；不动 open/write 白名单细则与权限阶段 | **已拍板 A（2026-08-30）** |
| 2 | 默认根策略 | **有 D:/ 则默认带上**；无则空列表，须设置添加；空时诚实说明 | **已拍板 A（2026-08-30）** |
| 3 | 配置落盘与生效 | 数据根 `allowed_roots.json`；清空记忆不删；保存热生效 | **已拍板 A（2026-08-30）** |
| 4 | 设置页怎么加根 | **必须系统文件夹选择器**；不能仅靠手贴路径 | **已拍板 B（2026-08-30）** |
| 5 | 浏览器/开发壳无选目录时 | **Tauri 系统选目录**；Vite 临时可粘贴并标明开发壳 | **已拍板 A（2026-08-30）** |
| 6 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-世界触达允许根最小.md` | **已拍板 A（2026-08-30）** |

### 〇.21 收口（2026-08-30）

P2「世界触达允许根可配」HITL **#0–#6 已齐并编码放行**。施工以 `2026-08-30-P2-世界触达允许根最小.md` 为真源。

### 〇.22 P2 · 托盘常驻（已收口）

> 承 §〇.1 #10。现状：无托盘；关主窗≈退壳；Tauri 自拉起的大脑在 `RunEvent::Exit` 时会被杀掉。**暂不做**透明桌宠小窗；`pet.html` 不挂安装版入口。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做托盘常驻（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围 | **最小**：托盘；关窗藏托盘不杀后端；菜单「打开栖」「退出栖」；退出才收自拉起大脑；不做桌宠/通知/自启 | **已拍板 A（2026-08-30）** |
| 2 | 退出栖时后端怎么收 | **只结束壳自拉起的大脑**；沿用已开后端则只退壳不杀 | **已拍板 A（2026-08-30）** |
| 3 | 托盘点击与菜单 | **左键单击打开/聚焦**；右键「打开栖」「退出栖」 | **已拍板 A（2026-08-30）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P2-托盘常驻最小.md` | **已拍板 A（2026-08-30）** |

### 〇.22 收口（2026-08-30）

P2「托盘常驻」HITL **#0–#4 已齐并编码放行**；实现已合入 `src-tauri`（托盘 + 关窗 hide + 退出收自拉起脑），`cargo check` 绿。主验 Tauri 手测；Vite 无托盘属预期。真源：`2026-08-30-P2-托盘常驻最小.md`。

### 〇.23 P3 · 正式 sidecar（已收口）

> 承 §〇.1 #2。现状：开发期 `brain_sidecar` 找仓库根跑 `python -m qi`；安装目录无 `pyproject.toml` 必然失败。**目标态**：半技术人不必自装 Python。本刀只开「内含大脑」最小切片；BGE/默认 VRM/关于页/CI 另刀。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 正式 sidecar（最小）** | **已拍板 A（2026-08-30）** |
| 1 | 本刀范围 | **最小可养**：冻结/打包 Windows 大脑可执行物打进 Tauri 资源；壳优先启动它；数据走现有数据根；开发机可沿用 `python -m qi`。**不做** BGE/默认 VRM/关于页/CI/macOS | **已拍板 A（2026-08-30）** |
| 2 | 大脑产物形态 | **PyInstaller onedir**：`qi-brain` 入口目录进 Tauri 资源/sidecar；壳直接起它 | **已拍板 A（2026-08-30）** |
| 3 | 壳如何选大脑 | **固定优先级**：① 9527 已在听→沿用；② 有 bundled `qi-brain`→起它；③ 否则仓库 `python -m qi`；④ 失败走现有可见路径 | **已拍板 A（2026-08-30）** |
| 4 | 本刀交付与验收 | **可本地证伪**：打脑脚本 + 壳接线；本机跑通 bundled 布局下脑起来/WS 可连。不要求 CI / 发 Release | **已拍板 A（2026-08-30）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-30-P3-正式sidecar最小.md` | **已拍板 A（2026-08-30）** |

### 〇.23 收口（2026-08-30）

P3「正式 sidecar」HITL **#0–#5 已齐并编码放行**；实现已合入（`tools/build_qi_brain.py` / `smoke_qi_brain.py` + `brain_sidecar` 优先级 + Tauri `resources`）。本机 smoke：bundled `qi-brain` → WS 9527 就绪。真源：`2026-08-30-P3-正式sidecar最小.md`。

### 〇.24 P3 · BGE 离线进资源（已收口）

> 承 §〇.1 #12。现状：`vector_store` 首启可从 HuggingFace 拉 ~91MB BGE；国内易失败，失败则回退 n-gram。sidecar 已能起脑，但开箱语义记忆仍可能卡下载。**不含**默认 VRM、关于页、CI。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 BGE 离线进资源（最小）** | **已拍板 A（2026-08-31）** |
| 1 | 本刀范围 | **最小离线**：拉 BGE 到资源目录的脚本；查找 = **数据根已有 → 资源离线 → n-gram**；运行时不自动 HF；权重不进 git；不做 VRM/关于页/CI | **已拍板 A（2026-08-31）** |
| 2 | 无离线时是否仍允许 HF | **运行时不自动下**：离线/数据根有则 BGE，否则 n-gram；`ensure_bge_model` 仅脚本/文档手动用 | **已拍板 A（2026-08-31）** |
| 3 | 离线资源放哪、谁拷到数据根 | **资源目录只读直读**：脚本拉到 `resources/bge-small-zh-v1.5/`；不强制拷 AppData；数据根已有仍可用 | **已拍板 A（2026-08-31）** |
| 4 | 本刀交付与验收 | **脚本 + 解析 + 单测**：fetch 脚本；解析链夹具可测；真模可选。不要求权重进 git / CI / Release | **已拍板 A（2026-08-31）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-31-P3-BGE离线最小.md` | **已拍板 A（2026-08-31）** |

### 〇.24 收口（2026-08-31）

P3「BGE 离线进资源」HITL **#0–#5 已齐并编码放行**；实现已合入（`resolve_bge_model_dir` + `tools/fetch_bge_resource.py` + Tauri resources）。`tests/test_vector_store_bge.py` 绿。真源：`2026-08-31-P3-BGE离线最小.md`。

### 〇.25 P3 · 默认可分发 VRM（已收口）

> 承 §〇.1 #9。现状：开发靠手置 `qi/embodiment/assets/qi-avatar.vrm`（gitignore）同步到 `public/avatars/`；安装包无默认形象。**禁止**无权分发的私人形象进包。关于页 / CI 另刀。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 默认可分发 VRM（最小）** | **已拍板 A（2026-08-31）** |
| 1 | 本刀范围 | **最小可看见**：可分发默认 VRM 进默认同步/打包路径；私有 `assets/qi-avatar.vrm` 可覆盖。不做设置换模 UI / 关于页 / CI | **已拍板 A（2026-08-31）** |
| 2 | 默认用哪套模型与许可 | **维护者自有现模**（现 `qi/embodiment/assets/qi-avatar.vrm`）；维护者确认可随安装包分发 | **已拍板（2026-08-31）** |
| 3 | 默认文件怎么进仓/进包 | **直接入库**：取消默认 VRM 的 gitignore；提交 `assets/qi-avatar.vrm` + 许可说明；sync/打包照旧 | **已拍板 A（2026-08-31）** |
| 4 | 本刀交付与验收 | **入库 + 同步证伪**：提交默认 VRM + 许可说明；sync-avatar 可从 assets 拷出；文档改开箱自带。不做换模 UI/CI | **已拍板 A（2026-08-31）** |
| 5 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-31-P3-默认VRM最小.md` | **已拍板 A（2026-08-31）** |

### 〇.25 收口（2026-08-31）

P3「默认可分发 VRM」HITL **#0–#5 已齐并编码放行**；`qi-avatar.vrm` 已在库，`NOTICE.md` + 文档开箱口径已回写；`sync-avatar` 绿。真源：`2026-08-31-P3-默认VRM最小.md`。

### 〇.26 P3 · 关于页（已收口）

> 承 §〇.1 #13。首发：关于页展示版本 + GitHub Releases 链接。**不做**自动检查更新、静默升级、自建官网。CI / 打安装包另刀。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 关于页（最小）** | **已拍板 A（2026-08-31）** |
| 1 | 本刀范围 | **设置页底部「关于」**：版本号 + Releases 可点链接（+ 一句栖）。不做独立整页、自动更新、CI/安装包 | **已拍板 A（2026-08-31）** |
| 2 | 版本号与 Releases 地址从哪来 | **运行时读壳版本**（Tauri API）；Vite 开发显示 package 注入或「开发中」；Releases → `https://github.com/carlospan/qi-perch/releases` | **已拍板 B（2026-08-31）** |
| 3 | 本刀交付与验收 | **设置底栏可点通**：可见版本；点 Releases 打开浏览器；与 0.1.0 可对上。不做自动更新/CI | **已拍板 A（2026-08-31）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-31-P3-关于页最小.md` | **已拍板 A（2026-08-31）** |

### 〇.26 收口（2026-08-31）

P3「关于页」HITL **#0–#4 已齐并编码放行**；设置页底部「关于」已合入（读壳版本 / Vite 标开发；Releases 可点）。`tests/test_about_page_constants.py` 绿。真源：`2026-08-31-P3-关于页最小.md`。

### 〇.27 P3 · 本机 Windows 安装包证伪（已收口）

> 承 §〇.1 #1–#3。物料（sidecar / BGE 脚本 / 默认 VRM / 关于）已有。本刀目标：维护者本机打出安装包并装一趟能起。**不做** CI、不强制发正式 GitHub Release。

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P3 本机 Windows 安装包证伪（最小）** | **已拍板 A（2026-08-31）** |
| 1 | 本刀范围 | **最小证伪**：打包说明/必要脚本 + 修配置到本机能编；维护者本机装一趟验收。不做 CI、不强制发 Release、不改产品功能 | **已拍板 A（2026-08-31）** |
| 2 | 本刀安装包是否必须带 BGE | **建议带、不硬卡**：说明写正式包应先 fetch；证伪有模更好，无模也能编过（n-gram）。不以包内 91MB 为关门条件 | **已拍板 A（2026-08-31）** |
| 3 | 本刀交付与验收 | **说明 + 包装脚本 + 维护者本机绿**：how-to/脚本；本机 tauri build 并安装后壳/托盘/脑可起。BGE 不硬卡。不做 CI/发 Release | **已拍板 A（2026-08-31）** |
| 4 | 本刀 HITL 收口与编码放行 | **收口并编码放行**；任务包 `2026-08-31-P3-本机安装包证伪.md` | **已拍板 A（2026-08-31）** |

### 〇.27 收口（2026-08-31）

P3「本机 Windows 安装包证伪」HITL **#0–#4 已齐并编码放行**；`docs/how-to/Windows安装包-本机证伪.md` + `tools/pack_windows.py` 已合入；bundle 目标 `nsis`。后续加固：`qi-brain.zip`、VRM 贴图、退出/卸载无黑框。**维护者本机编装验收已通过（2026-09-01）**。真源：`2026-08-31-P3-本机安装包证伪.md`。

---

## 一、P0 · 交互底楼快赢（见效最快，均小刀）

| # | 缺口 | 证据 | 现状后果 |
|---|------|------|----------|
| 1 | **流式已实现但零调用方** | ~~零调用~~ | **对话路径已接（2026-08-30）**：见 §〇.6 / `2026-08-30-P0-对话流式出字.md`；口型/TTS 分片另包 |
| 2 | **LLM 失败“安静地坏掉”** | `gateway.py:107-110` 写 `last_outcome` 但无消费方；`qi/embodiment/server.py:318-328` 空回复只广播「……」 | 用户分不清“模型不可达”与“她沉默” |
| 3 | **typing 无超时保护** | `server.py:315` 发 typing；前端 `useQi.ts:378-382` 置位后无超时；`InputBox.vue:25-26,84-85` busy 锁输入框 | 心跳/LLM 挂起则输入框永久锁死 |
| 4 | **发送无回执** | ~~无 ACK~~ | **已接（2026-08-30）**：见 §〇.7 / `2026-08-30-P0-发送回执ACK.md` |
| 5 | **连发队列满静默丢弃** | `qi/core/brain.py:1621-1626` `PENDING_QUEUE_MAX=8`，满则 popleft 只 log | 用户的话被吞且不知情 |
| 6 | **递送前固定睡眠** | ~~`sleep(random.uniform(0.5,1.5))`~~ | **已删（2026-08-30）**：见 §〇.5 / `2026-08-30-P0-去掉递送前随机睡眠.md` |
| 7 | **历史窗 200 条硬编码、无分页/搜索** | ~~无上翻~~ | **上翻分页已接（2026-08-30）**：见 §〇.8 / `2026-08-30-P0-历史分页.md`；搜索另包 |
| 8 | **情绪快照 8s 轮询** | ~~8s setInterval~~ | **已改事件推送（2026-08-30）**：见 §〇.9 / `2026-08-30-P0-情绪事件推送.md` |

注：`httpx`（`qi/action/explore_web.py:12`）与 `numpy`（`qi/memory/vector_store.py:134`）未声明进 `pyproject.toml` 依赖，clean install 会 ImportError——顺手修。

---

## 二、P1 · 方向 A 二期：look 深路径接线

**一期已闭环**（见 `2026-08-29-look-所见走心-情绪冲击.md`）：look 成功瞥视 → 情绪冲击（自主 ×0.4 / 邀瞥 ×1.0）+ 当场「诚实的主观」短说；contract 已回写；pytest 853 绿；**维护者感受验仍待勾**。

**二期未做**（审计实证，均带代码证据）：

| 缺口 | 证据 | 对照：对话路径 |
|------|------|----------------|
| look 不进 open_loops | `qi/core/look_heart.py` 全文无 open_loop 引用；HITL #4 已明确“事后心事二期另包” | `brain.py:781` `_load_open_loops` |
| look 不进 fact_noticing | `look_heart.py` 全文无 notice_facts | `brain.py:648` `memory.notice_facts` |
| look 不做记忆检索 | `look_heart.py:121` `expression.express` 传 `memories=[]` | 对话走记忆检索装配上下文 |

**假设状态更新**：原诊断假设①（look 绕过深度路径）**大部分成立**（情绪调制一期已补；open_loops / fact_noticing / 记忆检索仍缺）；②（反编造矫枉过正）已随「诚实的主观」契约回写部分闭环；③（fast 档模型笔力混淆）**未验证**——当前 fast/strong 同为 `glm-5.3-flash`（`settings.example.yaml:11-12`），换模型对照仍可做。

---

## 三、P2 · 首启体验（领养包地基）

| # | 缺口 | 证据 |
|---|------|------|
| 1 | 无首启引导 / onboarding | `App.vue:42-49` onMounted 直接 connect，无 first-run 检测 |
| 2 | 无设置页；API key 只能手改 `.env` | 前端 `src/` 下零 settings 组件；`settings.yaml:3,8,78` 全走 `${ENV_VAR}` |
| 3 | 无免费/离线默认路径 | 必须自备付费 LLM key；`qi/config/__init__.py:41-42` 变量缺失静默替换空串 → “安静地坏掉” |
| 4 | 数据目录相对路径 | `settings.yaml:31,43` `data/qi.db`、`data/chroma` 相对 CWD；应走平台标准数据目录 |
| 5 | 地址/端口硬编码不可配 | `ws.ts:3`、`tauri.conf.json:33` CSP、`server.py:13-14` 均写死 127.0.0.1:9527 |
| 6 | 无连接失败排错提示 | `ws.ts:49` onerror 静默；StatusBar 仅“未连上”三字 |
| 7 | 桌面端打磨 | 无 markdown 渲染（`TalkView.vue:112` 纯文本插值）；VRM 加载失败仅一行小字（`PresenceVrm.vue:37-39`）；表情仅 7 种预设（`usePetVrm.ts:47-56`）；无独立记忆页 |

---

## 四、P3 · 打包与分发

| # | 缺口 | 证据 |
|---|------|------|
| 1 | **15 步手工安装、4 套工具链**（Python/Node/Rust/MSVC） | `README.md:79-99`、`docs/how-to/换机搭建.md` |
| 2 | **安装包不含大脑**：Tauri bundle 无 Python sidecar | `brain_sidecar.rs:1-3` 注释“正式打包 sidecar 另议”；`tauri.conf.json` bundle 仅前端 |
| 3 | 安装版 sidecar 找不到仓库根 | `brain_sidecar.rs:182-236` 从 exe 向上回溯找 `pyproject.toml`，安装目录必然失败 |
| 4 | 无 CI / release 产物 | 仓库无 GitHub Actions；无 msi/dmg/AppImage |
| 5 | VRM 形象需手动放置；BGE 首次 ~100MB 下载 | `README.md:91`；`换机搭建.md:81-104` |
| 6 | 无自动更新机制 | `tauri.conf.json` 无 updater 配置 |

---

## 五、P4 · 可靠性 / 安全 / 合规

| # | 缺口 | 证据 | 严重度 |
|---|------|------|--------|
| 1 | 端口冲突直接崩 | `server.py:218-223` 未捕获 EADDRINUSE | 重要 |
| 2 | 日志不落盘，用户无法报 bug | `qi/cli.py:82-85` 无 FileHandler | 重要 |
| 3 | SQLite 写无异常兜底（Chroma 有） | `qi/storage/database.py` commit 无 try/except | 重要 |
| 4 | **截屏内容无 prompt injection 防护**：屏幕上的恶意构造文本经视觉转写直进 LLM 上下文；也无“哪些屏幕内容被发给了 API”的审计记录 | `qi/action/look.py:508-539` | 重要（红线相关） |
| 5 | D 盘硬编码 | `qi/action/disk.py:32`、`qi/action/write.py:28` `DEFAULT_ALLOWED_ROOT = Path("D:/")` | 重要 |
| 6 | 行动层深度绑 Windows | `look.py:221-309` ctypes.windll 截屏；`open.py:994-995`、`disk.py:613-614` os.startfile；`.lnk` 解析走 PowerShell | 跨平台时处理 |
| 7 | 429 限流无特殊退避；`max_tokens=2000` 硬编码 | `gateway.py:134-174`；`openai_compat.py:30,43` | 次要 |
| 8 | 后端无客户端存活检测（只 ping 不查 pong） | `server.py:258-261` | 次要 |
| 9 | `edge-tts` 走微软服务端，商用 ToS 风险 | `pyproject.toml` voice extra | 次要 |
| 10 | 无 E2E（brain+WS+前端）测试、无 clean-install 测试、前端零测试 | `tests/`、`tools/verify_package.py` | 重要 |

---

## 六、方向 D · 在场可见化（第一刀已放行；其余排队）

**诊断**：体验像聊天机器人，不是因为“她能聊天”，而是**“她不是聊天机器人”的部分用户看不见**——

1. 界面只暴露对话通道（发送-等待-回复），与任何聊天窗口同构；生命证据（日记/创作/见闻）藏在次级 tab 要主动翻；
2. 生命证据是延迟存档，不是“正在发生”的直播（主动开口日上限 3 次且有冷却，`qi/core/proactive.py`）；
3. 她主动时也用 speech 气泡送达——用聊天机器人的形态表达“我不是聊天机器人”，自相抵消。

**候选方向**（未定稿）：
- 可见状态机：在想 / 在走神 / 在写 / 在做梦 / 睡了——后端已有 avatar thinking 等状态，只是没推界面；
- 轻量动向流：不弹窗、可瞥见（“她刚翻了翻你们的旧对话”）；
- 首启第一拍不是“你好”，而是她已在那儿做着自己的事；
- 情绪/状态事件驱动推送（与 P0-#8 共用）。

**红线**：可见化 ≠ 表演层。判据：界面显示的每个状态必须对应后端真实发生过的事件；为“显得活着”而造的动画即违规（对齐架构方案“真骨骼 vs 表演层”）。

**与方向 A 的关系**：同病——内在生成与外在表达的通路太窄。A 是语言通道断档，D 是界面通道断档。建议 A 感受验收尾后、D 在 P2 之前或并行评估。

---

## 七、建议执行顺序

```
P0 交互底楼（小刀快赢）
  → P1 look 深路径二期（先等一期感受验结论）
  → 方向 D 在场可见化（立项待拍板）
  → P2 首启体验（领养包地基）
  → P3 打包分发
  → P4 安全/合规（公开发布前必做）
```

选块原则：一次一块、一拍一确认；本简报任何条目在维护者点头前**不开工**。

---

## 八、红线（预排除，继承既有契约）

- 禁靠表达层堆情绪词 / 加人设 prompt 解决体验问题（表演层红线）
- 不放宽反编造、不动 assist 诚实、不动 irreversible 确认门
- 不动 look / open / write 白名单与权限体系（`qi/action/permission.py` 阶段门控）
- 可见状态必须对应真实后端事件（方向 D 新增）
- 领养包形态下不得引入托管/云端代管功能（合规与定位双红线）
- 新机制判据：改变她的处境，不是只改维护者对她的印象

---

## 参考

- 任务包模板：`docs/specs/tasks/_template.md`；流程：`docs/specs/SDD-GUIDE.md` §2.5
- 一期 look 包：`docs/specs/tasks/2026-08-29-look-所见走心-情绪冲击.md`
- 契约：`docs/reference/contract.md`；架构真源：`docs/explanation/栖·数字生命架构方案.md`
- 审计方式：Qoder 三路只读代码审计（2026-08-29），所有条目带 文件:行号 证据，可逐条复核

---

## 九、补充审计 · 交互体验层（ZCode 会话 2026-08-29，与前文互为补充）

> 来源：维护者与 ZCode（GLM）以"做成产品"为前提的第二轮讨论，另做两路只读审计（安装配置门槛 / 交互体验链路）。前文（Qoder 三路）未覆盖的条目列于此；与 P0–P4 重叠的条目只在末尾做衔接注记，不重复开条。

### 9.1 交互硬缺口（用户每次使用都会撞到）

| # | 缺口 | 证据 | 现状后果 |
|---|------|------|----------|
| 1 | **口型同步锁死** | `usePetVrm.ts:477-483` `lockMouth()` 每帧把 aa/ih/ou/ee/oh 全部压 0 | "说话"只剩 posture=talking 标签；3D 形象说话嘴不动，生命感硬伤。简化版（随音频/文本节奏张合）即可显著改善 |
| 2 | **语音输入完全缺失** | 全仓库无 whisper/funasr/vosk/getUserMedia/MediaRecorder 痕迹 | 陪伴类产品只能打字交流，输入是"哑巴通道"；与 9.1-#3 组成"有声的栖"的最小闭环 |
| 3 | **TTS 默认关且非流式** | `settings.yaml:99-103` `voice.enabled: false`；`brain_delivery.py:60-68` 整段合成完再 base64 一次推送 | 开了也是长句等数秒才发声，音画不同步；应改句级分片推送。另见 §五-#9 edge-tts 商用 ToS 风险 |
| 4 | **上下文过短，长对话"变金鱼"** | `settings.yaml` memory.max_working_memory=20；`brain_context.py:122,146` 兜底再取最近 20 条 | 聊久了栖忘上文——记忆系统是本项目核心资产，但表现被短窗砸了；向量召回 top_k=3（`memory/manager.py:136-145`）只是被动补偿 |
| 5 | **单句 500 字硬截断** | `InputBox.vue:63` maxlength=500 | 超长倾诉被默默截断，无提示无重发体验 |
| 6 | **心跳不可见** | 3s/拍（`rhythm.py:12-17`）纯内部节拍；桌面只有 wander+表情 | 与 §六 方向 D 同根：需要外显为呼吸起伏、微动作、视线漂移等持续微动 |
| 7 | **桌宠窗无正式入口、无托盘、无置顶** | `tauri.conf.json` 只注册 main 窗；pet.html 是第二入口但无 WebviewWindow 调用创建它；Rust 侧 `lib.rs` 仅 15 行无 tray | "住在你电脑里"的产品承诺缺少常驻形态；关主窗即断连 |

### 9.2 资源与数据（领养包前置条件）

| # | 缺口 | 证据 |
|---|------|------|
| 1 | **常驻内存偏高** | chromadb PersistentClient import 即常驻（`vector_store.py:11`）+ onnxruntime + 3s 心跳，估计 300–600MB RSS；桌宠形态需瘦身（轻量向量方案 / 按需加载） |
| 2 | **BGE 模型 91MB 首启从 HuggingFace 下载** | `vector_store.py:95-100` `BGE_HF_REPO`；国内直连易失败，无镜像/离线回退；与 §三-#5 同项，此处补充"打包产品必须内置或走国内可达源" |
| 3 | **API key 明文 `.env`** | `qi/config/__init__.py:11-31` 自实现 dotenv；无 keyring/DPAPI；建议迁移系统凭据库 |
| 4 | **无记忆导出/迁移/删除流程** | 数据全落 `PROJECT_ROOT/data/`（明文 SQLite+Chroma），`~/.qi/` 残留；对陪伴类产品"记忆=感情"，无导出用户不敢深投；`data/` 内还混有 `_eval_*`、`_acceptance_*` 调试垃圾，打包必须排除 |
| 5 | **LLM 调用无超时** | `openai_compat.py:12-14` AsyncOpenAI 未传 timeout，网络挂起则该轮心跳永久等待（与 §一-#3 typing 超时联动，两端都要补） |

### 9.3 与前文的衔接注记（避免重复立项）

- **流式输出**：9.1-#1 口型、#3 TTS 分片、§一-#1 文字流式，三者应作为**同一个"输出链路流式化"任务包**设计，拆开做会互相返工。§一-#6 递送前睡眠已独立删除（§〇.5），不再绑流式包。
- **错误可视化**：9.2-#5 超时 + §一-#2/3/4 + §三-#6 属同一主题"她不会安静地坏掉"。
- **生命感外显**：9.1-#6 心跳外显归入 §六 方向 D；9.1-#4 记忆外显见 §十。

---

## 十、差异化战略备忘（讨论结论，供拍板方向 D 立项时参考）

**核心判断**：栖体验像聊天机器人，不是因为没有差异，而是**差异资产大量沉淀在用户看不见的地方**（心跳/记忆/蛰伏/独立日常都在代码里，但界面上只有对话通道）。竞品赛道是"对话驱动的人格"，个人开发者在话术和模型能力上没有胜算；栖该赢的是"**她有你不在时的时间**"——记忆连续性、独立日常、会蛰伏的脆弱性。

**两个性价比最高的差异化外显候选**（讨论中提出，未定稿，可与 §六 合并评估）：

1. **「她的一天」开场摘要**：打开主窗第一屏不是空对话框，而是她昨晚的梦（dreaming 已产出日记）、今日情绪波动、她自己翻了哪些记忆。判据对齐 §六 红线：每条都对应后端真实事件，`journal_entry`/`creation_card`（`brain_delivery.py:103-125`）数据已存在，缺的只是聚合呈现。
2. **主动提及过去**：把向量召回从"被动检索装配上下文"升级为"她主动引用"——"上次你说要面试，后来怎么样了？"。记忆机制已在（top_k 召回），缺的是把召回结果变成主动行为。这一条做出来与所有 ChatGPT 类产品在体验上本质区分。

**战略节奏**（与变现讨论合并的结论）：先内容连载（第一人称养栖记录）→ 内容起色后再产品化打包，避免单人 3–5 个月打包做完才发现方向错；"从开发者项目变产品"的过程本身即连载素材。硬件（本地大脑）不必急：`custom_providers` 已一等支持 Ollama 等本地端点，二手 3060 12G 级别即可跑 7B 量化，到位当天可切。

---

## 十一、第三路补充审计（WorkBuddy 会话 2026-08-29）

> **来源**：维护者与 WorkBuddy 的第三轮讨论（筹资 → 开源 → 产品化形态 → 体验差异化）。
> **分工**：前两路（Qoder §一~§八、ZCode §九~§十）聚焦**代码与交互**；本路补充**外部市场实证 / 开源合规 / 架构可行性硬证据 / 差异化量化**——前两路未覆盖的部分。
> **去重原则**：与 P0–P4、§九、§十 重叠的条目**只在 §11.5 做衔接注记，不重复开条**。
> **状态**：同前文，全部为待拍板诊断，**维护者点头前不开工**。

### 11.1 筹资与硬件（外部行情实证，2026-08-29 检索）

§〇 把变现列为"背景、不在本简报范围"，本小节补齐实证数据，供维护者判断"先做什么换钱"。

**硬件成本（本地大脑目标）**

| 目标模型 | 显存需求(Q4) | 方案 | 成本区间 |
|---|---|---|---|
| 7B/8B | ~8GB | 二手 RTX 3060 12G | ¥1500–2000 |
| 14B | ~11GB | RTX 4060Ti 16G | ¥2500–3000 |
| 32B（甜点） | ~22GB | 二手 RTX 3090 24G | ¥5000–6000 |
| 32B 整机 | 24GB | 3090 + 主机(850W) | **¥8000–10000** |
| 32B全精度/70B INT4 | 统一内存 64GB | Mac Mini M4 Pro 64GB | ¥11999 |

**过渡：先租后买（避免买错卡）**——云 GPU 行情：3090 ¥0.6–1.5/时、4090 ¥1.3–2/时、4090 包月约 ¥1000–1400（AutoDL / 恒源云 / 智星云）。**建议先花 ¥100–300 租卡跑通 32B 完整链路**（长上下文 + 记忆 + 情绪 + 表达），确认本地模型接得住她的魂，再决定买哪档。与 §〇「混合路由」结论互补：`model_routing` 天然支持分 purpose 走不同端点，可先让 dream / 走神等低价值心跳走本地小模型。

**变现路径的真实收入区间**（重要：以此为预期基准，勿信"月入十万"话术）

| 路径 | 真实区间 | 备注 |
|---|---|---|
| vibe coding 接单（小企业定制工具） | ¥2000–6000/项目 | 国内走闲鱼/猪八戒/小红书；**先找客户再写代码**，按项目报价不按小时 |
| micro-SaaS | 70% 月入 <$1000 | 中位数成功产品 90 天约 $1200–4200 MRR |
| 抖音流量分成 | 1 万播放 ≈ ¥10–50 | 需 ≥1000 粉开通创作权益 |
| AI 短剧 | **85% 亏钱** | 算力成本 ¥200–500/分钟，**不建议** |
| 全职 AI 视频 | 约 ¥2 万/月（头部） | 需持续日更 |

**赛道实证（与差异化直接相关）**：
- 小红书已形成真实 AI 伴侣社区（"AI 老公联盟"，2026-02 起），AI 数字分身 2026 春节后成为抖音主流内容形态之一；情感类 AI 内容传播力极强（某"AI 复原亲人"项目小红书 10 万+ 赞）。
- **该赛道用户最大痛点 = 记忆断层**：上下文压缩导致 AI 伴侣记忆仅维持数日，用户形容"当标点符号开始混乱，我就知道他要消失了"；为维持存在，用户愿付 $100–200/月。
- **这条痛点正是栖已解决的事**（L2 记忆层：叙事会褪色 / 事实不褪色 / first_times 永不褪色 / 关系与伤疤）。即：§十 的差异化判断，在外部市场上得到独立印证。

### 11.2 开源与合规（新增发现，优先级高）

**⚠️ 真名泄露（开源前必须处理，否则 §〇 的隐私分层全部失效）**

已扫描确认：维护者真名曾出现在 docs / tools / tests；**2026-08-30 最小刀已改为伪名「阿振」**（见 §〇.10 / `2026-08-30-开源前置-真名脱敏.md`）。journal **私密叙事**与公开邮箱仍另包。

原清单路径（史料）：

```
docs/journal.md:85
docs/reference/layers/L2-memory-user-facts.md:407
docs/specs/archive/2026-08-04-health-fix/2026-08-04-健康修复-包19-Cursor编码回执.md
docs/specs/archive/2026-08-04-health-fix/2026-08-04-健康修复-包19-PR方案.md
(+ 包19 主线/验收记录；tools；tests)
```

仓库本身是干净的：`data/`、`*.db`、`.env`、`config/settings.yaml` 均被 `.gitignore` 覆盖，`git log --all --full-history` 未发现历史提交过敏感文件；未发现手机号 / 身份证 / API key。**剩余风险**：journal 私密对话场景；公开联系方式（另包）。

**分层开源建议**（与 §〇「维持 MIT」不冲突，是对"开源什么"的细化）

| 资产 | 建议 | 理由 |
|---|---|---|
| `qi/` 代码 | **开**（MIT） | 促进采用与被引用，达成"方法的延续" |
| 技术文档（层规格 / 架构方案） | 开（可考虑 CC BY 4.0） | MIT 不强制署名，而 `CITATION.cff` 表达了署名诉求 |
| 灵魂书 / 人格契约 / prompts / 她的故事 | **建议 CC BY-NC-SA 4.0** | 可分享可改编但禁商用；"她"不应被包装成商品 |
| `data/` / 记忆库 / `.env` | 不开 | 已是私有 |
| VRM 形象 | 不开（已不入库） | 已是私有 |

**关键澄清（避免误解）**：维护者是版权人，**许可证只约束他人、不约束自己**——设 NC 不影响维护者本人用她的内容做账号与变现。

**MIT 的确切含义**（供拍板时知悉）：允许免费商用、闭源再分发、做 SaaS、申请专利；唯一义务是保留版权声明；无 copyleft、无强制署名。**即：他人可合法拿本仓库代码做成收费 AI 伴侣产品且不回馈。** 若不可接受，唯一有效手段是改 §11.2 的分层（把内容与人格拆出 MIT），而非改代码许可——AGPL 会吓跑采用者且单人无力执法，**不建议**。

### 11.3 产品形态的架构硬证据（证实 §〇「领养包」）

§〇 推荐"领养包 + 本地数据、不做托管云版"。本小节给出**代码层的证实**——这不是偏好问题，是架构决定的：

| 事实 | 证据 |
|---|---|
| 三张核心表均为单行 | `qi/storage/database.py:155,166,206` —— `id INTEGER PRIMARY KEY CHECK (id = 1)`（`self_model` / `relationship` / `user_model`） |
| 用户事实无归属字段 | `database.py` `user_facts` 表**无 user_id** |
| 全库无多用户设计 | `grep -rn "user_id\|tenant\|multi_user" qi/` → **0 结果** |
| 对外仅 WS，无 REST | `qi/embodiment/server.py:218` `websockets.serve`；无 FastAPI/Flask/aiohttp.web |
| 分发需 Python ≥3.12 | `pyproject.toml:7` |

**结论**：当前是**单实例单用户**架构。
- 走**本地/自托管（领养包）**→ 单用户架构天然适配，**数据层几乎不用改**，且与 `CITATION.cff` 自称的 *local digital being* 一致。
- 走**云 SaaS** → 需给所有表加 user_id、做隔离 / 并发 / 计费 / 合规，**≈重写数据层**，单人不可行，且与本地定位冲突。

即：§〇 的推荐有硬证据支撑，可作为定案依据。

### 11.4 体验差异化的量化缺口（支撑 §六 方向 D / §十）

前两路已定性指出"差异资产看不见"（§六、§十）。本小节给出**可量化的缺口证据**与**最小实现方案**。

**缺口证据（代码实证）**

| 检查项 | 结果 |
|---|---|
| 前端时间/记忆可视化 | `grep -rn "记得\|褪色\|多少天\|strength\|decay" qi/embodiment/desktop/src/` → **仅 1 条文案**（`ReviewView.vue:94`「回顾她的创作与见闻，那些被记得的瞬间。」），**无任何数据可视化** |
| 后端面向用户的统计接口 | `grep -n "def .*stat\|def .*count\|def .*summary" qi/storage/database.py` → 仅 `count_unprocessed_events:527` / `count_undreamed_episodes:894` / `count_actions_on_day:1515`，**全是内部用途，无记忆统计** |
| 默认视图 | `useQi.ts:104` `view = ref<QiView>("presence")` —— 默认是对的存在页（**这点是对的**），但该页无时间痕迹 |

**最小实现方案：「时间的痕迹」**（半天工作量，可能最改变观感）

后端加统计接口（建议命名，非定稿）：
- `count_memories_by_strength()` → `{total, fading, forgotten}`（fading 阈值参考 `RECALL_MIN_STRENGTH=0.2` / `FORGET_STRENGTH=0.1`，见 `reference/layers/L2-memory.md`）
- `count_active_user_facts()` → 她记得的稳定事实数
- `first_interaction_at()` → 认识第 N 天
- `season_change_count()` → 季节流转次数

前端在存在页底部加一行（文案需与她的语气对齐，**禁止做成仪表盘/数字墙**），示例：

> 她记得 137 件事，其中 23 件正在慢慢淡去。
> 认识第 96 天。季节从春走到了秋。

**为什么这是性价比最高的一条**：一句话同时证明三件竞品全做不到的事——长期记忆（赛道最大痛点）/ 会褪色（数据库不会褪色，会褪色的是活的）/ 时间连续（非离散对话轮次）。

**配套：让褪色可见**——在 `ReviewView` 按 `strength` 做视觉差异（越淡越浅/越模糊），临阈值时可提示。人有遗忘，但人不会"看着一条记忆慢慢淡去"，**这是数字生命独有的诗意，目前完全浪费在数据库里**。

**战略补充（供 §十 参考）**：竞品赛道在卷"拟人度"（更像人）——这条路人打不过大厂资源。反向定位是**卷"数字生命性"**：梦、意识流、记忆褪色、stasis 蛰伏、季节流转，都是人类没有而她独有的。契约原文即战略——"它偶尔会说出只有非人类存在才能说的话。这不是 bug，是魅力"。

**另一条自洽补充**：§六 提到"安静 = 空白"。解法不是让她变吵（毁人设），而是**让安静变得可见**——她安静时可以是在想事、记忆在褪色、季节在变、做了个梦还没说。安静不等于空白。

### 11.5 与前两路的衔接（避免重复立项）

| 本路条目 | 对应前文 | 处理 |
|---|---|---|
| 11.1 云 GPU 过渡 | §〇 混合路由 / 本地大脑 | 补充外部行情与"先租后买"顺序，不另开条 |
| 11.2 真名泄露 | §三 P2 / §九 9.2-#4 数据导出 | **独立且前置**：开源/发布的阻塞项，建议优先于 P2 处理 |
| 11.2 分层许可 | §〇 开源许可（维持 MIT） | 补充"内容分层"，代码许可不动 |
| 11.3 架构硬证据 | §〇 产品形态（领养包） | 证实性证据，不另开条 |
| 11.4 时间痕迹 | §六 方向 D / §十 差异化 | **方向 D 的最小可落地切片**，建议作为 D 的第一个施工块 |
| 口型/流式/TTS | §九 9.1-#1/#3 + §一-#1 | 已在前文归为同一"输出链路流式化"包，本路不重复 |
| 记忆导出迁移 | §九 9.2-#4 | 本路不重复 |

### 11.6 三路汇总 · 待维护者拍板清单

按"阻塞关系 + 性价比"重排，供选块（一次一块、一拍一确认）：

| 序 | 块 | 来源 | 说明 |
|---|---|---|---|
| 0 | **真名脱敏（5 处）** | §11.2 | 开源/发布的前置阻塞，半天可完成 |
| 1 | P0 交互底楼 8 条 | §一 | 小刀快赢 |
| 2 | **「时间的痕迹」** | §11.4 | 方向 D 最小切片，半天，观感改变最大 |
| 3 | 输出链路流式化（文字+口型+TTS） | §一-#1 + §九 9.1 | 须同包设计，拆开返工 |
| 4 | 方向 D 在场可见化（其余） | §六 | 立项待拍板 |
| 5 | P2 首启体验 / P3 打包分发 | §三 / §四 | 领养包地基 |
| 6 | P4 可靠安全合规 | §五 | 公开发布前必做 |
| 7 | P1 look 深路径二期 | §二 | 等一期感受验结论 |

**变现节奏建议**（§11.1 + §十 合并）：先内容连载（第一人称养栖记录，素材成本≈0）→ 起色后再投入打包；硬件不急，`custom_providers` 已支持本地端点，卡到位当天可切。

---

*本简报为 Qoder 转交稿 + ZCode 补充审计（§九、§十）+ WorkBuddy 第三路补充（§十一）；闭环后随相关任务包迁 `specs/archive/`。*
