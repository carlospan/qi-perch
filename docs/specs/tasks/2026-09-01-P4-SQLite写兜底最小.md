# 2026-09-01 · P4：SQLite 写兜底（最小）

> Cursor 单线。HITL 真源：审计简报 §〇.33；承审计 §五-#3。  
> **编码放行**：**已放行 A（2026-09-01）**。

---

## Spec（外部行为）

1. SQLite **写路径**（DML/DDL `execute` + `commit`）统一捕获数据库错误。  
2. 失败：ERROR 日志 + 抛 **`StorageWriteError`**；本刀不做前端专用提示。  
3. **不做**：换库、全量 ORM 重写、读路径包装。

## 实现（步骤）

1. [x] HITL 齐 + 编码放行  
2. [x] `Database._execute` / `_commit` + `StorageWriteError`  
3. [x] 单测 + 针对性 pytest  

## 验收（判据）

- [x] 写 execute / commit 失败抛 `StorageWriteError`（单测）  
- [x] 原有 `tests/test_database.py` 绿  
- [ ] （感受）维护者：模拟写失败时日志可读  

## HITL

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 下一刀选题 | **做 P4 SQLite 写兜底（最小）** | **已拍板 B（2026-09-01）** |
| 1 | 本刀范围 | 统一写路径包装；不换库 | **已拍板 A（2026-09-01）** |
| 2 | 失败后对外表现 | 日志 + `StorageWriteError`；无 UI | **已拍板 A（2026-09-01）** |
| 3 | 本刀 HITL 收口与编码放行 | **收口并编码放行** | **已拍板 A（2026-09-01）** |

## 不含

- system_notice / 前端提示 / 换库  
