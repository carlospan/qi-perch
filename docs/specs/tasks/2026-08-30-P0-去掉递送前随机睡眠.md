# 2026-08-30 · P0：去掉递送前随机睡眠

> Cursor 单线。HITL 真源：审计简报 §〇.5。  
> **编码放行**：维护者 2026-08-30 选 A。

---

## Spec（外部行为）

1. 用户句生成完、有待递送 speech 时：**不再**在推送前额外 `sleep` 随机 0.5～1.5 秒。
2. 出锁后尽快 `_deliver_qi_message`；自然延迟只来自模型生成等真实耗时。
3. 不含：流式出字、口型/TTS、发送 ACK、打断逻辑改动。

## 实现（步骤）

1. [x] 落本任务包；§〇.5 收口
2. [x] 删除 `receive_user_message` 出锁后随机 sleep；清无用 `random` 导入
3. [x] 单测改为断言不 sleep；回写 L1 / code-wiki / 简报 §一-#6
4. [x] 相关 pytest

## 验收（判据）

- [x] 递送路径无 0.5～1.5 随机 sleep
- [x] 相关测绿
- [ ] 维护者感受：回复不再「生成完还愣一下」

## HITL 批点

| 点 | 结论 |
|----|------|
| 递送前随机 sleep | **否决并删除**（生成已有延迟，再叠多余） |
| 施工方式 | 独立小刀，不绑流式 |
| 编码放行 | **已放行（A）** |

## 改动摘要

- `brain.receive_user_message`：出锁后直接递送
- 契约「不秒回」改为不靠人工 sleep；L1 / code-wiki 同步
