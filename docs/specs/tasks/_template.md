# 任务包模板

> 复制本文件改名 `YYYY-MM-DD-任务名.md` 使用。四栏必填。
> **协作分工（固定流程，详见 `specs/SDD-GUIDE.md` 第二节）：本任务包由 CodeBuddy 起草（出方案），Cursor 按 PR 方案落地编码；CodeBuddy 不写 `qi/` 实现代码，Cursor 不重新设计架构。双方过程稿均落盘为文件，Cursor 自行读取目录，无需人工转发。**

---

## Spec（外部行为）
描述这次改动要达成的可观测结果，不写实现细节。
> 本栏由 CodeBuddy 写（方案侧）。

## 实现（步骤）
1. ...
2. ...
> 本栏在「PR 方案」文档中给出精确改动点（CodeBuddy 出方案）；**编码由 Cursor 落地**，不直接在此栏敲实现。

## 验收（判据）
- [ ] 可外部验证的勾选清单
> 由 CodeBuddy 拟清单，Cursor 落地后自查，CodeBuddy 做实施验收。

## HITL 批点
- 哪些节点需维护者拍板
- 路线/阶段退出决策记录
