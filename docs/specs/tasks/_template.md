# 任务包模板

> 复制本文件改名 `YYYY-MM-DD-任务名.md` 使用。四栏必填。  
> **流程（详见 `specs/SDD-GUIDE.md` §2，2026-08-13）**：**Cursor 单线默认**——同一会话起草任务包、落地编码、工程自验、回写文档。维护者只做 HITL（感受 / 路线 / R 红线）。勿再默认走双 Agent 全套过程稿。  
> **人机协作铁律（2026-08-15）**：产品边界未清时，用 **§2.5 渐进对齐（一块一拍）** 填 HITL——谈定立刻写进本包，再编码放行。见 `docs/README.md` 开篇 ★。

---

## Spec（外部行为）
描述这次改动要达成的可观测结果，不写实现细节。
> Cursor（或维护者口述后由 Cursor）落盘。

## 实现（步骤）
1. ...
2. ...
> 可勾选；大改动可另附可选 `-方案.md`。编码与本文同一 Cursor 会话完成。

## 验收（判据）
- [ ] 可外部验证的勾选清单
> Cursor 工程自验；感受项留给维护者。

## HITL 批点
- 哪些节点需维护者拍板（**一块一拍对齐后写入这里**，勿只留在聊天里）
- 路线/阶段退出决策记录
> 可标注倾向供参考；**不**代替维护者拍板。
> 编码放行单独一条；未放行不大改码。