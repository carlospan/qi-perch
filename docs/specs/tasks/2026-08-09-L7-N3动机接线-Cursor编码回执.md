# L7 · N3 动机接线——Cursor 编码回执

> **角色**：Cursor（编码）  
> **依据**：[方案审查回复](./2026-08-09-L7-N3动机接线-方案审查回复.md)（B1/B2 已关，明示可编码）+ 修订后 [PR 方案](./2026-08-09-L7-N3动机接线-PR方案.md)  
> **时刻**：2026-08-09  

---

## 落地

| 项 | 状态 |
|----|------|
| `brain.py`：`last_pressure_response` 初始化 + heartbeat 写入；`tick`/`execute_kind` 传 `pressure` | ✓ |
| `layer.py`：`tick` + `execute_kind` 签名加 `pressure`，传 `action_intentions` / `drift` | ✓ |
| `explore.py`：`drift(..., pressure=None)`；`if not force` 时机门 + 压力；`force=True` 单独软门 `p_force`（下限 0.2） | ✓ B1 |
| `volition.py`：`action_intentions(..., pressure=None)`；explore `pri *= (1-0.3*throttle)` | ✓ |
| `trace.py`：`collect_contenders` 读 `brain.last_pressure_response` 传 `action_intentions` | ✓ B2 |
| 常量 `PRESSURE_THROTTLE_K=0.5` / `PRESSURE_REST_K=0.6`（非 config） | ✓ HITL a/a/a |
| `tests/test_explore_pressure_modulation.py`：None / throttle / rest / curiosity 硬阈 / force 软门拦·放 / force+None / volition / brain 更新 | ✓ |

---

## 构建与测试

| 命令 | 结果 |
|------|------|
| `python -m pytest -q tests/test_explore_pressure_modulation.py` | **9 passed** |
| `python -m pytest -q` 全量 | **515 passed**（≥506） |

---

## 偏离

无实质偏离。B1 按审查回复：force 路径仍过压力软门、极高压下限 0.2；B2 按审查回复：`collect_contenders` 接线。概率测用 `random.random` patch 做确定性断言（避免统计抖动）。

---

## 下一拍

- Trae：实施验收  
- 维护者：相处观察 throttle/rest 高时 explore 是否变少（软调制，不归零）  

---

*Cursor 编码回执 · 2026-08-09 · 515 passed · B1/B2 已落地 · 明示可验收*
