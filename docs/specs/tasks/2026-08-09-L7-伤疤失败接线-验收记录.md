# L7 · 伤疤失败接线——验收记录

> **角色**：Trae（方案 Agent · 实施验收）  
> **依据**：[方案审查回复](./2026-08-09-L7-伤疤失败接线-方案审查回复.md)（B1/B2 已关，明示可编码）+ 修订后 [PR 方案](./2026-08-09-L7-伤疤失败接线-PR方案.md)  
> **编码回执**：[Cursor 编码回执](./2026-08-09-L7-伤疤失败接线-Cursor编码回执.md)  
> **提交**：b3736ec（已推送 origin/main）  
> **时刻**：2026-08-09

---

## 总判

**验收通过。** 代码与修订后 PR 逐项对齐；B1（result dict 补 outcome）+ B2（trust brain 传参）两处修订均已落地；522 passed（独立 `--basetemp` 系统临时区，exit 0）；scope 未越界。

---

## scope 核对（commit b3736ec）

| 文件 | 状态 | 核对 |
|------|------|------|
| `qi/action/layer.py` | M | ✓ import + `_maybe_save_scar` + tick/execute_kind 加 trust + 两处调 _maybe_save_scar |
| `qi/core/brain.py` | M | ✓ tick + execute_kind 两处算 trust + 传参 |
| `qi/action/share.py` | M | ✓ return dict 加 outcome=SUCCESS + summary |
| `qi/action/tend.py` | M | ✓ return dict 加 outcome=SUCCESS |
| `qi/action/explore.py` | M | ✓ return dict 加 outcome=outcome 变量 |
| `qi/action/self_ops.py` | M | ✓ return dict ×2 加 outcome=SUCCESS |
| `tests/test_action_scar_wiring.py` | A | ✓ 7 测含 B1/B2 路径 |

**diff stat**：6 改 + 1 新 + 5 文档（+968）。**未碰** permission.py / save_scar / brain_delivery / 前端 / settings ✓

---

## 代码核对（逐项对齐修订后 PR）

### §0 action return dict 补 outcome（B1 关闭）

| 文件 | 改动 | 核对 |
|------|------|------|
| `share.py` L117-120 | `"outcome": OUTCOME_SUCCESS, "summary": summary` | ✓ summary 便于 origin 摘要（回执说明，非偏离）|
| `tend.py` L88 | `"outcome": OUTCOME_SUCCESS` | ✓ |
| `explore.py` L382 | `"outcome": outcome`（用 outcome 变量，success/failed_capability 都带）| ✓ |
| `self_ops.py` L109/L243 | `"outcome": OUTCOME_SUCCESS` ×2 | ✓ |

B1 关闭：result.get("outcome") 读法现有效 ✓

### §1 `_maybe_save_scar`（layer.py L100-128）

- 读 `result.get("outcome")` → `outcome_creates_scar` 判定 ✓
- severity：`overstepped=0.7` / `failed_judgment=0.3` ✓ HITL a
- origin：`f"[action:{kind}] {summary[:80]}"` ✓
- `save_scar(origin_event, severity, trust_before=float(trust or 0.5))` ✓
- `result["created_scar"] = scar_id` ✓
- `except Exception` 兜底 logger.debug ✓
- logger.info 记录 kind/outcome/scar_id ✓

### §2 layer tick/execute_kind 加 trust（B2 关闭）

- `tick` 签名加 `trust: float = 0.5` ✓
- `execute_kind` 签名加 `trust: float = 0.5` ✓
- tick 调 `_maybe_save_scar(result, chosen.kind, now, trust=trust)` ✓
- execute_kind 调 `_maybe_save_scar(result, kind, now, trust=trust)` ✓

B2 关闭：不挂 relationship，改用 trust 参 ✓

### §3 brain 传 trust

- [brain.py L849-853](file:///d:/qi-perch/qi/core/brain.py#L849) tick 前：`trust = 0.5; if self.relationship is not None: trust = float(getattr(self.relationship.state, "trust", 0.5) or 0.5)` ✓
- L863 tick 传 `trust=trust` ✓
- [brain.py L945-949](file:///d:/qi-perch/qi/core/brain.py#L945) execute_kind 前：同 trust 逻辑 ✓
- L962 execute_kind 传 `trust=trust` ✓

relationship is None 时降级 0.5（防 AttributeError）✓ B2 关闭

### 测试 7 个

| 测 | 核对 |
|----|------|
| `test_maybe_save_scar_failed_judgment` | severity 0.3 + [action:explore] + trust_before 0.62 ✓ |
| `test_maybe_save_scar_overstepped_severity` | severity 0.7 + [action:assist] ✓ |
| `test_success_outcome_no_scar` | save_scar not called + 无 created_scar ✓ |
| `test_failed_capability_no_scar` | save_scar not called ✓ |
| `test_scar_blocks_kind_after_save` | scar_blocks_kind("explore")=True / ("share")=False ✓ |
| `test_execute_kind_overstepped_creates_scar` | execute_kind 路径 + trust 0.71 + severity 0.7 + [action:explore] ✓ B2 |
| `test_return_dict_carries_outcome_success` | §0 return 带 outcome=success 不建疤 ✓ B1 |

7 测全过 ✓

---

## 构建（独立复核）

| 命令 | 结果 |
|------|------|
| `pytest -q --basetemp=%TEMP%\qi_test_tmp` | **522 passed**（exit 0，56.66s）|

---

## 纪律红线

- R1 不破 ✓
- R2（行动克制）：伤疤只阻断，不增 ✓
- LLM 走 gateway（不碰）✓
- 不引入 agent framework（permission/save_scar 既有）✓
- 一次一包 ✓
- permission.py 规则不改（已收口）✓
- save_scar 签名不改（已够用）✓

---

## 偏离确认

无实质偏离。share return 额外带 summary（回执说明便于 origin 摘要，非验收必需）；brain relationship is None 时显式降级 0.5（与审查回复 getattr 等价，防 AttributeError）。均合理。

---

## 剩余（交维护者）

1. **触发源待产出**：现状 outcome 都 SUCCESS，无真实伤疤生成；assist/irreversible 落地时自然产出（用户拒绝确认 → overstepped）
2. **相处观察**：assist 落地后，失败行动是否真正形成伤疤阻断后续同类
3. **可选文档回写**：`L7-action.md` Step 5「实际 save_scar 接线待后续」改为「已接」；`progress.md` 补决策行

---

## 收口状态

- **伤疤失败接线工程交付**：✅ 完成（Step 5 骨架通了，触发源待 assist）
- **L7 三/四整体进度**：N3 ✅ / 伤疤接线 ✅（骨架）/ assist ⏳ / irreversible ⏳

---

*验收记录 · 2026-08-09 · 通过 · 522 passed · B1/B2 已落地 · HITL a/a/b 追认 · 已推送 b3736ec*
