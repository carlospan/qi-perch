# 2026-09-06 · P0 · 口型 + idle（输出链路同包）

> Cursor 单线。承 `2026-09-05-交互形态-从对话到共处.md` 第五层；`2026-08-29` 审计 9.1-#1/#3 + 9.3。  
> **状态**：**已编码放行并实现（2026-09-06）** · HITL #0–#4 齐。  
> **不含**：改左右分栏；心跳外显呼吸；数据模型去对话化；真 TTS 发声；音素级口型；可见胸肩呼吸；预留空 WS 语音事件。

## Spec（外部行为）

1. **说话时嘴在动（#1=A）**：解 `lockMouth` 死锁；随 `speech_delta` **到达节奏**张合；现成 aa/ih/ou 等简单轮换或能量感，非音素级。非流式整段：简版起伏一次即可。
2. **静默 idle（#2=A）**：保留 idle FBX + blink；补**轻量视线漂移**；说话/口型活跃时不抢视线。不为「显得活着」伪造后端事件。
3. **TTS 挂钩（#0=B，#3=A）**：口型由可替换的「输出时钟」驱动——今日接 `speech_delta`，日后 TTS 分片改接同一入口。任务包 + 代码小模块/注释写清；**不新增** WS 语音事件、不改 edge-tts、不默认开 `voice.enabled`。
4. **打断同构**：打断 / 收回流式时口型立刻收住。
5. **布局不变**。

## 实现（步骤）

1. [x] 解 `lockMouth`；`speakClock` 输出时钟 + 接 `speech_delta` / 非流式 `speech`（#1=A，#3=A）
2. [x] idle：轻量视线漂移（#2=A）
3. [x] 契约：`speakClock.ts` 模块头注释（日后 TTS 换时钟源）
4. [x] `tests/test_speak_clock_contract.py` + 手测口径（感受项）
5. [x] 回写交互形态第五层 + 临时批次

## 验收（判据）

- [x] 工程：契约单测绿；`lockMouth` 已移除
- [ ] 流式出字时嘴随 delta 节奏张合；打断后合上（手测）
- [ ] 非流式整段：简版起伏，不永久张嘴（手测）
- [ ] 静默轻量视线漂移；既有 blink/idle 仍在（手测）
- [x] 口型入口可替换（`speakClock` / `pulseSpeak`）；无新 WS 语音事件、无开默认语音
- [x] 不改左右分栏；无胸肩表演呼吸
- [ ] 感受：维护者勾

## HITL 批点

| # | 议题 | 结论 | 状态 |
|---|------|------|------|
| 0 | 本刀范围 | **B**：口型 + idle；TTS 只契约挂钩，不真发声 | **已拍板 B（2026-09-06）** |
| 1 | 口型驱动 | **A**：跟 `speech_delta` 节奏；简单轮换/能量感 | **已拍板 A（2026-09-06）** |
| 2 | idle 微动 | **A**：保留 idle/blink；轻量视线漂移；不做可见呼吸 | **已拍板 A（2026-09-06）** |
| 3 | TTS 契约粒度 | **A**：前端可替换输出时钟入口；不新增 WS 语音事件 | **已拍板 A（2026-09-06）** |
| 4 | 编码放行 | **A**：按 #0–#3 范围开工 | **已拍板 A（2026-09-06）** |

---

## 手测口径

1. `npm run tauri:dev` → 说话看嘴是否跟字动；点「先别说了」嘴是否马上合。
2. 静默几秒：视线是否轻微左右/上下漂（幅度应小）。
3. 确认设置里语音仍默认关；无新音频事件。

## 依据摘要

- 入口：`qi/embodiment/desktop/src/pet/speakClock.ts`
- 接线：`useQi` `mouthPulse`/`mouthClearTick` → `PresenceVrm` → `pulseSpeak`/`clearSpeak`
