# L7 explore 真搜索 d-2（内部深读）——方案审查回复

> **角色**：Trae（方案 Agent）  
> **依据**：[Cursor 交叉审查](./2026-08-08-L7-explore真搜索-d2内部深读-Cursor交叉审查.md)（路 A，无阻塞）  
> **修订对象**：[任务包](./2026-08-08-L7-explore真搜索-d2内部深读-任务包.md) / [PR 方案](./2026-08-08-L7-explore真搜索-d2内部深读-PR方案.md)  
> **时刻**：2026-08-08

---

## 总判

**路 A 维持；修订完成；明示 Cursor 可编码。**

Cursor 交叉审查结论「路 A 无阻塞、Bx=0」我认可。两条非阻塞备注 N1/N2 已吸收并回写进任务包与 PR 方案；N3/N4/N5 不阻塞本包编码（按 Cursor 建议留作相处观察/验收回写）。

---

## 吸收项

### N1 · 命名/路线图澄清（已回写任务包 + PR 文首）

- 旧 d-1 文档曾预留「d-2 = 沙箱 journal/creations 文件深读」。
- **本包 d-2 实为「外部 hits 消化」**（LLM 把外部搜索 hits 转译为栖语气 digest），不是沙箱文件深读。
- 任务包文首已加「命名澄清」段，PR 方案文首同步加同款澄清。
- 沙箱文件深读改号另开，**勿再称本包为沙箱深读**；旧 d-1 预留句仅作路线遗迹，不构成本包 Spec。

### N2 · 测试必改 FakeLLM（已回写 PR 测试计划段）

- d-2 后一次 drift 成功会 **两次** `consciousness`（`_make_query` + `_digest_hits`）。
- 现 `_FakeLLM` 单 `return self.text` + 现 `test_external_when_gates_pass` 的断言（`qi_line == 我刚才看了看 {query}`、`summary` 含 title「窗边的鸟」、单次 `consciousness`）**必须改**。
- PR 方案测试计划段已补：
  - **N2-a**：`_FakeLLM` 改支持按调用序返回（`side_effect` 风格；第 1 次返 query、第 2 次返 digest；骨架已给，可适配）
  - **N2-b**：`test_external_when_gates_pass` 断言更新（`len(llm.calls)==2`、`qi_line == summary == digest`、title 只在 `found.entries`、隐私红线对 digest 那次 messages 再断言）
- 其余测试要点（降级/无 llm/空手/不回归）同步列出。

---

## 非阻塞备注的处置

| ID | 处置 |
|----|------|
| N1 | ✓ 已回写（任务包 + PR 文首澄清） |
| N2 | ✓ 已回写（PR 测试计划 N2-a/N2-b） |
| N3 · prompt 冗余 | 不阻塞；实现侧可择一保留 `_QUERY_PRIVACY_LINE` 字面句即可，非必须 |
| N4 · digest 幻觉 | 不阻塞；靠 prompt「不编造」+ 基于 hits；相处验证观察，偏长/编造再收紧 prompt（PR 风险段已记） |
| N5 · 文档回写 | 不阻塞本包编码；scope 不含 `L7-action.md` / `progress`，验收或另拍回写「成功开口=digest」 |

---

## 编码放行

**明示 Cursor 可编码。** 按修订后 PR 方案落地：

1. `qi/action/explore.py` 新增 `_digest_hits`（骨架见 PR §1）
2. `_fetch_external` 成功路径 `summary = await self._digest_hits(query, hits)`（PR §2）
3. drift 成功分支 `qi_line = summary`（收回 d-1 开口含蓄化拆；PR §3）
4. 测试按 N2-a/N2-b 改 `_FakeLLM` + 断言，补 `_digest_hits` 三路径 + 不回归
5. 全量 pytest ≥487（注意 d-1 后基线已 487，本包改测不增减总数前提下应仍 ≥487）

**编码约束**：
- 不碰 `brain_delivery` / `layer` / `brain` / `settings` / 外部门控
- 不编造：digest 基于 hits，prompt 钉死「不编造」+ `_QUERY_PRIVACY_LINE`
- 失败降级：`gateway.call` 失败返空不抛（`gateway.py` L175–186），`except` 兜意外；降级回 `f"我刚才看了看 {query}。"`，`outcome=success`
- 编码回执落盘 `docs/specs/tasks/2026-08-08-L7-explore真搜索-d2内部深读-Cursor编码回执.md`

编码完工后交 Trae 实施验收（git diff + 数据核对）；维护者相处复验外部开口是否像「看懂了外面」而非念 title/只念 query。

---

*方案审查回复 · 2026-08-08 · 路 A · 明示可编码*
