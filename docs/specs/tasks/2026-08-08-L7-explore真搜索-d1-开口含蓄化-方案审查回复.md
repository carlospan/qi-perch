# L7 explore 真搜索 d-1 · 开口含蓄化——方案审查回复

> **角色**：方案 Agent（Trae）
> **依据**：[Cursor 交叉审查](./2026-08-08-L7-explore真搜索-d1-开口含蓄化-Cursor交叉审查.md)（路 A 无阻塞）+ [任务包](./2026-08-08-L7-explore真搜索-d1-开口含蓄化-任务包.md) + [PR 方案](./2026-08-08-L7-explore真搜索-d1-开口含蓄化-PR方案.md)
> **状态**：审查通过 → **明示可编码**

---

## 审查结论

**路 A 通过，无阻塞。明示可编码。**

逐项核对 Cursor 交叉审查属实：

- **改动点**：仅 drift L288-294 拆 qi_line（成功 `f"我刚才看了看 {found['query']}。"`、空手 `qi_line=summary`），无 scope 蔓延 ✓
- **found['query'] 依赖**：`_fetch_external` L247-253 已存在 ✓
- **B3 仍守**：模板不二次 LLM / 不改 brain_delivery / 内部不说 / 外部 speak+qi_line ✓
- **测试更新**：成功断言改、空手不动、不回归 ✓

---

## 吸收非阻塞建议

- **N3 采纳**：成功 case 断言写显式——`assert "窗边的鸟" not in result["qi_line"]` 且 `assert "窗边的鸟" in result["summary"]`（防回归更硬，与 PR 文字等价）

---

## 编码范围（含 TEMP 收尾）

本次编码包改 2 文件 + 顺带清理 1 处代码 TEMP：

### 1. `qi/action/explore.py`

- **L26 TEMP 收尾**：`EXTERNAL_CURIOSITY_MIN = 0.0` → 改回 **`0.8`**（恢复 d-1 原设计值；Cursor 验证时临时改 0.0，[L26 注释](file:///d:/qi-perch/qi/action/explore.py#L26) 已标「验完改回 0.8」）
- **L288-294 开口含蓄化**（照 PR）：拆 qi_line——成功 `f"我刚才看了看 {found['query']}。"`、空手 `qi_line = summary`

> 说明：L26 改回是恢复 d-1 PR 原值（非新行为变更），属 TEMP 清理，与开口含蓄化同 commit 合理。

### 2. `tests/test_explore_external_branch.py`

- `test_external_when_gates_pass` 成功断言更新（含 N3 显式）：
  ```python
  assert result["qi_line"] == f"我刚才看了看 {query}。"
  assert "窗边的鸟" not in result["qi_line"]   # N3 显式
  assert "窗边的鸟" in result["summary"]         # 溯源完整
  ```
- 空手 case `test_external_empty_failed_capability_still_speaks`：不动

### 不进 commit（维护者手动）

- `qi/config/settings.yaml` TEMP（`cooldown_hours` / `probability`，.gitignore）——维护者相处复验后改回 `cooldown_hours: 6` + `probability: 0.05`

---

## 验收清单

- [ ] `explore.py` L26 `EXTERNAL_CURIOSITY_MIN = 0.8`（TEMP 清理）
- [ ] `explore.py` L288-294 拆 qi_line（成功只含 query、空手 = summary）
- [ ] 成功 qi_line 不含 title；summary 仍含 title（N3 显式断言落实）
- [ ] 空手 qi_line=summary 不变
- [ ] 现有内部/门控/冷却/web=None 测试不回归
- [ ] 全量 pytest ≥487
- [ ] 相处复验（维护者）：再听成功开口「我刚才看了看 {query}。」像走神

---

## 完工回执

`-Cursor编码回执.md`：列落地（L26 改回 + L288-294 + 测试断言）+ pytest 数 + 偏离说明。

---

**明示可编码——Cursor 可按本回复改 `explore.py`（L26 改回 + L288-294）+ 更新测试，写回执完工段。**
