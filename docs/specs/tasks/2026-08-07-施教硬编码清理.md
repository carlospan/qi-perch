# 施教硬编码清理——任务包

> **协作分工（固定流程，详见 `specs/SDD-GUIDE.md` 第二节）：本任务包由方案 Agent（CodeBuddy）起草（出方案），Cursor 按 PR 方案落地编码；方案 Agent 不写 `qi/` 实现代码，Cursor 不重新设计架构（执行编码者固定为 Cursor）。双方过程稿均落盘为文件，Cursor 自行读取目录，无需人工转发。**

---

## Spec（外部行为）

包 15/17 为修施教反转 bug 累积了一批睡眠专项硬编码（正则只认"入睡/躺着"、prompt 硬编码"数到七"、fallback 模板只对睡眠有效）。N5-a（共同回忆声明闸）和 N5-b（检索相关门）落地后，这些睡眠专项代码变成冗余的墙，违反 contract.md "少靠不准说清单 / 只钉存在论级诚实"。

本任务清理睡眠专项硬编码，将施教防护泛化为"任意已存档方向的施教反转检测"：
1. prompt 删掉具体细节（"数到七"/"躺着/不强迫/看天花板"），改回通用约束
2. `detect_sleep_teach_inversion` 泛化为 `detect_teach_inversion`（查任意 recall_relation 反转）
3. fallback 模板改为通用（不提"入睡"具体内容）
4. 通用框架（`infer_recall_relation` / `anchor_teaching_relation` / `_RELATION_HINT`）不删

> 本栏由 CodeBuddy 写（方案侧）。

## 实现（步骤）

见配套 PR 方案文档（`docs/specs/archive/2026-08-07-teach-hardcode-cleanup/包-施教硬编码清理-PR方案.md`）。

## 验收（判据）

- [x] `consciousness_stream.txt` 无"数到七"等具体细节硬编码
- [x] `culture.py` 方向锤不写死"躺着/不强迫/看天花板"原话
- [x] `detect_sleep_teach_inversion` rename 为 `detect_teach_inversion`（别名兼容；逻辑未改——B1-A）
- [x] `_TEACH_INVERSION_FALLBACK` / `_TEACH_INVERSION_CONSTRAINT` 改为通用模板
- [x] 包 15-17 既有施教回归测试仍全绿
- [x] 全量 `pytest` 通过，`ruff` 零问题（编码回执：457 passed）
- [x] contract.md 护栏原则不违反（无具体内容硬编码）

> 方案 Agent 终验前，上列由 Cursor 自测勾选；正式验收以 `archive/.../验收记录.md` 为准。

## HITL 批点

- **无**。清理是既有路线内的去技术债，不改架构方向、不触 R1-R5、不开新阶段。按 SDD-GUIDE §6.1——**可自治（Agent 直接定）**。
